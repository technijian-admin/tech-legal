---
phase: 04-read-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/IReadOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ReadOpBase.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ArgReader.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/CompanyInfoOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/CompanyPreferencesOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ReportOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListCustomersOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListVendorsOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListAccountsOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListItemsOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListInvoicesOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListBillsOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListPaymentsOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/GetTransactionOp.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/RunQueryOp.cs
  - quickbooks/QbConnectService/src/QbConnectService/Program.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/CompanyInfoOpTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/CompanyPreferencesOpTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/ReportOpTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/ListEntityOpsTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/ListTransactionOpsTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/GetTransactionOpTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/RunQueryOpTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/OpRegistrationTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/HostCompanyQueryRs.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/PreferencesQueryRs.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AccountQueryRs.arap.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/GeneralSummaryReportQueryRs.balancesheet.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AgingReportQueryRs.ar.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AgingReportQueryRs.ap.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/VendorQueryRs.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AccountQueryRs.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/ItemQueryRs.normal.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/InvoiceQueryRs.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/BillQueryRs.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/ReceivePaymentQueryRs.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/TransactionQueryRs.byid.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/TransactionQueryRs.byref.multi.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/EmployeeQueryRs.qbxml
autonomous: true

must_haves:
  truths:
    - "op company_info returns company name, address, fiscal-year start month, and QuickBooks edition (from HostRet.ProductName)"
    - "op get_company_preferences returns sales-tax-enabled, decimal places, multi-currency-enabled, and default A/R and A/P accounts"
    - "op report runs ProfitAndLoss, BalanceSheet, AgingAR, AgingAP over an explicit date range or a date macro and returns the parsed report"
    - "op list_customers / list_vendors / list_items / list_accounts return lists with optional active-only / name filter; list_items normalizes polymorphic Item*Ret shapes via the parser's type discriminator"
    - "op list_invoices / list_bills / list_payments return transactions filtered by date range and/or entity"
    - "op get_transaction looks up a transaction by TxnID or RefNumber and returns {matches, count, ambiguous} (never collapses to one row; RefNumber is non-unique)"
    - "op run_query runs a generic entity query over a hard-coded read-only-entity whitelist with simple child-element filters and returns parsed rows"
    - "all read ops are registered as IReadOp DI singletons; the host resolves IEnumerable<IReadOp> with >=12 ops and unique Names"
    - "dotnet build and dotnet test pass on Windows with no QuickBooks SDK installed (every op exercised against FakeRequestProcessor)"
  artifacts:
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/IReadOp.cs"
      provides: "IReadOp interface (Name + RunAsync(args, ct))"
      contains: "interface IReadOp"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ReadOpBase.cs"
      provides: "ReadOpBase: QuerySingleAsync / QueryListAsync / QueryReportAsync + ListResult helpers"
      contains: "abstract class ReadOpBase"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/CompanyInfoOp.cs"
      provides: "company_info op (bundled HostQueryRq + CompanyQueryRq)"
      contains: "company_info"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/CompanyPreferencesOp.cs"
      provides: "get_company_preferences op (PreferencesQueryRq + AccountQueryRq AR/AP follow-up)"
      contains: "get_company_preferences"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ReportOp.cs"
      provides: "report op (ProfitAndLoss / BalanceSheet / AgingAR / AgingAP, exactly-one-of range|macro)"
      contains: "report"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListCustomersOp.cs"
      provides: "list_customers op"
      contains: "list_customers"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListItemsOp.cs"
      provides: "list_items op (polymorphic Item normalization free via parser type)"
      contains: "list_items"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListInvoicesOp.cs"
      provides: "list_invoices op (TxnDateRangeFilter + EntityFilter)"
      contains: "list_invoices"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/GetTransactionOp.cs"
      provides: "get_transaction op (TxnID | RefNumber -> {matches, count, ambiguous, lite})"
      contains: "get_transaction"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/RunQueryOp.cs"
      provides: "run_query op (read-only-entity whitelist + simple child-element filters)"
      contains: "run_query"
    - path: "quickbooks/QbConnectService/src/QbConnectService/Program.cs"
      provides: "DI registration of all 12 read ops as IReadOp singletons"
      contains: "AddSingleton<IReadOp"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Tests/OpRegistrationTests.cs"
      provides: "proves host resolves IEnumerable<IReadOp>, >=12 ops, unique names"
      contains: "IReadOp"
  key_links:
    - from: "Qb/Ops/*Op.cs"
      to: "QbXmlBuilder.Rq / BuildRequest"
      via: "every op builds its *Rq body via QbXmlBuilder.Rq(name, ...content)"
      pattern: "QbXmlBuilder\\.Rq\\("
    - from: "Qb/Ops/ReadOpBase.cs"
      to: "QbConnectionManager.ExecuteAsync / QbListExecutor.RunAsync / QbReportParser.Parse / QbXmlParser.Parse"
      via: "QuerySingleAsync / QueryListAsync / QueryReportAsync helpers"
      pattern: "ExecuteAsync|_listExecutor\\.RunAsync|_reportParser\\.Parse|_xmlParser\\.Parse"
    - from: "Program.cs"
      to: "Qb/Ops/*Op.cs"
      via: "builder.Services.AddSingleton<IReadOp, XxxOp>()"
      pattern: "AddSingleton<IReadOp,"
    - from: "Qb/Ops/ListItemsOp.cs"
      to: "QbXmlParser polymorphic Item type discriminator"
      via: "list_items returns parsed.First.Rows where each row carries a `type` key set by QbXmlParser"
      pattern: "ItemQueryRq"
---

<objective>
Implement every v1 QuickBooks read operation as an in-process `IReadOp` on top of the Phase-3 qbXML engine, and wire them into DI. After this plan: `company_info`, `get_company_preferences`, `report`, `list_customers`/`list_vendors`/`list_items`/`list_accounts`, `list_invoices`/`list_bills`/`list_payments`, `get_transaction`, and `run_query` all exist as classes implementing `IReadOp`, are registered as DI singletons, and are exercised end-to-end against `FakeRequestProcessor`.

Purpose: Reads are the primary value path — Claude runs a QuickBooks read and gets an answer in seconds. Phase 3 built the qbXML engine (`QbXmlBuilder` / `QbXmlParser` / `QbReportParser` / `QbListExecutor` / `QbConnectionManager.ExecuteAsync`); this phase is the thin op layer above it. Phase 5 then exposes these ops over HTTP via an `OpRegistry` built from `IEnumerable<IReadOp>`.

Output: ~15 new source files under `Qb/Ops/`, ~8 new test files, ~12 new constructed qbXML fixtures, and the `Program.cs` DI lines. Zero new NuGet packages.
</objective>

<execution_context>
@C:\Users\rjain\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rjain\.claude/get-shit-done/templates/summary.md

**Executor: Codex CLI** (`codex exec --dangerously-bypass-approvals-and-sandbox` via `quickbooks/dev/run-codex-phase.ps1`).

Repo & git rules (NON-NEGOTIABLE):
- Repo root for git is `C:\VSCode\tech-legal\tech-legal` (NOT a parent). Use absolute paths for all file I/O.
- Stay on the current branch `quickbooks/direct-sdk-integration-2026-05-11`. NEVER `git checkout` another branch, NEVER touch `main`, NEVER `git init` (no nested repo), NEVER `git reset --hard`, NEVER `git add -A` / `git add .` / `git add :/`.
- The working tree contains UNRELATED India-HR work. Every commit in this plan must `git add` **only** the specific files that task created/modified, all under `quickbooks/`. Use explicit per-file `git add` (or `git add quickbooks/QbConnectService/...` paths) — never a wildcard that could sweep in other dirs.
- The CLAUDE.md GitNexus / Client-Portal / "no direct main commit" rules DO NOT apply here — this is brand-new, isolated subsystem code under `quickbooks/`. (The "feature-branch + PR" tech-legal rule is already satisfied: you are on a feature branch.)
- **One git commit per task**, each with a DISTINCT conventional-commit subject. Tasks 1–8 use `feat(04-01): <distinct title>` — NEVER reuse a subject line. If you revise an already-committed task, `git commit --amend` it or use `fix(04-01): ...` / `refactor(04-01): ...` — never a duplicate `feat(04-01)` title.
- Task 9 (final) commits the plan-checkbox tick + the `04-01-SUMMARY.md` as `docs(04-01): phase 4 read ops summary`.
- After each task: run `dotnet build` then `dotnet test` from `C:\VSCode\tech-legal\tech-legal\quickbooks\QbConnectService` (or the solution dir) — both MUST be green with NO QuickBooks SDK installed. Do not commit a task whose build or tests are red. Leave nothing dangling/uncommitted between tasks.

Build/test commands (run from the QbConnectService solution dir):
- `dotnet build` — must succeed (net8.0-windows, x86).
- `dotnet test` — must be green. Test count grows from 76 toward ~95+ as ops are added.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-read-ops/04-RESEARCH.md
@docs/superpowers/specs/2026-05-11-quickbooks-direct-sdk-accounting-design.md

# Phase-3 engine this plan builds on (read these before starting):
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlBuilder.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlParser.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbReportParser.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbListExecutor.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbConnectionManager.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlModels.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlOptions.cs
@quickbooks/QbConnectService/src/QbConnectService.Tests/Fakes/FakeRequestProcessor.cs
@quickbooks/QbConnectService/src/QbConnectService.Tests/QbListExecutorTests.cs
@quickbooks/QbConnectService/src/QbConnectService/Program.cs
</context>

<scope_discipline>
**Phase 4 is ONLY the v1 read ops as in-process implementations.** It builds: `IReadOp` + `ReadOpBase` + `ArgReader` + the 12 concrete read-op classes + their DI registration + tests against the fake/manager + the new qbXML fixtures.

**Phase 4 does NOT build (out of scope — later phases):**
- The `OpRegistry`, any REST controller, `/api/ops/{op}`, `/api/health`, `/api/qbxml`, Kestrel HTTPS, bearer auth — **Phase 5**.
- Any write op, `/dryrun`, the `AllowWrites` gate, the audit log — **Phases 6 & 7**.
- The Python client (`qb_client.py`), the `quickbooks-accounting` Claude skill, dev tooling — **Phase 8**.
- Cert/install/uninstall scripts, deploy runbooks, the on-box live smoke checklist — **Phase 9**.

**Notes for the reviewer:**
- Phase-4 ops are tested **in-process** (call `op.RunAsync(args, default)` directly against a wired `QbConnectionManager` + `FakeRequestProcessor`). "Callable over HTTP" is a Phase-5 concern — do NOT expect an HTTP surface here.
- A per-element `statusCode != 0` (e.g. an `Error` severity) and a zero-row result are **surfaced in the op's result body** (`status.severity == "Error"` / `rows == []`), **never thrown**. This matches READ-01 and API-06. Ops only let exceptions propagate for transport/COM failures (`QbException` / `QbBusyException` / `QbTimeoutException` from `QbConnectionManager`) and for caller-arg validation errors (`ArgumentException`, which Phase 5 will map to 400).
- All new qbXML fixtures in this plan are **constructed** (not live-captured from `10.120.254.13`) — same caveat already applied to the Phase-3 P&L fixture. Each fixture file gets a leading XML comment noting it is constructed and that Phase 9 re-pins element/enum casing against the live host. The MEDIUM-confidence qbXML element/enum names below (e.g. `GeneralSummaryReportType=ProfitAndLossStandard`, `AgingReportType=ARAgingSummary`, `ActiveStatus=ActiveOnly|InactiveOnly|All`, `TxnDateRangeFilter`, `RefNumberFilter`, `IncludeLineItems`, `IsMultiCurrencyOn`) are training/OSR-derived; build the fixtures and ops to match them now, and note in code XML-doc that Phase 9 confirms them.
</scope_discipline>

<tasks>

<task type="auto">
  <name>Task 1: IReadOp + ReadOpBase + ArgReader + company_info + test harness + DI scaffolding</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/IReadOp.cs
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ReadOpBase.cs
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ArgReader.cs
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/CompanyInfoOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/CompanyInfoOpTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/HostCompanyQueryRs.qbxml
  </files>
  <action>
Create the op abstraction and the first op, plus the shared test harness, and register `company_info` in DI.

1. `Qb/Ops/IReadOp.cs` — `namespace QbConnectService.Qb.Ops;`
   ```csharp
   public interface IReadOp
   {
       string Name { get; }   // e.g. "company_info" — Phase 5's OpRegistry key
       Task<object?> RunAsync(IReadOnlyDictionary<string, object?> args, CancellationToken ct = default);
   }
   ```

2. `Qb/Ops/ArgReader.cs` — static helper over `IReadOnlyDictionary<string, object?>` (handle args that arrive as `string`, `JsonElement`, `bool`, `DateOnly`/`DateTime`, or `IReadOnlyDictionary<string,object?>`):
   - `string? String(IReadOnlyDictionary<string,object?> a, string key)` — `a.TryGetValue` → `null` if missing/null/empty; coerce `JsonElement` to string; trim.
   - `bool? Bool(...)` — accept `bool`, `"true"`/`"false"` (case-insensitive), `JsonElement` true/false.
   - `DateOnly? Date(...)` — accept `DateOnly`, `DateTime`, or a `"yyyy-MM-dd"` string (`DateOnly.TryParse` / `DateOnly.TryParseExact`); return `null` on absent, throw `ArgumentException` on a present-but-unparseable value.
   - `IReadOnlyDictionary<string,object?>? Dict(...)` — return a nested dictionary if present (coerce `JsonElement` object via `EnumerateObject`).

3. `Qb/Ops/ReadOpBase.cs` — `abstract class ReadOpBase : IReadOp`, primary-constructor-injecting `QbXmlBuilder builder, QbConnectionManager manager, QbXmlParser xmlParser, QbReportParser reportParser, QbListExecutor listExecutor` (store in fields). Abstract `Name` and `RunAsync`. Three protected helpers:
   - `protected async Task<ParsedQbXmlResponse> QuerySingleAsync(XElement rq, CancellationToken ct) => _xmlParser.Parse(await _manager.ExecuteAsync(_builder.BuildRequest(rq), ct));`
   - `protected async Task<ParsedQbXmlResponse> QuerySingleAsync(IEnumerable<XElement> rqs, CancellationToken ct) => _xmlParser.Parse(await _manager.ExecuteAsync(_builder.BuildRequest(rqs), ct));` (overload — multiple `*Rq` in one message; `parsed.Elements` then has multiple `*Rs`)
   - `protected Task<ParsedQbXmlResponse> QueryListAsync(XElement rq, bool? ownerIdZero, CancellationToken ct) => _listExecutor.RunAsync(rq, ownerIdZero, ct);`
   - `protected async Task<ParsedReport> QueryReportAsync(XElement rq, CancellationToken ct) => _reportParser.Parse(await _manager.ExecuteAsync(_builder.BuildRequest(rq), ct));`
   - `protected static object ListResult(ParsedQbXmlResponse r)` → `new Dictionary<string,object?> { ["status"]=r.First.Status, ["rows"]=r.First.Rows, ["count"]=r.First.Rows.Count, ["rawSpilledTo"]=r.RawSpilledTo }`. (Confirm the actual property names on `ParsedQbXmlResponse`/`ParsedElement`/`QbStatus` by reading `QbXmlModels.cs`; adapt accessor names to match. `BuildRequest` accepts both `XElement` and `IEnumerable<XElement>` — confirm the overloads exist on `QbXmlBuilder`; if only one form exists, use whichever is present and adjust.)
   - Add an XML-doc comment on the class noting: status (`statusCode`/`statusSeverity`/`statusMessage`) and zero-row are returned in the body, never thrown; only transport/COM exceptions propagate (READ-01 / API-06).

4. `Qb/Ops/CompanyInfoOp.cs` — `sealed class CompanyInfoOp(QbXmlBuilder b, QbConnectionManager m, QbXmlParser xp, QbReportParser rp, QbListExecutor le) : ReadOpBase(b, m, xp, rp, le)`:
   - `Name => "company_info"`.
   - `RunAsync`: build ONE message with two `*Rq` — `QuerySingleAsync(new[] { QbXmlBuilder.Rq("HostQueryRq"), QbXmlBuilder.Rq("CompanyQueryRq") }, ct)`. From `parsed.Elements`, find the `HostQueryRs` element and the `CompanyQueryRs` element (match by `Name`); take `host = hostRs.Rows.FirstOrDefault() ?? new()` (`HostRet`) and `co = companyRs.Rows.FirstOrDefault() ?? new()` (`CompanyRet`).
   - Return `Dictionary<string,object?>` with: `status` (the `CompanyQueryRs` element status), `companyName` = `co["CompanyName"]`, `legalCompanyName` = `co["LegalCompanyName"]`, `address` = `co["Address"]`, `legalAddress` = `co["LegalAddress"]`, `phone` = `co["Phone"]`, `email` = `co["Email"]`, `fiscalYearStartMonth` = `co["FirstMonthFiscalYear"]`, `incomeTaxYearStartMonth` = `co["FirstMonthIncomeTaxYear"]`, `taxForm` = `co["TaxForm"]`, `companyType` = `co["CompanyType"]`, `edition` = `host["ProductName"]`, `quickBooksMajorVersion` = `host["MajorVersion"]`, `quickBooksMinorVersion` = `host["MinorVersion"]`, `country` = `host["Country"]`, `supportedQbXmlVersions` = `host["SupportedQBXMLVersionList"]` (whatever the parser produces — list or nested dict), `rawCompanyRet` = `co`, `rawHostRet` = `host`. Use `GetValueOrDefault` for all dictionary reads (rows are `Dictionary<string,object?>`). XML-doc note: `CompanyRet` does not carry the edition string; `HostRet.ProductName` is the source (READ-04). Phase 9 re-pins the exact `ProductName` format.

5. `Program.cs` — after the Phase-3 singleton registrations and before `var app = builder.Build();`, add:
   ```csharp
   // Phase 4: read ops (registered as IReadOp so Phase 5's OpRegistry is `IEnumerable<IReadOp>` -> dictionary by Name)
   builder.Services.AddSingleton<IReadOp, CompanyInfoOp>();
   ```
   Add the `using QbConnectService.Qb.Ops;` import. (Later tasks add the remaining `AddSingleton<IReadOp, ...>()` lines under this comment.)

6. `Tests/OpTestHarness.cs` — `namespace QbConnectService.Tests;` — a `static class OpTestHarness` (or a small instantiable harness) that mirrors `QbListExecutorTests.CreateExecutor()` and additionally builds the ops:
   - `public static (FakeRequestProcessor Fake, QbConnectionManager Manager, IReadOnlyDictionary<string,IReadOp> Ops) Create(QbXmlOptions? options = null)`:
     build `FakeRequestProcessor fake`; `QbConnectionManager manager` with the same `Options.Create(new QbOptions{ AppId="app", AppName="QbConnectService", CompanyFilePath=@"C:\co.QBW" })`, `Options.Create(new RequestOptions{ TimeoutSeconds=30, BusyWaitSeconds=5 })`, `NullLogger<QbConnectionManager>.Instance`, and `() => fake`; `QbXmlOptions xo = options ?? new QbXmlOptions{ /* mirror QbListExecutorTests defaults: Version, MaxReturned=1, MaxResponseBytes (large), SpillPath=null */ }`; `var builder = new QbXmlBuilder(Options.Create(xo))`, `var xp = new QbXmlParser(Options.Create(xo))`, `var rp = new QbReportParser()`, `var spiller = new QbResponseSpiller(...)`, `var le = new QbListExecutor(manager, xp, spiller, Options.Create(xo), NullLogger<QbListExecutor>.Instance)` — **read `QbListExecutorTests.cs` and the Phase-3 ctors and copy them exactly** (constructor signatures matter; do not guess).
     Then build each op with `(builder, manager, xp, rp, le)` and return them keyed by `Name`. As more ops are added in later tasks, add them to this dictionary.
   - `public static string Fixture(string name) => File.ReadAllText(Path.Combine(AppContext.BaseDirectory, "Fixtures", "qbxml", name));` (copy the existing `Fixture` helper convention from the Phase-3 tests — check whether fixtures are copied to output via the `.csproj` `<Content>`/`<None CopyToOutputDirectory>` and reuse the same pattern; if not, add the `.qbxml` to the test `.csproj` so it's copied to the bin dir).

7. `Tests/CompanyInfoOpTests.cs` — `[Fact]`s:
   - `company_info_returns_company_fields_and_edition`: `Create()`, `fake.AddResponse("HostQueryRq", ...)` and `fake.AddResponse("CompanyQueryRq", ...)` — **or**, since both `*Rq` are in ONE message, the fake matches by the first/each request name; mirror however `FakeRequestProcessor` routes a multi-`*Rq` message (read `FakeRequestProcessor.cs` — it likely matches by the first `*Rq` child name). If the fake keys a whole message by its first `*Rq` (`HostQueryRq`), then `fake.AddResponse("HostQueryRq", Fixture("HostCompanyQueryRs.qbxml"))` returns the single combined `*Rs` document. Use `OpTestHarness.Fixture("HostCompanyQueryRs.qbxml")` which contains ONE `<QBXML><QBXMLMsgsRs>` with both a `<HostQueryRs>` (with `<HostRet><ProductName>QuickBooks Enterprise Solutions: Manufacturing and Wholesale 24.0</ProductName><MajorVersion>34</MajorVersion><MinorVersion>0</MinorVersion><Country>US</Country>...</HostRet>`) and a `<CompanyQueryRs>` (with `<CompanyRet><CompanyName>Acme Roofing Inc</CompanyName><Address><Addr1>...</Addr1><City>Irvine</City><State>CA</State><PostalCode>92614</PostalCode></Address><FirstMonthFiscalYear>January</FirstMonthFiscalYear>...</CompanyRet>`). Assert `result["companyName"] == "Acme Roofing Inc"`, `result["edition"]` contains `"Enterprise"`, `result["fiscalYearStartMonth"] == "January"`, and the captured request (`fake.ProcessRequests[0]`) contains both `<HostQueryRq` and `<CompanyQueryRq`. Wrap in `try/finally { await manager.DisposeAsync(); }`.
   - The fixture file `Fixtures/qbxml/HostCompanyQueryRs.qbxml` starts with `<!-- CONSTRUCTED fixture (not live-captured). Phase 9 re-pins HostRet.ProductName format / element casing against 10.120.254.13. -->`.

Run `dotnet build` + `dotnet test` — green. Commit only the 8 files above with `git add <each path>` then `git commit -m "feat(04-01): IReadOp + ReadOpBase + ArgReader + company_info + DI"`.
  </action>
  <verify>
`dotnet build` succeeds and `dotnet test` is green from the QbConnectService solution dir, with NO QuickBooks SDK installed. New test class `CompanyInfoOpTests` runs and passes (`company_info_returns_company_fields_and_edition`). `git log -1 --oneline` shows `feat(04-01): IReadOp + ReadOpBase + ArgReader + company_info + DI`. `git status` shows only the 8 expected files were committed (no India-HR files).
  </verify>
  <done>
`IReadOp`, `ReadOpBase` (with `QuerySingleAsync`/`QueryListAsync`/`QueryReportAsync`/`ListResult`), `ArgReader`, and `CompanyInfoOp` exist; `company_info` is registered as an `IReadOp` singleton in `Program.cs`; the `OpTestHarness` helper builds wired ops; `CompanyInfoOpTests` proves `company_info` returns company name, address, fiscal-year start month, and the QuickBooks edition from `HostRet.ProductName`. Build + tests green. One commit, distinct title. (Covers READ-04.)
  </done>
</task>

<task type="auto">
  <name>Task 2: get_company_preferences op (PreferencesQueryRq + AR/AP AccountQueryRq follow-up)</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/CompanyPreferencesOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/CompanyPreferencesOpTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/PreferencesQueryRs.qbxml
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AccountQueryRs.arap.qbxml
  </files>
  <action>
1. `Qb/Ops/CompanyPreferencesOp.cs` — `sealed class CompanyPreferencesOp(...) : ReadOpBase(...)`, `Name => "get_company_preferences"`. `RunAsync(args, ct)`:
   - First call: `var prefParsed = await QuerySingleAsync(QbXmlBuilder.Rq("PreferencesQueryRq"), ct);` → `var p = prefParsed.First.Rows.FirstOrDefault() ?? new();` (`PreferencesRet`, a nested dictionary). Pull the sub-blocks defensively (cast `p.GetValueOrDefault("SalesTaxPreferences")` etc. to `IReadOnlyDictionary<string,object?>` — write a small `static IReadOnlyDictionary<string,object?>? AsDict(object? o)` local that returns the dict or null): `salesTax`, `accounting` (`AccountingPreferences`), `multiCcy` (`MultiCurrencyPreferences`), `purchases` (`PurchasesAndVendorsPreferences`), `itemsInv` (`ItemsAndInventoryPreferences`).
   - Second call (AR/AP follow-up — READ-05's "default A/R and A/P accounts"): `var arApParsed = await QueryListAsync(QbXmlBuilder.Rq("AccountQueryRq", new XElement("AccountType", "AccountsReceivable")), ownerIdZero: null, ct);` and one for `"AccountsPayable"` — OR a single `AccountQueryRq` with two `<AccountType>` children if that's allowed; do TWO separate queries to keep it simple. From each, take `.First.Rows.FirstOrDefault()` as the default AR / AP account (rows are `AccountRet` dicts; surface the whole dict plus `FullName`).
   - Return `Dictionary<string,object?>`:
     - `status` = `prefParsed.First.Status`
     - `salesTaxEnabled` = `salesTax is not null` (block present ⇔ sales tax on)
     - `defaultItemSalesTaxRef` = `salesTax?.GetValueOrDefault("DefaultItemSalesTaxRef")`
     - `multiCurrencyEnabled` = parse `multiCcy?.GetValueOrDefault("IsMultiCurrencyOn")` as bool ("true"/"false", case-insensitive) ?? `false`
     - `homeCurrencyRef` = `multiCcy?.GetValueOrDefault("HomeCurrencyRef")`
     - `decimalPlaces` = whatever decimal/quantity-decimals field is present in `itemsInv` or `accounting` (best-effort; if none, `null`) — XML-doc note: Phase 9 pins the exact source field name (see RESEARCH Open Question 3)
     - `classTrackingOn` = `accounting?.GetValueOrDefault("IsUsingClassTracking")`
     - `requireAccounts` = `accounting?.GetValueOrDefault("IsRequiringAccounts")`
     - `useAccountNumbers` = `accounting?.GetValueOrDefault("IsUsingAccountNumbers")`
     - `defaultDiscountAccountRef` = `purchases?.GetValueOrDefault("DefaultDiscountAccountRef")`
     - `defaultArAccount` = the AR `AccountRet` dict (or `{ fullName: ..., listId: ... }`), with `defaultArAccountSource = "AccountQuery"`
     - `defaultApAccount` = the AP `AccountRet` dict, `defaultApAccountSource = "AccountQuery"`
     - `rawPreferencesRet` = `p`
   - XML-doc note: `PreferencesRet` has no clean "default A/R / default A/P account" element, so READ-05's default AR/AP comes from `AccountQueryRq` filtered by `AccountType` — documented data source. Field names `IsMultiCurrencyOn`/`IsUsingClassTracking`/`DefaultItemSalesTaxRef` are training/OSR-derived; Phase 9 confirms.

2. `Program.cs` — add `builder.Services.AddSingleton<IReadOp, CompanyPreferencesOp>();` under the Phase-4 comment.

3. `OpTestHarness.cs` — add `CompanyPreferencesOp` to the ops dictionary built by `Create()`.

4. Fixtures (each with the `<!-- CONSTRUCTED fixture ... Phase 9 re-pins ... -->` header):
   - `PreferencesQueryRs.qbxml` — one `<PreferencesQueryRs statusCode="0" ...>` with a `<PreferencesRet>` containing `<SalesTaxPreferences><DefaultItemSalesTaxRef><FullName>CA Sales Tax</FullName></DefaultItemSalesTaxRef>...</SalesTaxPreferences>`, `<AccountingPreferences><IsUsingClassTracking>true</IsUsingClassTracking><IsRequiringAccounts>true</IsRequiringAccounts><IsUsingAccountNumbers>false</IsUsingAccountNumbers></AccountingPreferences>`, `<MultiCurrencyPreferences><IsMultiCurrencyOn>false</IsMultiCurrencyOn></MultiCurrencyPreferences>`, `<PurchasesAndVendorsPreferences>...</PurchasesAndVendorsPreferences>`, `<ItemsAndInventoryPreferences>...</ItemsAndInventoryPreferences>`.
   - `AccountQueryRs.arap.qbxml` — TWO files? No — keep ONE fixture with two `AccountRet` rows (`<AccountRet><Name>Accounts Receivable</Name><FullName>Accounts Receivable</FullName><AccountType>AccountsReceivable</AccountType>...</AccountRet>` and one with `AccountsPayable`). The test will `AddResponses("AccountQueryRq", arApFixtureForReceivable, arApFixtureForPayable)` — i.e. produce two single-row fixtures inline in the test OR a single fixture with one AR row and a second fixture with one AP row. **Simplest:** create the file `AccountQueryRs.arap.qbxml` containing ONLY the AR row, and inline the AP one-row response as a string literal in the test (mirroring how `QbListExecutorTests` inlines small responses). Adjust the file name/content to whatever the test ergonomics make cleanest — the requirement is: the op fires two `AccountQueryRq` (one `AccountType=AccountsReceivable`, one `AccountType=AccountsPayable`) and surfaces both default accounts.

5. `Tests/CompanyPreferencesOpTests.cs`:
   - `get_company_preferences_returns_salesTax_decimals_multiCurrency_and_default_ar_ap`: `Create()`; `fake.AddResponse("PreferencesQueryRq", Fixture("PreferencesQueryRs.qbxml"))`; `fake.AddResponses("AccountQueryRq", arResponse, apResponse)`. Call `ops["get_company_preferences"].RunAsync(new Dictionary<string,object?>(), default)`. Assert `result["salesTaxEnabled"] == true`, `result["multiCurrencyEnabled"] == false`, `result["defaultArAccount"]` has `FullName == "Accounts Receivable"`, `result["defaultApAccount"]` has `FullName == "Accounts Payable"`, and `result["decimalPlaces"]` is non-throwing (may be null). Assert `fake.ProcessRequests` contains a `<PreferencesQueryRq` and two `<AccountQueryRq` (one with `<AccountType>AccountsReceivable</AccountType>`, one with `AccountsPayable`). `try/finally { await manager.DisposeAsync(); }`.

`dotnet build` + `dotnet test` green. `git add` the 6 files, `git commit -m "feat(04-01): get_company_preferences op"`.
  </action>
  <verify>
`dotnet build` + `dotnet test` green. `CompanyPreferencesOpTests` passes. `git log -1 --oneline` shows `feat(04-01): get_company_preferences op` (distinct from Task 1's title). `git status` shows only the 6 expected files committed.
  </verify>
  <done>
`get_company_preferences` exists, registered as an `IReadOp` singleton, returns sales-tax-enabled, decimal places (best-effort), multi-currency-enabled, and the default A/R and A/P accounts (via the `AccountQueryRq` AR/AP follow-up) plus `rawPreferencesRet`; tests prove it. Build + tests green. One commit, distinct title. (Covers READ-05.)
  </done>
</task>

<task type="auto">
  <name>Task 3: report op (ProfitAndLoss / BalanceSheet / AgingAR / AgingAP)</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ReportOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/ReportOpTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/GeneralSummaryReportQueryRs.balancesheet.qbxml
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AgingReportQueryRs.ar.qbxml
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AgingReportQueryRs.ap.qbxml
  </files>
  <action>
1. `Qb/Ops/ReportOp.cs` — `sealed class ReportOp(...) : ReadOpBase(...)`, `Name => "report"`. `RunAsync(args, ct)`:
   - `var type = ArgReader.String(args, "type") ?? throw new ArgumentException("report: 'type' is required (ProfitAndLoss|BalanceSheet|AgingAR|AgingAP).");`
   - `var from = ArgReader.Date(args, "fromDate"); var to = ArgReader.Date(args, "toDate"); var macro = ArgReader.String(args, "dateMacro");`
   - Validate: `bool hasRange = from is not null && to is not null;` — require **exactly one** of `(from AND to)` or `(macro)`, and reject a half range:
     `if ((from is not null) != (to is not null)) throw new ArgumentException("report: fromDate and toDate must be supplied together.");`
     `if (hasRange == (macro is not null)) throw new ArgumentException("report: supply exactly one of (fromDate+toDate) or dateMacro.");`
   - Build the date child: `XElement dateChild = macro is not null ? new XElement("ReportDateMacro", macro) : new XElement("ReportPeriod", new XElement("FromReportDate", from!.Value.ToString("yyyy-MM-dd")), new XElement("ToReportDate", to!.Value.ToString("yyyy-MM-dd")));`
   - `switch` on `type` (case-sensitive; throw `ArgumentException` on unknown):
     - `"ProfitAndLoss"` → `QbXmlBuilder.Rq("GeneralSummaryReportQueryRq", new XElement("GeneralSummaryReportType", "ProfitAndLossStandard"), dateChild)`
     - `"BalanceSheet"` → `QbXmlBuilder.Rq("GeneralSummaryReportQueryRq", new XElement("GeneralSummaryReportType", "BalanceSheetStandard"), dateChild)`
     - `"AgingAR"` → `QbXmlBuilder.Rq("AgingReportQueryRq", new XElement("AgingReportType", "ARAgingSummary"), dateChild)`
     - `"AgingAP"` → `QbXmlBuilder.Rq("AgingReportQueryRq", new XElement("AgingReportType", "APAgingSummary"), dateChild)`
   - `var report = await QueryReportAsync(rq, ct);` → return `new Dictionary<string,object?> { ["type"] = type, ["report"] = report };` (`report` is `ParsedReport`, already JSON-serializable).
   - XML-doc note: the report-query envelope (`<{Report}QueryRq><{Report}Type>...</...><ReportDateMacro>...</...|<ReportPeriod>...</...></{Report}QueryRq>`) is verified from an Intuit SDK sample; the `*Type` enum values (`ProfitAndLossStandard`, `BalanceSheetStandard`, `ARAgingSummary`, `APAgingSummary`) and the aging-report date element (`ReportPeriod` vs a `ReportAgingAsOf`/range) are MEDIUM-confidence / training-OSR-derived — Phase 9 re-pins against the live host (RESEARCH Open Question 2). Don't validate the `dateMacro` string against an enum — forward QB's error if it's wrong.
   - Reports do NOT go through `QbListExecutor` (no iterators) — `QueryReportAsync` is a single-shot `ExecuteAsync` + `QbReportParser.Parse`.

2. `Program.cs` — add `builder.Services.AddSingleton<IReadOp, ReportOp>();`.

3. `OpTestHarness.cs` — add `ReportOp` to the ops dictionary.

4. Fixtures (each with the constructed-fixture header comment): build them by copying the *shape* of the existing `GeneralSummaryReportQueryRs.pnl.qbxml` (a `<ReportRet>` with `<ColDesc colID="...">` headers, `<ReportData>` with `<DataRow>`/`<SubtotalRow>`/`<TotalRow>` containing `<ColData colID="..." value="..."/>`):
   - `GeneralSummaryReportQueryRs.balancesheet.qbxml` — a small balance-sheet `ReportRet` (a couple of rows; `QbReportParser` handles the `*ReportQueryRs` suffix generically).
   - `AgingReportQueryRs.ar.qbxml` and `AgingReportQueryRs.ap.qbxml` — small aging `ReportRet`s (AR / AP). Note `QbReportParser` matches `*ReportQueryRs` suffix, so `AgingReportQueryRs` parses fine.

5. `Tests/ReportOpTests.cs`:
   - `report_profit_and_loss_with_date_range_builds_GeneralSummaryReportQueryRq`: `Create()`; `fake.AddResponse("GeneralSummaryReportQueryRq", Fixture("GeneralSummaryReportQueryRs.pnl.qbxml"))`; call `RunAsync(new Dictionary<string,object?>{ ["type"]="ProfitAndLoss", ["fromDate"]="2025-01-01", ["toDate"]="2025-12-31" }, default)`. Assert `result["type"] == "ProfitAndLoss"`, `result["report"]` is a `ParsedReport` with at least one column/row, and the captured request has `<GeneralSummaryReportType>ProfitAndLossStandard</GeneralSummaryReportType>` and `<ReportPeriod><FromReportDate>2025-01-01</FromReportDate><ToReportDate>2025-12-31</ToReportDate></ReportPeriod>`.
   - `report_balance_sheet_with_date_macro_builds_ReportDateMacro`: `fake.AddResponse("GeneralSummaryReportQueryRq", Fixture("GeneralSummaryReportQueryRs.balancesheet.qbxml"))`; `RunAsync({ type:"BalanceSheet", dateMacro:"ThisFiscalYear" })`. Assert request has `<GeneralSummaryReportType>BalanceSheetStandard</GeneralSummaryReportType>` and `<ReportDateMacro>ThisFiscalYear</ReportDateMacro>` and NO `<ReportPeriod>`.
   - `report_aging_ar_and_ap`: two responses (`AgingReportQueryRs.ar.qbxml`, `.ap.qbxml`); `RunAsync({ type:"AgingAR", dateMacro:"Today" })` → request `<AgingReportType>ARAgingSummary</AgingReportType>`; `RunAsync({ type:"AgingAP", toDate:"2025-12-31", fromDate:"2025-01-01" })` → `<AgingReportType>APAgingSummary</AgingReportType>` + `<ReportPeriod>`. (Use a fresh `Create()` per call or a fake with two queued `AgingReportQueryRq` responses — match the fake's keying.)
   - `report_rejects_bad_date_args`: `RunAsync({ type:"ProfitAndLoss" })` → throws `ArgumentException` (neither); `RunAsync({ type:"ProfitAndLoss", fromDate:"2025-01-01", toDate:"2025-12-31", dateMacro:"ThisMonth" })` → throws (both); `RunAsync({ type:"ProfitAndLoss", fromDate:"2025-01-01" })` → throws (half range). `RunAsync({ type:"Bogus", dateMacro:"ThisMonth" })` → throws (unknown type). `RunAsync(new Dictionary<string,object?>())` → throws ('type' required).
   - `try/finally { await manager.DisposeAsync(); }` in each.

`dotnet build` + `dotnet test` green. `git add` the 7 files; `git commit -m "feat(04-01): report op (ProfitAndLoss / BalanceSheet / AgingAR / AgingAP)"`.
  </action>
  <verify>
`dotnet build` + `dotnet test` green. `ReportOpTests` passes (4+ facts incl. the date-arg validation throws). `git log -1 --oneline` shows the distinct `feat(04-01): report op ...` title. `git status` shows only the 7 expected files committed.
  </verify>
  <done>
`report` is one op that runs `ProfitAndLoss` / `BalanceSheet` / `AgingAR` / `AgingAP` over an explicit date range OR a date macro (exactly-one-of enforced) and returns the parsed `ParsedReport`; registered as an `IReadOp` singleton; tests prove all four types build the right `*ReportQueryRq` + `*Type` value and that bad date-arg combos throw. Build + tests green. One commit, distinct title. (Covers READ-06.)
  </done>
</task>

<task type="auto">
  <name>Task 4: list_customers / list_vendors / list_accounts ops</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListCustomersOp.cs
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListVendorsOp.cs
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListAccountsOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/ListEntityOpsTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/VendorQueryRs.qbxml
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/AccountQueryRs.qbxml
  </files>
  <action>
Create three list ops sharing an `ActiveStatus` + `NameFilter` filter shape. Add a small `protected static XElement? ActiveStatusElement(IReadOnlyDictionary<string,object?> args)` and `protected static XElement? NameFilterElement(IReadOnlyDictionary<string,object?> args)` helper to `ReadOpBase` (or a `static class QueryFilters` in `Qb/Ops/`) so the three ops don't duplicate the filter-building code:
- `ActiveStatusElement`: read `ArgReader.String(args, "activeStatus")` (default `"All"`), normalize: `"Active"`/`"ActiveOnly"` → `"ActiveOnly"`, `"Inactive"`/`"InactiveOnly"` → `"InactiveOnly"`, `"All"` → `"All"` (anything else → throw `ArgumentException`). **Always** return `new XElement("ActiveStatus", normalized)` (emit it explicitly even for the default so the request is unambiguous and easy to assert).
- `NameFilterElement`: if `ArgReader.String(args, "name")` is non-empty → `new XElement("NameFilter", new XElement("MatchCriterion", ArgReader.String(args, "nameMatch") ?? "Contains"), new XElement("Name", name))` (accepted criteria: `StartsWith`/`Contains`/`EndsWith` — don't validate, forward QB's error). Else `null`.

1. `Qb/Ops/ListCustomersOp.cs` — `Name => "list_customers"`. `RunAsync(args, ct)`:
   `var rq = QbXmlBuilder.Rq("CustomerQueryRq");` `rq.Add(ActiveStatusElement(args)!);` `if (NameFilterElement(args) is { } nf) rq.Add(nf);` — **do NOT add `MaxReturned` or iterator attrs** (`QbListExecutor.RunAsync` does that). `var parsed = await QueryListAsync(rq, ownerIdZero: null /* falls back to QbXmlOptions.OwnerIdZero */, ct);` `return ListResult(parsed);`
2. `Qb/Ops/ListVendorsOp.cs` — identical but `VendorQueryRq`, `Name => "list_vendors"`.
3. `Qb/Ops/ListAccountsOp.cs` — `AccountQueryRq`, `Name => "list_accounts"`; same `ActiveStatus`; `NameFilter` is also valid on `AccountQueryRq` so include it; additionally accept an optional `accountType` passthrough → `new XElement("AccountType", value)` (no validation). XML-doc note: account name filtering via `FullNameWithChildren` is awkward — v1 uses `ActiveStatus` + `NameFilter` + optional `AccountType`.

4. `Program.cs` — add three `AddSingleton<IReadOp, ...>()` lines (`ListCustomersOp`, `ListVendorsOp`, `ListAccountsOp`).
5. `OpTestHarness.cs` — add the three ops to the dictionary.
6. Fixtures (constructed-header comment):
   - `VendorQueryRs.qbxml` — one `<VendorQueryRs statusCode="0" ... iteratorRemainingCount="0"><VendorRet><ListID>...</ListID><Name>Office Depot</Name><FullName>Office Depot</FullName>...</VendorRet><VendorRet>...second...</VendorRet></VendorQueryRs>` (two rows).
   - `AccountQueryRs.qbxml` — two `<AccountRet>` rows (e.g. `Checking` / `Accounts Receivable`) with `Name`, `FullName`, `AccountType`.
   - (`list_customers` reuses the existing `Fixtures/qbxml/CustomerQueryRs.normal.qbxml`.)
7. `Tests/ListEntityOpsTests.cs`:
   - `list_customers_returns_rows_and_applies_filters`: `Create()`; `fake.AddResponse("CustomerQueryRq", Fixture("CustomerQueryRs.normal.qbxml"))`; `RunAsync(new Dictionary<string,object?>{ ["activeStatus"]="Active", ["name"]="Acme" }, default)`. Assert `result["rows"]` has the expected count, `((List<Dictionary<string,object?>>)result["rows"])[0]["FullName"]` (or `Name` — match the fixture), and the captured request (`XDocument.Parse(fake.ProcessRequests[0]).Root!.Element("QBXMLMsgsRq")!.Element("CustomerQueryRq")!`) has `ActiveStatus == "ActiveOnly"`, `NameFilter/Name == "Acme"`, and `iterator="Start"` attribute (added by `QbListExecutor`); `Assert.Single(fake.ProcessRequests)`.
   - `list_customers_defaults_active_status_to_All`: `RunAsync(new Dictionary<string,object?>())` → request `ActiveStatus == "All"`.
   - `list_customers_rejects_bad_active_status`: `RunAsync({ activeStatus:"Bogus" })` → throws `ArgumentException`.
   - `list_vendors_returns_rows` (using `VendorQueryRs.qbxml`): assert two rows, request is `<VendorQueryRq>` with `ActiveStatus`.
   - `list_accounts_returns_rows_and_account_type_filter` (using `AccountQueryRs.qbxml`): `RunAsync({ accountType:"Bank" })` → request `<AccountQueryRq>` has `<AccountType>Bank</AccountType>` + `<ActiveStatus>All</ActiveStatus>`.
   - `try/finally { await manager.DisposeAsync(); }` in each.

`dotnet build` + `dotnet test` green. `git add` the 8 files; `git commit -m "feat(04-01): list_customers / list_vendors / list_accounts ops"`.
  </action>
  <verify>
`dotnet build` + `dotnet test` green. `ListEntityOpsTests` passes (incl. the `ActiveStatus` default `All` and the `iterator="Start"` assertions). `git log -1 --oneline` shows the distinct title. `git status` shows only the 8 expected files committed.
  </verify>
  <done>
`list_customers`, `list_vendors`, `list_accounts` exist as `IReadOp` singletons, return their lists with optional `activeStatus` (default `All`) / `name` filters (and `accountType` for accounts), go through `QbListExecutor` (iterator-safe), and surface status + rows + `rawSpilledTo`; tests prove the filters land in the request and a single page → one `ProcessRequests` entry. Build + tests green. One commit, distinct title. (Partial READ-07.)
  </done>
</task>

<task type="auto">
  <name>Task 5: list_items op (polymorphic Item normalization via parser type discriminator)</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListItemsOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/ListEntityOpsTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/ItemQueryRs.normal.qbxml
  </files>
  <action>
1. `Qb/Ops/ListItemsOp.cs` — `sealed class ListItemsOp(...) : ReadOpBase(...)`, `Name => "list_items"`. `RunAsync(args, ct)`:
   `var rq = QbXmlBuilder.Rq("ItemQueryRq");` `rq.Add(ActiveStatusElement(args)!);` `if (NameFilterElement(args) is { } nf) rq.Add(nf);` `var parsed = await QueryListAsync(rq, ownerIdZero: null, ct);` `return ListResult(parsed);`
   - **No extra polymorphism work** — `QbXmlParser` already collapses `ItemServiceRet`/`ItemInventoryRet`/`ItemNonInventoryRet`/`ItemInventoryAssemblyRet`/`ItemFixedAssetRet`/`ItemOtherChargeRet`/`ItemSubtotalRet`/`ItemDiscountRet`/`ItemPaymentRet`/`ItemSalesTaxRet`/`ItemSalesTaxGroupRet`/`ItemGroupRet` into one row shape per item with a `type` key (`"Service"`,`"Inventory"`,`"NonInventory"`,`"InventoryAssembly"`,`"FixedAsset"`,`"OtherCharge"`,`"Subtotal"`,`"Discount"`,`"Payment"`,`"SalesTax"`,`"SalesTaxGroup"`,`"Group"`). XML-doc note: list_items relies on `QbXmlParser`'s `type` discriminator for the polymorphic `Item*Ret` shapes — verify the discriminator key/value names against `QbXmlParser.cs`.

2. `Program.cs` — add `builder.Services.AddSingleton<IReadOp, ListItemsOp>();`.
3. `OpTestHarness.cs` — add `ListItemsOp` to the ops dictionary.
4. `Fixtures/qbxml/ItemQueryRs.normal.qbxml` (constructed-header comment) — a single-type-ish response: one `<ItemServiceRet>` and one more `<ItemServiceRet>` (or a `<ItemNonInventoryRet>`) — a simple list. (The polymorphic-mix case reuses the existing `Fixtures/qbxml/ItemQueryRs.polymorphic.qbxml`.)
5. `Tests/ListEntityOpsTests.cs` — add:
   - `list_items_normalizes_polymorphic_shapes`: `Create()`; `fake.AddResponse("ItemQueryRq", Fixture("ItemQueryRs.polymorphic.qbxml"))`; `RunAsync(new Dictionary<string,object?>())`. Assert `rows[0]["type"]` and `rows[1]["type"]` match the fixture's item kinds (read `ItemQueryRs.polymorphic.qbxml` to know what's in it — e.g. `"Service"` then `"Inventory"`); assert the captured request is `<ItemQueryRq>` with `<ActiveStatus>All</ActiveStatus>` and `iterator="Start"`.
   - `list_items_returns_rows_for_simple_list`: using `ItemQueryRs.normal.qbxml` — assert the row count and that each row has a `type` of `"Service"` (or whatever you put in the fixture).
   - `try/finally { await manager.DisposeAsync(); }`.

`dotnet build` + `dotnet test` green. `git add` the 5 files; `git commit -m "feat(04-01): list_items op"`.
  </action>
  <verify>
`dotnet build` + `dotnet test` green. `ListEntityOpsTests` `list_items_*` facts pass, asserting `rows[i]["type"]` for the polymorphic fixture. `git log -1 --oneline` shows the distinct `feat(04-01): list_items op` title. `git status` shows only the 5 expected files committed.
  </verify>
  <done>
`list_items` exists as an `IReadOp` singleton, returns the item list with optional `activeStatus`/`name` filters, and the polymorphic `Item*Ret` shapes come back normalized with a `type` discriminator per row (free via `QbXmlParser`); tests prove it. Build + tests green. One commit, distinct title. (Completes READ-07.)
  </done>
</task>

<task type="auto">
  <name>Task 6: list_invoices / list_bills / list_payments ops</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListInvoicesOp.cs
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListBillsOp.cs
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/ListPaymentsOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/ListTransactionOpsTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/InvoiceQueryRs.qbxml
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/BillQueryRs.qbxml
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/ReceivePaymentQueryRs.qbxml
  </files>
  <action>
Three transaction-list ops sharing a `TxnDateRangeFilter` + `EntityFilter` shape. Add a `protected static XElement? TxnDateRangeFilterElement(IReadOnlyDictionary<string,object?> args)` and `protected static XElement? EntityFilterElement(IReadOnlyDictionary<string,object?> args)` helper (to `ReadOpBase` or `QueryFilters`):
- `TxnDateRangeFilterElement`: read `from = ArgReader.Date(args, "fromDate")`, `to = ArgReader.Date(args, "toDate")`, `macro = ArgReader.String(args, "dateMacro")`. If all null → return `null`. Else build `<TxnDateRangeFilter>`: if `macro` is non-null → `<DateMacro>macro</DateMacro>` (and ignore from/to, or throw if both supplied — throw `ArgumentException` if `macro` AND (`from` or `to`) are both present); else add `<FromTxnDate>from</FromTxnDate>` and/or `<ToTxnDate>to</ToTxnDate>` for whichever is present.
- `EntityFilterElement`: if `ArgReader.String(args, "entity")` non-empty → `new XElement("EntityFilter", new XElement("FullNameList", new XElement("FullName", entity)))`. Else `null`.

1. `Qb/Ops/ListInvoicesOp.cs` — `Name => "list_invoices"`. `RunAsync(args, ct)`:
   `var rq = QbXmlBuilder.Rq("InvoiceQueryRq");` `if (TxnDateRangeFilterElement(args) is { } d) rq.Add(d);` `if (EntityFilterElement(args) is { } e) rq.Add(e);` `if (ArgReader.Bool(args, "includeLineItems") == true) rq.Add(new XElement("IncludeLineItems", "true"));` `var parsed = await QueryListAsync(rq, ownerIdZero: null, ct);` `return ListResult(parsed);` — XML-doc: header-only is the qbXML default for `*QueryRq`; `includeLineItems:true` opts into line detail (`IncludeLineItems` is MEDIUM-confidence/OSR; Phase 9 confirms). For invoices/payments the `entity` is a customer; for bills it's a vendor.
2. `Qb/Ops/ListBillsOp.cs` — `BillQueryRq`, `Name => "list_bills"`, same shape (entity = vendor).
3. `Qb/Ops/ListPaymentsOp.cs` — `ReceivePaymentQueryRq`, `Name => "list_payments"`, same shape (entity = customer).

4. `Program.cs` — add three `AddSingleton<IReadOp, ...>()` lines.
5. `OpTestHarness.cs` — add the three ops to the dictionary.
6. Fixtures (constructed-header comment):
   - `InvoiceQueryRs.qbxml` — two `<InvoiceRet>` rows with `TxnID`, `TxnDate`, `RefNumber`, `CustomerRef`, `BalanceRemaining`, `Subtotal`/`TotalAmount`. `iteratorRemainingCount="0"`.
   - `BillQueryRs.qbxml` — two `<BillRet>` rows with `TxnID`, `TxnDate`, `RefNumber`, `VendorRef`, `AmountDue`/`OpenAmount`.
   - `ReceivePaymentQueryRs.qbxml` — two `<ReceivePaymentRet>` rows with `TxnID`, `TxnDate`, `RefNumber`, `CustomerRef`, `TotalAmount`.
7. `Tests/ListTransactionOpsTests.cs`:
   - `list_invoices_returns_rows_and_applies_date_and_entity_filters`: `Create()`; `fake.AddResponse("InvoiceQueryRq", Fixture("InvoiceQueryRs.qbxml"))`; `RunAsync(new Dictionary<string,object?>{ ["fromDate"]="2025-01-01", ["toDate"]="2025-06-30", ["entity"]="Acme Roofing Inc" }, default)`. Assert two rows; captured `<InvoiceQueryRq>` has `<TxnDateRangeFilter><FromTxnDate>2025-01-01</FromTxnDate><ToTxnDate>2025-06-30</ToTxnDate></TxnDateRangeFilter>` and `<EntityFilter><FullNameList><FullName>Acme Roofing Inc</FullName></FullNameList></EntityFilter>` and `iterator="Start"`.
   - `list_invoices_with_date_macro`: `RunAsync({ dateMacro:"ThisFiscalQuarter" })` → request `<TxnDateRangeFilter><DateMacro>ThisFiscalQuarter</DateMacro></TxnDateRangeFilter>`.
   - `list_invoices_with_no_filters_omits_filter_elements`: `RunAsync(new Dictionary<string,object?>())` → request has NO `<TxnDateRangeFilter>` and NO `<EntityFilter>`.
   - `list_invoices_include_line_items`: `RunAsync({ includeLineItems:true })` → request has `<IncludeLineItems>true</IncludeLineItems>`.
   - `list_bills_returns_rows` (using `BillQueryRs.qbxml`) → request is `<BillQueryRq>`.
   - `list_payments_returns_rows` (using `ReceivePaymentQueryRs.qbxml`) → request is `<ReceivePaymentQueryRq>`.
   - `try/finally { await manager.DisposeAsync(); }` in each.

`dotnet build` + `dotnet test` green. `git add` the 9 files; `git commit -m "feat(04-01): list_invoices / list_bills / list_payments ops"`.
  </action>
  <verify>
`dotnet build` + `dotnet test` green. `ListTransactionOpsTests` passes (incl. the date-range + entity filter assertions and the no-filters-omits case). `git log -1 --oneline` shows the distinct title. `git status` shows only the 9 expected files committed.
  </verify>
  <done>
`list_invoices`, `list_bills`, `list_payments` exist as `IReadOp` singletons, return their transactions filtered by date range (explicit dates or a date macro) and/or entity (customer for invoices/payments, vendor for bills), go through `QbListExecutor`; tests prove the filters land in the request. Build + tests green. One commit, distinct title. (Covers READ-08.)
  </done>
</task>

<task type="auto">
  <name>Task 7: get_transaction op (TxnID | RefNumber, handles non-unique RefNumber)</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/GetTransactionOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/GetTransactionOpTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/TransactionQueryRs.byid.qbxml
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/TransactionQueryRs.byref.multi.qbxml
  </files>
  <action>
1. `Qb/Ops/GetTransactionOp.cs` — `sealed class GetTransactionOp(...) : ReadOpBase(...)`, `Name => "get_transaction"`. `RunAsync(args, ct)`:
   - `var txnId = ArgReader.String(args, "txnId"); var refNo = ArgReader.String(args, "refNumber");`
   - `if ((txnId is null) == (refNo is null)) throw new ArgumentException("get_transaction: supply exactly one of txnId or refNumber.");`
   - Build `var rq = QbXmlBuilder.Rq("TransactionQueryRq", txnId is not null ? new XElement("TxnIDList", new XElement("TxnID", txnId)) : new XElement("RefNumberFilter", new XElement("MatchCriterion", "Equals"), new XElement("RefNumber", refNo)));`
   - Optional narrowing: `if (ArgReader.String(args, "txnType") is { Length: >0 } tt) rq.Add(new XElement("TransactionTypeList", new XElement("TxnType", tt)));`
   - `var parsed = await QuerySingleAsync(rq, ct);` `var rows = parsed.First.Rows;` (single-shot is fine for a point lookup; `QbListExecutor` not needed.)
   - Return `new Dictionary<string,object?> { ["status"]=parsed.First.Status, ["matches"]=rows, ["count"]=rows.Count, ["ambiguous"]= txnId is null && rows.Count > 1, ["lite"]=true };`
   - XML-doc note: `RefNumber` is editable and non-unique → never collapse to one row; the result is always a list (`matches`). `TransactionRet` is header-level only (no line items) → `lite:true`; for full detail use `list_invoices`/`list_bills`/`list_payments`. `TxnIDList`/`RefNumberFilter`/`TransactionTypeList` element names are MEDIUM-confidence/OSR — Phase 9 confirms. A per-element `Error` status (e.g. an unknown `TxnID`) is surfaced in `status`, not thrown.

2. `Program.cs` — add `builder.Services.AddSingleton<IReadOp, GetTransactionOp>();`.
3. `OpTestHarness.cs` — add `GetTransactionOp` to the ops dictionary.
4. Fixtures (constructed-header comment):
   - `TransactionQueryRs.byid.qbxml` — one `<TransactionQueryRs statusCode="0" ...><TransactionRet><TxnID>123-456</TxnID><TxnType>Invoice</TxnType><TxnDate>2025-03-01</TxnDate><RefNumber>1001</RefNumber><EntityRef><FullName>Acme Roofing Inc</FullName></EntityRef><Amount>2500.00</Amount></TransactionRet></TransactionQueryRs>`.
   - `TransactionQueryRs.byref.multi.qbxml` — two `<TransactionRet>` rows sharing `<RefNumber>1001</RefNumber>` (different `TxnID`, e.g. one `TxnType=Invoice` 2024 and one `TxnType=Check` 2025).
5. `Tests/GetTransactionOpTests.cs`:
   - `get_transaction_by_txn_id_returns_single_match`: `Create()`; `fake.AddResponse("TransactionQueryRq", Fixture("TransactionQueryRs.byid.qbxml"))`; `RunAsync(new Dictionary<string,object?>{ ["txnId"]="123-456" }, default)`. Assert `result["count"]` == 1, `result["ambiguous"]` == false, `result["lite"]` == true; captured `<TransactionQueryRq>` has `<TxnIDList><TxnID>123-456</TxnID></TxnIDList>`.
   - `get_transaction_by_ref_number_returns_multiple_matches_and_flags_ambiguous`: `fake.AddResponse("TransactionQueryRq", Fixture("TransactionQueryRs.byref.multi.qbxml"))`; `RunAsync({ refNumber:"1001" })`. Assert `result["count"]` == 2, `result["ambiguous"]` == true; captured request has `<RefNumberFilter><MatchCriterion>Equals</MatchCriterion><RefNumber>1001</RefNumber></RefNumberFilter>`.
   - `get_transaction_with_txn_type_narrows_query`: `RunAsync({ txnId:"123-456", txnType:"Invoice" })` → request has `<TransactionTypeList><TxnType>Invoice</TxnType></TransactionTypeList>`.
   - `get_transaction_requires_exactly_one_of_txn_id_or_ref_number`: `RunAsync(new Dictionary<string,object?>())` → throws `ArgumentException`; `RunAsync({ txnId:"x", refNumber:"y" })` → throws.
   - `get_transaction_surfaces_error_status_does_not_throw`: `fake.AddResponse("TransactionQueryRq", <a TransactionQueryRs with statusCode="3120" statusSeverity="Error" statusMessage="Object not found">)` (inline string, mirror `InvoiceAddRs.error.qbxml`'s shape); `RunAsync({ txnId:"nope" })` → does NOT throw; `((QbStatus)result["status"]!).IsError` (or whatever the property is) == true, `result["count"]` == 0.
   - `try/finally { await manager.DisposeAsync(); }` in each.

`dotnet build` + `dotnet test` green. `git add` the 6 files; `git commit -m "feat(04-01): get_transaction op (TxnID | RefNumber)"`.
  </action>
  <verify>
`dotnet build` + `dotnet test` green. `GetTransactionOpTests` passes (incl. the multi-match `ambiguous` case and the error-status-not-thrown case). `git log -1 --oneline` shows the distinct title. `git status` shows only the 6 expected files committed.
  </verify>
  <done>
`get_transaction` exists as an `IReadOp` singleton, looks up a transaction by `TxnID` or `RefNumber` (exactly-one-of enforced; optional `txnType` narrowing), and always returns `{ matches:[...], count, ambiguous, lite }` — never collapsing to one row (because `RefNumber` is non-unique); a per-element `Error` status is surfaced in the body, not thrown; tests prove all of it. Build + tests green. One commit, distinct title. (Covers READ-09.)
  </done>
</task>

<task type="auto">
  <name>Task 8: run_query op (read-only-entity whitelist + simple filters) + register all read ops + OpRegistrationTests</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/Ops/RunQueryOp.cs
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpTestHarness.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/RunQueryOpTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/OpRegistrationTests.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/EmployeeQueryRs.qbxml
  </files>
  <action>
1. `Qb/Ops/RunQueryOp.cs` — `sealed class RunQueryOp(...) : ReadOpBase(...)`, `Name => "run_query"`.
   - `private static readonly HashSet<string> Allowed = new(StringComparer.Ordinal) { "Employee", "OtherName", "SalesRep", "Class", "Term", "PriceLevel", "PaymentMethod", "ShipMethod", "Currency", "SalesTaxCode", "Vehicle", "SalesReceipt", "Estimate", "PurchaseOrder", "CreditMemo", "SalesOrder", "Deposit", "Check", "BillPaymentCheck", "BillPaymentCreditCard", "CreditCardCharge", "CreditCardCredit", "JournalEntry", "InventoryAdjustment", "TimeTracking", "VendorCredit", "ItemReceipt", "Customer", "Vendor", "Item", "Account", "Invoice", "Bill", "ReceivePayment", "Transaction", "ToDo", "Company", "Host", "Preferences" };` (curate as you see fit — the contract is "vetted read-only set, append exactly `QueryRq`, never accept a raw request name"). XML-doc: list the whitelist; note V2-01 promotes some to first-class ops.
   - `RunAsync(args, ct)`:
     - `var entity = ArgReader.String(args, "entity") ?? throw new ArgumentException("run_query: 'entity' is required.");`
     - `if (!Allowed.Contains(entity)) throw new ArgumentException($"run_query: entity '{entity}' is not allowed. Allowed: {string.Join(", ", Allowed.OrderBy(x => x))}");`
     - `var rq = QbXmlBuilder.Rq(entity + "QueryRq");`
     - If `ArgReader.Dict(args, "filters")` is non-null → `AddFilters(rq, filters)` — a private static recursive helper: for each `(key, value)`: if `value` is `IReadOnlyDictionary<string,object?>` (incl. coerced `JsonElement` object) → `var child = new XElement(key); AddFilters(child, nested); rq.Add(child);`; if `value` is an enumerable of scalars/dicts (not a string) → repeat the element per item; else (scalar / string / `JsonElement` scalar) → `rq.Add(new XElement(key, ToStringValue(value)))`. **Never** accept attributes, raw XML strings, or a key that contains `/`, `<`, `>`, whitespace, or `:` — throw `ArgumentException` on a bad key. (Element-name validity: `XElement` will itself throw on an invalid name; catch and rethrow as `ArgumentException` so Phase 5 maps to 400.)
     - `bool noIter = entity is "Company" or "Host" or "Preferences";` (these don't support iterators)
     - `var parsed = noIter ? await QuerySingleAsync(rq, ct) : await QueryListAsync(rq, ownerIdZero: null, ct);`
     - Return `ListResult(parsed)` but with `["entity"] = entity` added (build the dict directly so you can include `entity`).

2. `Program.cs` — add `builder.Services.AddSingleton<IReadOp, RunQueryOp>();`. After this task, the Phase-4 block in `Program.cs` has all 12 `AddSingleton<IReadOp, ...>()` lines (`CompanyInfoOp`, `CompanyPreferencesOp`, `ReportOp`, `ListCustomersOp`, `ListVendorsOp`, `ListAccountsOp`, `ListItemsOp`, `ListInvoicesOp`, `ListBillsOp`, `ListPaymentsOp`, `GetTransactionOp`, `RunQueryOp`).
3. `OpTestHarness.cs` — add `RunQueryOp` to the ops dictionary (now 12 ops).
4. `Fixtures/qbxml/EmployeeQueryRs.qbxml` (constructed-header comment) — one or two `<EmployeeRet>` rows (`ListID`, `Name`, `FullName`, `IsActive`). `iteratorRemainingCount="0"`.
5. `Tests/RunQueryOpTests.cs`:
   - `run_query_builds_entity_query_and_passes_filters`: `Create()`; `fake.AddResponse("EmployeeQueryRq", Fixture("EmployeeQueryRs.qbxml"))`; `RunAsync(new Dictionary<string,object?>{ ["entity"]="Employee", ["filters"]=new Dictionary<string,object?>{ ["ActiveStatus"]="ActiveOnly" } }, default)`. Assert `result["entity"] == "Employee"`, rows present; captured request is `<EmployeeQueryRq>` with `<ActiveStatus>ActiveOnly</ActiveStatus>` and `iterator="Start"` (because it went through `QbListExecutor`).
   - `run_query_supports_nested_filters`: `RunAsync({ entity:"PurchaseOrder", filters:{ TxnDateRangeFilter:{ FromTxnDate:"2025-01-01", ToTxnDate:"2025-06-30" } } })` — you'll need a `PurchaseOrderQueryRs` response (inline a minimal one). Assert the request has `<TxnDateRangeFilter><FromTxnDate>2025-01-01</FromTxnDate><ToTxnDate>2025-06-30</ToTxnDate></TxnDateRangeFilter>` nested inside `<PurchaseOrderQueryRq>`.
   - `run_query_rejects_non_whitelisted_entity`: `RunAsync({ entity:"Banana" })` → throws `ArgumentException`.
   - `run_query_rejects_verb_y_entity_name`: `RunAsync({ entity:"CustomerDel" })` → throws `ArgumentException` (`CustomerDel` is not in the whitelist; the op never appends anything but `QueryRq`, so a write is structurally impossible — but the whitelist makes it explicit).
   - `run_query_requires_entity`: `RunAsync(new Dictionary<string,object?>())` → throws `ArgumentException`.
   - `try/finally { await manager.DisposeAsync(); }` in each.
6. `Tests/OpRegistrationTests.cs` — uses `WebApplicationFactory<Program>` in the `Testing` environment (mirror `HostStartupTests.cs`):
   - `host_resolves_all_read_ops`: build the factory, `var ops = factory.Services.GetServices<IReadOp>().ToList();` Assert `ops.Count >= 12`; `ops.Select(o => o.Name)` are all unique; the set includes `"company_info"`, `"get_company_preferences"`, `"report"`, `"list_customers"`, `"list_vendors"`, `"list_items"`, `"list_accounts"`, `"list_invoices"`, `"list_bills"`, `"list_payments"`, `"get_transaction"`, `"run_query"`. (This proves Phase 5 can build `OpRegistry` = `ops.ToDictionary(o => o.Name)`.) — Read `HostStartupTests.cs` for the exact `WebApplicationFactory`/`Testing`-env wiring; reuse it.

Run the FULL `dotnet build` + `dotnet test` sweep — all green, test count ~95+. `git add` the 6 files; `git commit -m "feat(04-01): run_query op (whitelist) + register all read ops + OpRegistrationTests"`.
  </action>
  <verify>
`dotnet build` + `dotnet test` green (full sweep). `RunQueryOpTests` passes (incl. the non-whitelisted and verb-y rejections); `OpRegistrationTests.host_resolves_all_read_ops` passes (≥12 ops, unique names, all expected names present). `git log -1 --oneline` shows the distinct title. `git status` shows only the 6 expected files committed; the Phase-4 block in `Program.cs` has 12 `AddSingleton<IReadOp, ...>()` lines.
  </verify>
  <done>
`run_query` exists as an `IReadOp` singleton, runs a generic entity query over a hard-coded read-only-entity whitelist (`Employee`, `OtherName`, `SalesReceipt`, `Estimate`, `PurchaseOrder`, `CreditMemo`, `Deposit`, `Class`, … and the first-class entities), builds exactly `entity + "QueryRq"`, passes simple child-element `filters` (incl. nested) with no attributes/raw-XML, and routes non-iterator entities (`Company`/`Host`/`Preferences`) through a single-shot query; all 12 read ops are registered as `IReadOp` singletons and the host resolves them with unique names; tests prove the whitelist enforcement and the registration. Build + tests green. One commit, distinct title. (Covers READ-10 and the "all read ops registered" must-have.)
  </done>
</task>

<task type="auto">
  <name>Task 9: Phase 4 SUMMARY + plan checkbox tick</name>
  <files>
    .planning/phases/04-read-ops/04-01-SUMMARY.md
    .planning/phases/04-read-ops/04-01-PLAN.md
    .planning/ROADMAP.md
  </files>
  <action>
1. Create `.planning/phases/04-read-ops/04-01-SUMMARY.md` using the GSD summary template (`@C:\Users\rjain\.claude/get-shit-done/templates/summary.md`). Include in the frontmatter: `phase: 04-read-ops`, `plan: 01`, `subsystem: qb-read-ops`, `requires: [03-01]`, `affects` (Phase 5 — `OpRegistry` consumes `IEnumerable<IReadOp>`), `tech-stack` (no new packages), the commit range (the eight `feat(04-01)` commit SHAs from Tasks 1–8 — fill in actual SHAs via `git log --oneline`), and `tests` (count before → after). Body sections:
   - **What was built:** the `IReadOp`/`ReadOpBase`/`ArgReader` abstraction; the 12 read ops (`company_info`, `get_company_preferences`, `report`, `list_customers`/`list_vendors`/`list_items`/`list_accounts`, `list_invoices`/`list_bills`/`list_payments`, `get_transaction`, `run_query`); the shared filter helpers; DI registration; the `OpTestHarness`; ~8 test classes; ~12 constructed qbXML fixtures.
   - **Key decisions:** one `IReadOp` interface (not three); `report` is one op with a `type` switch; `run_query` uses a hard-coded read-only whitelist; `company_info` bundles `HostQueryRq`+`CompanyQueryRq` (edition from `HostRet.ProductName`); `get_company_preferences` does an `AccountQueryRq` AR/AP follow-up for the default-AR/AP-account piece; args are `IReadOnlyDictionary<string,object?>` (Phase 5 owns JSON binding); status/zero-row surfaced in the body, never thrown; transport/COM exceptions and arg-validation `ArgumentException`s propagate.
   - **CONSTRUCTED-FIXTURE NOTE:** list every new `.qbxml` fixture and the MEDIUM-confidence qbXML element/enum names that Phase 9 must re-pin against `10.120.254.13` (the report `*Type` enums, the aging-report date element, `ActiveStatus` values, `TxnDateRangeFilter`/`RefNumberFilter`/`IncludeLineItems`, `PreferencesRet` block/field names incl. the decimal-places source, `TxnIDList`/`RefNumberFilter`/`TransactionTypeList`). Same caveat the Phase-3 P&L fixture carries.
   - **For Phase 5:** `OpRegistry` = `IEnumerable<IReadOp>` → `ToDictionary(o => o.Name)`; `/api/ops/{op}` deserializes the JSON body to `Dictionary<string,object?>` and passes it straight to `op.RunAsync`; unknown op → 404; `ArgumentException` from `RunAsync` → 400; `QbException`/`QbBusyException`/`QbTimeoutException` → 502/409/504; a `statusCode != 0` in the result body → still a 200.
   - **Out of scope (deferred):** HTTP surface / `OpRegistry` / auth / health (Phase 5); writes / dry-run / `AllowWrites` / audit (Phases 6–7).
2. In `.planning/phases/04-read-ops/04-01-PLAN.md`, tick the task checkboxes if a checklist is present (and/or add a "Status: complete" line) — minimal edit.
3. In `.planning/ROADMAP.md`: under `### Phase 4: Read Ops` change `**Plans**: TBD` → `**Plans**: 1 plan` and replace the `Plans:` block `- [ ] 04-01: TBD` with `- [x] 04-01-PLAN.md — IReadOp + ReadOpBase + ArgReader + the 12 v1 read ops + DI + tests`. Update the Progress table row `| 4. Read Ops | 0/TBD | Not started | - |` → `| 4. Read Ops | 1/1 | ✓ Complete (...) | <date> |` (the reviewer/orchestrator may adjust the "reviewed N/N" wording — leave a sensible placeholder).
4. `git add .planning/phases/04-read-ops/04-01-SUMMARY.md .planning/phases/04-read-ops/04-01-PLAN.md .planning/ROADMAP.md` and `git commit -m "docs(04-01): phase 4 read ops summary"`. (Do NOT `git add` anything else.)
  </action>
  <verify>
`.planning/phases/04-read-ops/04-01-SUMMARY.md` exists with the constructed-fixture re-pin note and the Phase-5 handoff section. `.planning/ROADMAP.md` Phase 4 shows `1 plan` and `1/1` complete. `git log -1 --oneline` shows `docs(04-01): phase 4 read ops summary`. `git status` shows only the 3 planning files committed in this task. No duplicate-titled commits in `git log --oneline -10` (eight distinct `feat(04-01)` + one `docs(04-01)`).
  </verify>
  <done>
`04-01-SUMMARY.md` written (what built / decisions / constructed-fixture re-pin note / Phase-5 handoff / out-of-scope); the plan checkbox is ticked; ROADMAP Phase 4 updated to `1 plan` / `1/1`. One `docs(04-01)` commit, distinct title. Nothing dangling.
  </done>
</task>

</tasks>

<verification>
- `dotnet build` and `dotnet test` succeed from the QbConnectService solution dir on Windows with NO QuickBooks SDK installed, at every commit (each task's commit is independently green). Final test count grows from 76 to ~95+.
- The only project referencing the COM interop stub is still `QbConnectService.Qb.Com` (no new interop references).
- `git log --oneline` for this plan shows eight distinct `feat(04-01): ...` commits (Tasks 1–8) + one `docs(04-01): phase 4 read ops summary` commit (Task 9) — NO duplicate subject lines. Every commit added only files under `quickbooks/` (Tasks 1–8) or `.planning/` (Task 9); no India-HR files swept in; no `git add -A`/`.`/`reset --hard`/nested `git init`.
- `GetServices<IReadOp>()` on the host returns ≥12 ops with unique `Name`s including `company_info`, `get_company_preferences`, `report`, `list_customers`, `list_vendors`, `list_items`, `list_accounts`, `list_invoices`, `list_bills`, `list_payments`, `get_transaction`, `run_query`.
- Ops surface a per-element `statusCode != 0` / zero-row result in their result body (never throw it); only transport/COM exceptions (`QbException`/`QbBusyException`/`QbTimeoutException`) and caller-arg validation (`ArgumentException`) propagate. Ops are tested in-process (not over HTTP).
- No write op, `OpRegistry`, REST controller, auth, health endpoint, dry-run, `AllowWrites` gate, audit log, Python client, skill, or deploy script was added — those are Phases 5–9.
- New `.qbxml` fixtures each carry a `<!-- CONSTRUCTED ... Phase 9 re-pins ... -->` header comment; the SUMMARY lists them and the MEDIUM-confidence element/enum names for Phase-9 re-pinning.
</verification>

<success_criteria>
Phase 4 is complete when (mapping to READ-04..10):
1. **READ-04** — `op: company_info` returns company name, address, fiscal-year start month, and QuickBooks edition (from `HostRet.ProductName`). [`CompanyInfoOp` + `CompanyInfoOpTests`]
2. **READ-05** — `op: get_company_preferences` returns sales-tax-enabled, decimal places, multi-currency-enabled, and the default A/R and A/P accounts (default AR/AP via the `AccountQueryRq` follow-up; `rawPreferencesRet` always included). [`CompanyPreferencesOp` + `CompanyPreferencesOpTests`]
3. **READ-06** — `op: report` runs `ProfitAndLoss`, `BalanceSheet`, `AgingAR`, `AgingAP` over an explicit date range or a date macro (exactly-one-of enforced) and returns the parsed `ParsedReport`. [`ReportOp` + `ReportOpTests`]
4. **READ-07** — `op: list_customers` / `list_vendors` / `list_items` / `list_accounts` return the lists with optional active-only / name filter, and `list_items` normalizes the polymorphic `ItemServiceRet`/`ItemInventoryRet`/… shapes via the parser's `type` discriminator. [`ListCustomersOp`/`ListVendorsOp`/`ListAccountsOp`/`ListItemsOp` + `ListEntityOpsTests`]
5. **READ-08** — `op: list_invoices` / `list_bills` / `list_payments` return the transactions filtered by date range and/or entity (customer/vendor). [`ListInvoicesOp`/`ListBillsOp`/`ListPaymentsOp` + `ListTransactionOpsTests`]
6. **READ-09** — `op: get_transaction` returns a transaction looked up by `TxnID` or `RefNumber`, handling that `RefNumber` is non-unique (always a `{matches, count, ambiguous, lite}` list, never collapsed). [`GetTransactionOp` + `GetTransactionOpTests`]
7. **READ-10** — `op: run_query` runs a generic entity query (Employee, OtherName, SalesReceipt, Estimate, PurchaseOrder, CreditMemo, Deposit, Class, …) over a hard-coded read-only whitelist with simple child-element filters and returns parsed rows. [`RunQueryOp` + `RunQueryOpTests`]
8. All 12 read ops are registered as `IReadOp` DI singletons; the host resolves `IEnumerable<IReadOp>` with unique `Name`s (proves Phase 5 can build its `OpRegistry`). [`Program.cs` + `OpRegistrationTests`]
9. `dotnet build` + `dotnet test` green at every commit with no QuickBooks SDK; eight distinct `feat(04-01)` commits + one `docs(04-01)` commit; the `04-01-SUMMARY.md` carries the constructed-fixture re-pin note and the Phase-5 handoff.
</success_criteria>

<output>
After completion, `.planning/phases/04-read-ops/04-01-SUMMARY.md` exists (created in Task 9) following `@C:\Users\rjain\.claude/get-shit-done/templates/summary.md`, including: what was built, key decisions, the CONSTRUCTED-FIXTURE re-pin note (all new `.qbxml` files + the MEDIUM-confidence qbXML element/enum names Phase 9 must confirm against `10.120.254.13`), the Phase-5 handoff (`OpRegistry` from `IEnumerable<IReadOp>`; `/api/ops/{op}` JSON→`Dictionary` passthrough; unknown op → 404; `ArgumentException` → 400; `QbException`/`QbBusyException`/`QbTimeoutException` → 502/409/504; `statusCode != 0` → still 200), and the out-of-scope/deferred list (HTTP/auth/health → P5; writes/dry-run/AllowWrites/audit → P6–7).
</output>
