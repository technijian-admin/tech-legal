---
phase: 09-packaging-deploy-smoke
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickbooks/QbConnectService/deploy/_common.ps1
  - quickbooks/QbConnectService/deploy/make-cert.ps1
  - quickbooks/QbConnectService/deploy/install-service.ps1
  - quickbooks/QbConnectService/deploy/uninstall-service.ps1
  - quickbooks/QbConnectService/deploy/run-as-task.ps1
  - quickbooks/QbConnectService/docs/register-integrated-app.md
  - quickbooks/QbConnectService/docs/SMOKE-CHECKLIST.md
  - quickbooks/QbConnectService/docs/QBXML-REPIN.md
  - quickbooks/QbConnectService/README.md
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/README.md   # conditional (Task 7) — only if it lacks the QBXML-REPIN.md pointer
  - quickbooks/QbConnectService/src/QbConnectService/Program.cs
  - quickbooks/QbConnectService/src/QbConnectService/Options/ServerOptions.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/SampleConfigParityTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/VerifyAuditCliTests.cs
  - .claude/skills/quickbooks-accounting/references/setup-and-troubleshooting.md
  - .claude/skills/quickbooks-accounting/references/qbxml-cheatsheet.md
  - .github/workflows/quickbooks-ci.yml
  - .planning/phases/09-packaging-deploy-smoke/09-01-SUMMARY.md
  - .planning/phases/09-packaging-deploy-smoke/09-01-PLAN.md   # Task 8 ticks the Task-1..7 checkboxes here in the close-out commit
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - quickbooks/dev/MULTI-LLM.md
autonomous: true

must_haves:
  truths:
    - "An operator can run make-cert.ps1 on the host to generate the self-signed HTTPS PFX + public CER, with a -WhatIf dry-run that performs no changes."
    - "An operator can run install-service.ps1 to publish + register the QbConnectService Windows service as svc_qbsdk with restart-on-crash, and uninstall-service.ps1 to remove it; both have -WhatIf and elevation checks; reconfigure-in-place not delete+recreate."
    - "An operator can run run-as-task.ps1 to register (or -Remove) the documented session-0 fallback scheduled task as svc_qbsdk."
    - "appsettings.sample.json and clients/.env.sample contain every key the code reads (enforced by a test that fails if a key is missing); the real appsettings.json / .env stay gitignored and are NOT committed."
    - "register-integrated-app.md documents the one-time QuickBooks-side authorization (Admin, single-user mode, allow-to-login-automatically, bound to svc_qbsdk, the Reauthorize recovery path, and the PII gotcha)."
    - "README.md is the full 10.120.254.13 deploy runbook including prerequisites, install order, firewall guidance, the QBWC-fallback note, and an HRESULT troubleshooting table sourced verbatim from QbErrors.cs."
    - "SMOKE-CHECKLIST.md is an ordered, checkbox-style on-box smoke procedure: GET /api/health -> company_info -> a report -> create_customer dry-run -> with AllowWrites=true one real low-stakes write -> confirm in QuickBooks -> confirm the audit-log row, plus a table of environment facts to record."
    - "QBXML-REPIN.md documents the live-host re-pin procedure and a complete inventory of every constructed-fixture / Phase-9 re-pin marker in the codebase."
    - "QbConnectService.exe --verify-audit exposes AuditLog.VerifyChainAsync (prints OK / broken at seq N, sets exit code) and has a passing unit test."
    - "All bare-name forward-references to the deploy scripts / smoke checklist (in setup-and-troubleshooting.md, ServerOptions.cs, qbxml-cheatsheet.md, MULTI-LLM.md if any) point at the real chosen folders."
    - "dotnet build + dotnet test stay green (count rises from 255 to ~258+); CI stays green; a PSScriptAnalyzer lint job covers the new .ps1 files."
  artifacts:
    - path: "quickbooks/QbConnectService/deploy/make-cert.ps1"
      provides: "self-signed HTTPS cert generation (PFX + CER), -WhatIf, idempotency warning, optional -TrustLocally"
    - path: "quickbooks/QbConnectService/deploy/install-service.ps1"
      provides: "publish + register/reconfigure the Windows service as svc_qbsdk with restart-on-crash, -WhatIf, #requires -RunAsAdministrator"
    - path: "quickbooks/QbConnectService/deploy/uninstall-service.ps1"
      provides: "stop + remove the service, -WhatIf, elevation check, services.msc warning"
    - path: "quickbooks/QbConnectService/deploy/run-as-task.ps1"
      provides: "register / -Remove the session-0 fallback startup scheduled task as svc_qbsdk, -WhatIf"
    - path: "quickbooks/QbConnectService/deploy/_common.ps1"
      provides: "shared Assert-Elevated, Grant-ServiceLogonRight (secedit), Get-RepoRoot helpers"
    - path: "quickbooks/QbConnectService/docs/register-integrated-app.md"
      provides: "one-time QB-side authorization runbook"
    - path: "quickbooks/QbConnectService/docs/SMOKE-CHECKLIST.md"
      provides: "ordered on-box smoke checklist + environment-facts table"
    - path: "quickbooks/QbConnectService/docs/QBXML-REPIN.md"
      provides: "live-host re-pin procedure + constructed-fixture inventory"
    - path: "quickbooks/QbConnectService/README.md"
      provides: "full 10.120.254.13 deploy runbook + HRESULT troubleshooting table + QBWC-fallback note"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Tests/SampleConfigParityTests.cs"
      provides: "sample-parity test for appsettings.sample.json + .env.sample"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Tests/VerifyAuditCliTests.cs"
      provides: "unit test for the --verify-audit CLI path"
  key_links:
    - from: "quickbooks/QbConnectService/README.md"
      to: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbErrors.cs"
      via: "HRESULT troubleshooting table rows = QbErrors.cs entries verbatim"
      pattern: "0x8004"
    - from: ".claude/skills/quickbooks-accounting/references/setup-and-troubleshooting.md"
      to: "quickbooks/QbConnectService/deploy/ and quickbooks/QbConnectService/docs/"
      via: "forward-references updated to real folder paths"
      pattern: "deploy/|docs/"
    - from: "quickbooks/QbConnectService/src/QbConnectService/Program.cs"
      to: "AuditLog.VerifyChainAsync"
      via: "--verify-audit args branch resolves AuditLog and calls VerifyChainAsync"
      pattern: "verify-audit|VerifyChainAsync"
---

<objective>
Phase 9 (the FINAL phase): produce everything needed to install, configure, and verify the QbConnectService on the live QuickBooks host (`10.120.254.13`) — the PowerShell deploy/cert/install/uninstall/task scripts, the gitignored-config sample-parity guarantee, the integrated-app authorization runbook + the full deploy README + HRESULT table, the ordered on-box smoke checklist, the live-host qbXML re-pin procedure + inventory, and a small operator-facing `--verify-audit` CLI switch — then close the phase (SUMMARY + ROADMAP + REQUIREMENTS + STATE).

Purpose: the .NET service has been complete and tested since Phase 1 (tri-mode host, HTTPS file-cert Kestrel, full config surface, HRESULT map, hash-chained audit log). Phase 9 is the deploy/runbook/verification layer. There is no application logic to add beyond the tiny `--verify-audit` switch.

Output: ~6–8 atomic commits — the deploy scripts, the config-parity test, the QB-auth runbook, the deploy README + HRESULT table, the smoke checklist + re-pin doc, the `--verify-audit` switch + test, an optional CI lint job, and the final `docs(09-01)` close-out commit.

CRITICAL — host-only scope boundary (DO NOT cross it): you (the executor) run in the DEV environment. There is **no QuickBooks SDK** here and this is **not** host `10.120.254.13`. Therefore:
- The deploy/cert/install/uninstall/task scripts are **authored, self-checking, `-WhatIf`/`ShouldProcess`-capable, and PSScriptAnalyzer-clean** — but you do **NOT** execute them against a real service. Verification of those scripts in this env is limited to: PowerShell parse/syntax checks, `Invoke-ScriptAnalyzer`, `Get-Command`/`Get-Help`, and dot-sourcing `_common.ps1` to confirm its functions load.
- The **real** `appsettings.json` / `clients/.env` stay gitignored and are **NOT** created/committed. The deliverable is the audit + the sample-parity test.
- The `tlbimp` interop regen, the on-box smoke RUN, the QuickBooks-side authorization, and the live qbXML re-pin EDITS are **operator tasks done later on the host** — this plan *documents* them (in the README / SMOKE-CHECKLIST / QBXML-REPIN / register-integrated-app docs), you do **not** perform them. The run-codex-phase HARD RULES already forbid `tlbimp` in this env.

Stay on branch `quickbooks/direct-sdk-integration-2026-05-11`. Never touch `main`. Scoped `git add` only — there is unrelated India HR WIP in the tree. `.gitignore` stays append-only (and you should not need to touch it).
</objective>

<execution_context>
@quickbooks/dev/MULTI-LLM.md
@quickbooks/dev/run-codex-phase.ps1
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-packaging-deploy-smoke/09-RESEARCH.md

# Source-of-truth files this plan keys off:
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbErrors.cs
@quickbooks/QbConnectService/src/QbConnectService/Program.cs
@quickbooks/QbConnectService/src/QbConnectService/appsettings.sample.json
@quickbooks/clients/.env.sample
@.claude/skills/quickbooks-accounting/references/setup-and-troubleshooting.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy scripts — _common.ps1 + make-cert.ps1 + install-service.ps1 + uninstall-service.ps1 + run-as-task.ps1</name>
  <files>
    quickbooks/QbConnectService/deploy/_common.ps1
    quickbooks/QbConnectService/deploy/make-cert.ps1
    quickbooks/QbConnectService/deploy/install-service.ps1
    quickbooks/QbConnectService/deploy/uninstall-service.ps1
    quickbooks/QbConnectService/deploy/run-as-task.ps1
  </files>
  <action>
Create `quickbooks/QbConnectService/deploy/` with five PowerShell scripts. Follow the verified sketches in `09-RESEARCH.md` (the "Code Examples" + "Architecture / Deliverable Layout" sections). All scripts: `[CmdletBinding(SupportsShouldProcess = $true)]`, `$ErrorActionPreference = 'Stop'`, a header `<# .SYNOPSIS ... #>`, and clearly state at the top "Run this ON THE QuickBooks host (`10.120.254.13`), not in the dev/CI environment."

`_common.ps1` (dot-sourced helpers, no top-level side effects):
- `function Assert-Elevated` — throws if not running as Administrator (`([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltinRole]::Administrator)`).
- `function Grant-ServiceLogonRight { param([Parameter(Mandatory)][string]$Account) }` — grants `SeServiceLogonRight` via the `secedit /export` → edit INF → `secedit /configure /areas USER_RIGHTS` pattern from the research; idempotent (no-op if the account/SID is already on the line); respects `$WhatIfPreference` (print what it would do, change nothing). Resolve the account to a SID with `New-Object System.Security.Principal.NTAccount(...).Translate([System.Security.Principal.SecurityIdentifier])`. Include a comment pointing to the `secpol.msc` manual fallback.
- `function Get-RepoRoot` — resolves the repo root relative to `$PSScriptRoot` (deploy/ is at `<repo>/quickbooks/QbConnectService/deploy/`, so `Resolve-Path (Join-Path $PSScriptRoot '..\..\..')`).

`make-cert.ps1` — params `[string]$DnsName = '10.120.254.13'`, `[string[]]$ExtraNames = @('localhost')`, `[string]$PfxPath = '.\qbconnect.pfx'`, `[string]$CerPath = '.\qbconnect.cer'`, `[int]$ValidYears = 5`, `[switch]$TrustLocally`, `[securestring]$PfxPassword`. If `$PfxPassword` not supplied, `Read-Host -AsSecureString -Prompt 'PFX password'`. Body per the research sketch: `New-SelfSignedCertificate` (Subject `CN=$DnsName`, `-DnsName (@($DnsName)+$ExtraNames)`, `Cert:\LocalMachine\My`, `-KeyExportPolicy Exportable`, `-Type SSLServerAuthentication`, `-NotAfter (Get-Date).AddYears($ValidYears)`), `Export-PfxCertificate`, `Export-Certificate`, and if `-TrustLocally` `Import-Certificate -CertStoreLocation Cert:\LocalMachine\Root`. WARN (don't fail) if a cert with the same subject already exists in `LocalMachine\My`. Every state-changing call wrapped in `if ($PSCmdlet.ShouldProcess(...))`. Footer `Write-Host` instructing the operator to set `Server:CertPath`/`Server:CertPassword` in `appsettings.json` and distribute the `.cer` (or set the client's `QB_VERIFY_TLS`). Include an "if a strict client rejects the IP-as-DNS-name SAN" appendix comment pointing at `X509SubjectAlternativeNameBuilder`.

`install-service.ps1` — `#requires -RunAsAdministrator` at the top AND `Assert-Elevated` call; params `[string]$ServiceName='QbConnectService'`, `[string]$InstallDir="$env:ProgramFiles\QbConnectService"`, `[string]$Account='.\svc_qbsdk'`, `[switch]$SkipPublish`, `[string]$ConfigPath=(Join-Path $PSScriptRoot 'appsettings.json')`, `[string]$PfxPath=(Join-Path $PSScriptRoot 'qbconnect.pfx')`. Dot-source `_common.ps1`. Logic per the research sketch: assert `dotnet --version` ≥ 8; unless `-SkipPublish`, `dotnet publish <repoRoot>\quickbooks\QbConnectService\src\QbConnectService -c Release -r win-x86 --self-contained true -p:PublishSingleFile=true -o $InstallDir`; assert `QbConnectService.exe` exists post-publish; copy `$ConfigPath` → `$InstallDir\appsettings.json` (if it exists) and the `.pfx` if it exists; `Get-Credential -UserName $Account`; **reconfigure-in-place if the service exists** (`Stop-Service`; `sc.exe config $ServiceName binPath= "`"$exe`"" start= auto obj= $Account password= ...`) else `New-Service -BinaryPathName "`"$exe`"" -DisplayName 'QuickBooks Connect Service' -Description 'Direct-SDK QuickBooks Enterprise REST bridge' -StartupType Automatic -Credential $cred`; then `sc.exe failure $ServiceName reset= 86400 actions= restart/5000/restart/5000/restart/5000` and `sc.exe failureflag $ServiceName 1`; then `Grant-ServiceLogonRight -Account $cred.UserName`; after install run `sc.exe qc $ServiceName` and Write-Warning if `BINARY_PATH_NAME` isn't quoted. End with the "Next: run docs\register-integrated-app.md, then Start-Service, then docs\SMOKE-CHECKLIST.md" hint. All SCM-mutating calls behind `$PSCmdlet.ShouldProcess(...)` so `-WhatIf` prints every `sc.exe`/`New-Service` it *would* run and changes nothing (and `-WhatIf` should NOT require elevation — guard `Assert-Elevated` with `if (-not $WhatIfPreference) { Assert-Elevated }`).

`uninstall-service.ps1` — `#requires -RunAsAdministrator` + guarded `Assert-Elevated`; param `[string]$ServiceName='QbConnectService'`. `if (Get-Service $ServiceName -EA SilentlyContinue) { Stop-Service ...; Remove-Service $ServiceName (PS6+) or sc.exe delete $ServiceName }` else Write-Host "not installed". Write-Warning to close `services.msc` first (the "marked for deletion" race). ShouldProcess-wrapped; `-WhatIf` no-elevation.

`run-as-task.ps1` — params `[string]$TaskName='QbConnectService'`, `[string]$InstallDir="$env:ProgramFiles\QbConnectService"`, `[string]$Account='.\svc_qbsdk'`, `[switch]$Remove`; `#requires -RunAsAdministrator` + guarded `Assert-Elevated`. If `-Remove`: `Unregister-ScheduledTask -TaskName $TaskName -Confirm:$false` (ShouldProcess-wrapped) else: build with `New-ScheduledTaskAction -Execute "$InstallDir\QbConnectService.exe"`, `New-ScheduledTaskTrigger -AtStartup`, `New-ScheduledTaskPrincipal -UserId $Account -LogonType Password -RunLevel Highest` (Get-Credential for the password), `New-ScheduledTaskSettingsSet -RestartCount 3 -RestartInterval (New-TimeSpan -Minutes 1) -ExecutionTimeLimit ([TimeSpan]::Zero)`, `Register-ScheduledTask`. Header comment must say: this is the documented *fallback* for the session-0 problem — use it when the service install hits `0x80040408`/`0x80040414`/`0x80040401` at startup that don't reproduce running the exe interactively as `svc_qbsdk`; service and task are mutually exclusive (run ONE); not a guaranteed fix — the QBWC-polled redesign is the true last resort (cross-reference the README's QBWC note).

Do NOT create a real `appsettings.json` / `qbconnect.pfx` in `deploy/` (they're operator-supplied on the host).
  </action>
  <verify>
`pwsh -NoProfile -Command "Get-ChildItem quickbooks/QbConnectService/deploy/*.ps1 | ForEach-Object { [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw $_.FullName),[ref]$null) | Out-Null; $_.Name }"` parses every script with no errors;
`pwsh -NoProfile -Command "if (-not (Get-Module PSScriptAnalyzer -ListAvailable)) { Install-Module PSScriptAnalyzer -Scope CurrentUser -Force -AllowClobber }; Invoke-ScriptAnalyzer -Path quickbooks/QbConnectService/deploy -Recurse -Severity Warning,Error"` returns no Warning/Error findings (CI-verified by the Task-7 `powershell-lint` job which is the real gate — `windows-latest` ships PSScriptAnalyzer preinstalled; this local run is best-effort and auto-installs PSScriptAnalyzer to CurrentUser if Codex's dev machine lacks it);
`pwsh -NoProfile -Command ". ./quickbooks/QbConnectService/deploy/_common.ps1; Get-Command Assert-Elevated, Grant-ServiceLogonRight, Get-RepoRoot"` lists all three functions;
`pwsh -NoProfile -Command "Get-Command -Syntax ./quickbooks/QbConnectService/deploy/make-cert.ps1; Get-Command -Syntax ./quickbooks/QbConnectService/deploy/install-service.ps1; Get-Command -Syntax ./quickbooks/QbConnectService/deploy/uninstall-service.ps1; Get-Command -Syntax ./quickbooks/QbConnectService/deploy/run-as-task.ps1"` shows the expected params including `-WhatIf`.
(Do NOT execute the scripts — they target the host. `dotnet build` of the solution must still succeed — it's unaffected.)
  </verify>
  <done>The five scripts exist under `quickbooks/QbConnectService/deploy/`, all parse, all are PSScriptAnalyzer-clean, all support `-WhatIf`, `install-service.ps1`/`uninstall-service.ps1`/`run-as-task.ps1` require elevation (except under `-WhatIf`), `_common.ps1` exposes the three helpers, and DEPLOY-01's three named scripts (`make-cert.ps1`, `install-service.ps1`/`uninstall-service.ps1`, `run-as-task.ps1`) are present and behave as specified. Committed as `feat(09-01): deploy/cert/install/uninstall/task PowerShell scripts`.</done>
</task>

<task type="auto">
  <name>Task 2: Gitignored-config audit + sample-parity test</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Tests/SampleConfigParityTests.cs
    quickbooks/QbConnectService/src/QbConnectService/Options/ServerOptions.cs
  </files>
  <action>
DEPLOY-02: machine-specifics live ONLY in gitignored `QbConnectService/appsettings.json` + `clients/.env`, each with a committed `.sample`; nothing machine-specific hardcoded in source. The deliverable is the **audit + the test**, NOT a committed real config.

1. Confirm `.gitignore` already covers both real files — it does (`quickbooks/QbConnectService/src/QbConnectService/appsettings.json` and `quickbooks/clients/.env`). Do NOT modify `.gitignore`. Do NOT create real `appsettings.json` / `.env`.

2. Add `SampleConfigParityTests.cs` to the `QbConnectService.Tests` project (xUnit, matching the project's existing test style). It must:
   - Locate `appsettings.sample.json` (it's copied to the test output via `CopyToOutputDirectory="PreserveNewest"` in the service csproj — but the Tests project doesn't reference it; instead resolve it by walking up from `AppContext.BaseDirectory` to the repo's `quickbooks/QbConnectService/src/QbConnectService/appsettings.sample.json`, or add a `<None Link>` include in the Tests csproj that copies it — pick the path-walk approach to avoid touching the csproj). Parse it as JSON.
   - Assert it contains every section/key the Options classes bind. Enumerate the actual Options types in the solution (`ServerOptions`, `AuthOptions`, `QbOptions`, `SafetyOptions`, `QbXmlOptions`, `AuditOptions`, `RequestOptions`, `AuditAuthOptions`) — for robustness, bind `appsettings.sample.json` into a fresh `ConfigurationBuilder().AddJsonFile(...)` and assert each `Configuration.GetSection("Server").Get<ServerOptions>()` (etc.) is non-null AND that each public settable property name appears as a key under that section in the raw JSON (reflect over the Options type's properties; fail with the missing key name). This makes the test fail loudly if a future code change reads a key the sample doesn't carry.
   - Locate `quickbooks/clients/.env.sample` (path-walk from `AppContext.BaseDirectory`), parse `KEY=value` lines, and assert the keys `QB_API_BASE_URL`, `QB_API_TOKEN`, `QB_VERIFY_TLS`, `QB_TIMEOUT`, `QB_RETRIES` are all present (these are exactly what `quickbooks/clients/qb_client.py` reads — cross-check that file; if it reads more, add them to the assertion list).

3. In `ServerOptions.cs`, the XML doc comment currently says "see Phase 9 deploy notes / make-cert.ps1." — update it to point at the real path: `quickbooks/QbConnectService/deploy/make-cert.ps1` and `quickbooks/QbConnectService/README.md`.
  </action>
  <verify>
`dotnet build quickbooks/QbConnectService` succeeds;
`dotnet test quickbooks/QbConnectService` is green and the count has increased (≈256+) and includes the new `SampleConfigParityTests`;
`git status --porcelain` shows NO `appsettings.json` / `.env` (only `.sample`s exist, unchanged) and shows the test file + ServerOptions.cs as the only changes plus nothing in `.gitignore`.
  </verify>
  <done>A passing `SampleConfigParityTests` enforces that both `.sample` files carry every key the code/client reads; no real config committed; `.gitignore` untouched (already correct); `ServerOptions.cs` forward-reference fixed. Committed as `test(09-01): sample-config parity tests + fix ServerOptions deploy reference`.</done>
</task>

<task type="auto">
  <name>Task 3: register-integrated-app.md (QuickBooks-side authorization runbook)</name>
  <files>
    quickbooks/QbConnectService/docs/register-integrated-app.md
  </files>
  <action>
Create `quickbooks/QbConnectService/docs/register-integrated-app.md` — the one-time QuickBooks-side authorization runbook. Build it from the "QuickBooks-side: register-integrated-app.md" section of `09-RESEARCH.md`. Required content:
- A "Run this on the host (`10.120.254.13`), once" banner; a "MEDIUM confidence — verify exact dialog wording on the host" note.
- Prereqs: QuickBooks Desktop **Enterprise** installed; the `QBXMLRP2.RequestProcessor` COM server registered (ships with QuickBooks; install the QuickBooks SDK on the host for `tlbimp` + the OnScreen Reference + `qbsdklog.txt`); the COM server is 32-bit (hence the x86 service); if not registered → `0x80040154` (link to the README HRESULT table).
- **Two distinct identities** crisply: `svc_qbsdk` = the *Windows* account the service/task runs as (needs: real local account, "Log on as a service", read access to the `.pfx` and the `.QBW`/share, **and must have logged in interactively at least once** so its profile + QuickBooks per-user state exist; the integrated-app grant is stored per-Windows-user so it must be granted while logged in as / running as `svc_qbsdk`). The **QuickBooks login user** (Admin or a dedicated limited QB user) = the QB-internal user whose permissions the SDK runs under; `svc_qbsdk` ≠ this QB user.
- The procedure (a–g from the research): log in as `svc_qbsdk`; open the `.QBW` in QB **as Admin** in **single-user mode**; trigger a connection (`Start-Service QbConnectService` or run the exe as `svc_qbsdk`, then `GET /api/health` → `company_info` probe → first-contact authorization dialog naming `QbConnectService`); in the dialog choose **"Yes, always; allow access even if QuickBooks is not running"** (the batch/auto-login grant); pick the **login user** (Admin or dedicated QB user) + supply that QB user's password if asked; **PII gotcha** — the same dialog (or follow-up) has a "Allow this application to access personal data such as Social Security Numbers and customer credit card information" checkbox: decide deliberately; for a back-office accounting integration you almost certainly want to **check it**, else `list_customers`/`list_vendors`/`get_company_preferences` come back with masked/missing PII fields and a qbXML `statusCode` in the ~`530` family (NOT a COM HRESULT — it rides inside the HTTP-200 `result.status` body, so it won't appear in the HRESULT table); then switch the `.QBW` **back to multi-user / hosted mode** and confirm Database Server Manager is hosting it.
- Where the grant is stored: in QuickBooks's own integrated-application list for that company file (Edit → Preferences → Integrated Applications → Company Preferences tab → the app's row), keyed by AppID + the app's exe path + the Windows user that ran it (it is NOT a file next to the `.QBW`).
- **Reauthorize / recovery path**: Edit → Preferences → Integrated Applications → Company Preferences → select `QbConnectService` → **Properties** (review/adjust "allow even if QB not running" + login user + personal-data settings) or **remove it** and re-run the procedure. What triggers a fresh prompt: `AppID` changed; the exe path changed (so deploy to a stable path — `C:\Program Files\QbConnectService\` — and don't move it after granting); a major QuickBooks version upgrade; a redeploy *may* re-trigger the dialog (be ready to re-grant in single-user mode).
- A note on `Qb:AppId` (the sample has `"AppId": "REPLACE-WITH-APP-ID"`): clarify it is app-declared (a stable string/GUID you choose once and bake into `appsettings.json`), with QB keying the grant on it — MEDIUM confidence, confirm on the host. Recommend generating a stable GUID once.
- A note that whether `RealRequestProcessor` must declare a personal-data preference (`pdpRequired`) for the PII checkbox to even appear is unconfirmed — if on the host the PII fields come back masked and the auth dialog never offered a personal-data checkbox, that's a small follow-up `RealRequestProcessor`/interop change to make on the host (LOW confidence — contingency, not a task here).
- Cite: Intuit QBSDK Programmer's Guide / "Connections, sessions and authorizations"; `qbsdklog.txt` (in the user profile / `%ProgramData%\Intuit\QuickBooks\`) is the diagnostic when the dialog never appears or auth fails.
  </action>
  <verify>
`Test-Path quickbooks/QbConnectService/docs/register-integrated-app.md` is true;
the file contains the phrases "single-user", "allow access even if QuickBooks is not running" (or close), "svc_qbsdk", "personal data" / "PII", "Reauthorize" / "Properties", "AppId"/"AppID", and "qbsdklog.txt";
markdown renders (no broken headings/lists).
  </verify>
  <done>`register-integrated-app.md` documents the one-time QB-side authorization with the two-identities distinction, the allow-even-if-QB-not-running grant, the binding to `svc_qbsdk`, the Reauthorize recovery path, and the PII gotcha — satisfying the `register-integrated-app.md` half of DEPLOY-03. Committed as `docs(09-01): QuickBooks integrated-app authorization runbook`.</done>
</task>

<task type="auto">
  <name>Task 4: README.md deploy runbook + HRESULT troubleshooting table</name>
  <files>
    quickbooks/QbConnectService/README.md
  </files>
  <action>
Create (or rewrite) `quickbooks/QbConnectService/README.md` — the full `10.120.254.13` deploy runbook. Build it from the "QbConnectService/README.md deploy runbook structure" section of `09-RESEARCH.md`. Required content:
1. **What this is / prerequisites:** Windows Server / Win10+ on the QuickBooks host; .NET 8 **not** required to be installed (self-contained single-file publish bundles the runtime) — mention the framework-dependent + ASP.NET Core 8 Hosting Bundle alternative as a note only. QuickBooks Desktop **Enterprise** + a `.QBW`; **QuickBooks Database Server Manager** installed and *hosting* the `.QBW` (multi-user); the `QBXMLRP2.RequestProcessor` COM server (ships with QuickBooks; standalone QuickBooks SDK recommended for `tlbimp` + OSR + `qbsdklog.txt`). A `svc_qbsdk` local account: created, password set, "Log on as a service" granted (`install-service.ps1` does it via `secedit`; manual = `secpol.msc → Local Policies → User Rights Assignment → Log on as a service`), read access to the `.QBW`/share + the `.pfx`, and has logged in interactively at least once.
2. **Install order (happy path):** `cd quickbooks\QbConnectService\deploy` → `.\make-cert.ps1 -DnsName 10.120.254.13 -TrustLocally` → `copy ..\src\QbConnectService\appsettings.sample.json .\appsettings.json` and edit (`Auth:ApiToken` a long random token; `Qb:CompanyFilePath` full path/UNC of the `.QBW`; `Qb:AppId` per `register-integrated-app.md`; `Server:CertPath`/`Server:CertPassword` the `.pfx`; `Audit:Path` e.g. `C:\ProgramData\QbConnectService\audit`; `QbXml:Version` confirm against `GET /api/health → qbXmlVersionsSupported` — the sample says `16.0`; `Safety:AllowWrites` = **false**, flip to true only for the smoke write then back) → `.\install-service.ps1` (publishes to `C:\Program Files\QbConnectService\`, copies `appsettings.json` + the `.pfx` next to the exe, registers the service as `svc_qbsdk`, restart-on-crash, grants the logon right) — OR `.\run-as-task.ps1` if you hit session-0 problems → follow `docs\register-integrated-app.md` → `Start-Service QbConnectService` (or `Start-ScheduledTask`) → run `docs\SMOKE-CHECKLIST.md`. Note: put `appsettings.json` in the same folder as `QbConnectService.exe` (the ASP.NET Core host loads it relative to the exe directory for a self-contained single-file publish); the smoke checklist's health step (it echoes `companyFile` from `Qb:CompanyFilePath`) is the proof the config loaded.
3. **Firewall:** open the HTTPS port (default **8443** from `Server:BindUrls`) **inbound, scoped to the LAN** — example `New-NetFirewallRule -DisplayName 'QbConnectService HTTPS' -Direction Inbound -Protocol TCP -LocalPort 8443 -Action Allow -RemoteAddress LocalSubnet`. Do NOT expose it to the internet.
4. **QBWC-fallback note (one paragraph):** if direct COM proves unstable in session 0 even as a scheduled task (persistent `0x80040408`/`0x80040414`), the fallback architecture is the classic QuickBooks Web Connector–polled design — QB initiates the connection on a timer, the service is a SOAP endpoint it calls; different design, out of scope for v1, noted for completeness; nothing in the op layer / qbXML engine changes, only the transport.
5. **HRESULT troubleshooting table** — sourced **verbatim from `quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbErrors.cs`** (note that file's path — it is at `.../QbConnectService.Qb.Com/QbErrors.cs`, NOT a `Qb/` subpath). One row per `QbErrors.Map` entry: HRESULT (e.g. `0x80040401`), `Name`, the `Message` text, and the `RemediationHint` text — copy them faithfully (don't paraphrase the names/codes). Include the two extra cases: `REGDB_E_CLASSNOTREG` (`0x80040154`) and `QB_RP2_CAST_FAILED` (the `CastFailure` variant, also `0x80040154`). Add a note after the table: a non-zero qbXML `statusCode` (e.g. `3200` stale `EditSequence`, the PII `~530` family) is **not** one of these HRESULTs — it rides inside the HTTP-200 `result.status` body and is not an error at the COM/HTTP layer.
6. **Audit log:** `audit.jsonl` (or `audit.log` — match `AuditLog`'s actual filename; check `AuditLog.cs`) lives under `Audit:Path`. To verify the hash chain: run `QbConnectService.exe --verify-audit` (added in Task 6 — prints `OK` or `broken at seq N`, non-zero exit on a broken chain); or manually `Get-Content <audit-file> -Tail 5` and eyeball the `seq` (monotonic, 0-based) + `prevHash`/`hash` chaining (genesis `prevHash` is 64 zeros).
7. A short "Operator follow-ups not done by the build pipeline" section listing the host-only tasks: `tlbimp` interop regen on the host (replace the hand-written `Interop/QBXMLRP2Lib.cs` stub), run `docs\SMOKE-CHECKLIST.md`, do the QuickBooks-side authorization (`docs\register-integrated-app.md`), and the live qbXML re-pin (`docs\QBXML-REPIN.md`).
  </action>
  <verify>
`Test-Path quickbooks/QbConnectService/README.md` is true;
the file contains every HRESULT name from `QbErrors.cs` (`QB_ACCESS_FAILED`, `QB_UNEXPECTED_ERROR`, `QB_COULD_NOT_START`, `QB_DIFFERENT_FILE_OPEN`, `QB_INVALID_TICKET`, `QB_MODE_MISMATCH`, `QB_MODAL_DIALOG`, `QB_NO_FILE_SPECIFIED`, `QB_NO_PERMISSION`, `QB_ACCESS_DENIED`, `QB_PASSTHROUGH`, `QB_REQUIRES_SINGLE_USER`, `REGDB_E_CLASSNOTREG`, `QB_RP2_CAST_FAILED`) and the strings "Web Connector"/"QBWC", "8443", "svc_qbsdk", "--verify-audit", "register-integrated-app.md", "SMOKE-CHECKLIST.md";
markdown renders cleanly.
  </verify>
  <done>`README.md` is the full `10.120.254.13` deploy runbook with prerequisites, install order, firewall guidance, the QBWC-fallback note, and an HRESULT troubleshooting table sourced from `QbErrors.cs` — satisfying the `README.md` half of DEPLOY-03. Committed as `docs(09-01): QbConnectService deploy runbook + HRESULT table`.</done>
</task>

<task type="auto">
  <name>Task 5: SMOKE-CHECKLIST.md + QBXML-REPIN.md</name>
  <files>
    quickbooks/QbConnectService/docs/SMOKE-CHECKLIST.md
    quickbooks/QbConnectService/docs/QBXML-REPIN.md
  </files>
  <action>
Create both docs from the "docs/SMOKE-CHECKLIST.md" and "docs/QBXML-REPIN.md" sections of `09-RESEARCH.md`.

`SMOKE-CHECKLIST.md` — an ordered, literal-checkbox (`- [ ]`) on-box smoke procedure the operator runs on `10.120.254.13`. Use PowerShell `Invoke-RestMethod` with `$base='https://10.120.254.13:8443'`, `$token='<Auth:ApiToken>'`, `$h=@{ Authorization = "Bearer $token" }`. Steps in this exact order (matching DEPLOY-04): (0) environment facts captured (the table below filled first); (1) **health** — `GET /api/health` GOOD = `status:"healthy"`, `connectionState:"SessionOpen"`, `companyFile` matches `appsettings.json`, `quickBooksVersion` populated, `qbXmlVersionsSupported` non-empty, `lastError:null` (if `down`/`degraded`, stop, use the README HRESULT table — `lastError.code` is the HRESULT); (2) **company_info** — `POST /api/ops/company_info {}` GOOD = `result.status.statusCode == 0`, company name / fiscal year / `edition` right — record the exact `edition`/`ProductName` string (first re-pin data point); (3) **report** — `POST /api/ops/report { "report": "ProfitAndLoss", "dateMacro": "ThisFiscalYearToDate" }` GOOD = `statusCode == 0`, `rows` populated, column headers present — eyeball `ColDesc`/`ColTitle`/`ColType` casing (second re-pin point); (4) **create_customer DRY-RUN** — `POST /api/ops/create_customer/dryrun { "name": "ZZ_SMOKE_TEST_<date>" }` GOOD = `{ qbXml, summary, preFlight:[...all ok...], allowWrites:false }`, well-formed `CustomerAddRq`, NO side effect (re-check `company_info`/QB — no new customer); inspect element/enum names vs the OSR (re-pin point); (5) **flip AllowWrites** — edit `appsettings.json` `Safety:AllowWrites=true`, restart, confirm `health → allowWrites:true`; (6) **one real low-stakes write** — `POST /api/ops/create_customer { "name": "ZZ_SMOKE_TEST_<date>" }` GOOD = HTTP 200, `statusCode == 0`, a `ListID`/`EditSequence` returned (a customer = most reversible — no GL impact; append a timestamp/GUID if one already exists from a prior run); (7) **confirm in QuickBooks** — open QB on the host, Customers list, find it; then **make it inactive** to clean up (no `delete` op — use the raw qbXML passthrough `POST /api/qbxml` with a `CustomerModRq` setting `IsActive` false using the `ListID`+`EditSequence` from step 6, or just toggle "Make Inactive" in the QB UI; include the `CustomerModRq` snippet); (8) **confirm the audit row** — `Get-Content "<Audit:Path>\<audit-file>" -Tail 3` GOOD = last line is the `create_customer` write: `seq` = previous+1 (or 0 if first), `op:"create_customer"`, `args` has the name, `responseStatusCode:"0"`, `prevHash` = prior row's `hash` (or 64 zeros if first), `hash` present — and `QbConnectService.exe --verify-audit` prints `OK`; (9) **flip AllowWrites back to false**, restart, confirm `health → allowWrites:false`. Then a "Environment facts to record" table (rows: QuickBooks Enterprise year/version (build) — from `GET /api/health → quickBooksVersion` and QB Help→About; multi-user hosting status of the `.QBW`; `svc_qbsdk` account + "Log on as a service" right — `secpol.msc → User Rights Assignment`; firewall rule for the HTTPS port — `Get-NetFirewallRule -DisplayName 'QbConnectService HTTPS'`; `RequestProcessor` vs `RequestProcessor2` ProgID, registered & 32-bit; PII/personal-data access granted to the integrated app; `qbXmlVersionsSupported` — drives `QbXml:Version`). Confirm the actual op route paths and health field names against `quickbooks/QbConnectService/src/QbConnectService/Api/OpsEndpoints.cs` + `HealthEndpoints.cs` and the audit filename against `AuditLog.cs` — use the real names.

`QBXML-REPIN.md` — the live-host re-pin procedure + a **complete** inventory of constructed-fixture / Phase-9 re-pin markers. Build the inventory table by grepping the codebase for `Phase 9` / `re-pin` / `CONSTRUCTED` in `quickbooks/QbConnectService/src/` and the skill (`.claude/skills/quickbooks-accounting/`) — at minimum the items the research lists: `HostRet.ProductName`/`edition` in `CompanyInfoOp.cs`; preference field names in `CompanyPreferencesOp.cs` (+ the skill row); `get_transaction` query element names in `GetTransactionOp.cs`; `ListInvoicesOp` query element name (`IncludeLineItems`?) in `ListInvoicesOp.cs` (+ the skill row + the cheatsheet "Phase-9 re-pin candidates"); item subtype discriminators in `ListItemsOp.cs`; report enum values + aging-date shape + `ColDesc`/`ColTitle`/`ColType` casing in `ReportOp.cs` / `QbReportParser.cs` / the constructed P&L golden fixture; name-query filter wrappers in `WriteOpBase.cs` (`FetchByNameAsync`/`FetchCurrentAsync`); the write-verb enumeration in `QbWriteDetector.cs`; the `mod` stale-`EditSequence` `statusCode=3200` in `ModOp.cs` + tests; all Phase-7 `*Add` qbXML fixtures (`CreateEntityOpsTests.cs`, `CreateTransactionOpsTests.cs`, `CreateJournalEntryOpTests.cs`, `ReceivePaymentOpTests.cs`, `ModOpTests.cs`); the Phase-3/4 `*Rs` fixtures (the `Fixtures/qbxml/*.qbxml` files + `*OpTests.cs`). For each: where it lives + what to verify on the host. Then the **procedure** (operator, on the host): for each item — (1) call the op (or `/dryrun`, or raw `/api/qbxml`) against the live company file; (2) capture the real qbXML request *and* response (oversized responses spill to `QbXml:SpillPath`; small ones come back inline); (3) compare against the OnScreen Reference (OSR — shipped with the QuickBooks SDK) for the host's qbXML version and the relevant `*OpTests.cs` golden fixture / parser expectation; (4) if a name/enum/casing differs, fix the **fixture** AND the **builder/parser** in `QbConnectService.Qb.Com` to match reality; (5) re-run `dotnet test` (must stay green); (6) remove the "Phase 9 re-pin" comment from that file; (7) update the skill's `qbxml-cheatsheet.md` "Phase-9 re-pin candidates" section as items resolve. State clearly: this is the ONLY part of Phase 9 that edits production code, and it happens **on the host**, not in the dev/CI environment — the build pipeline does not perform it.
  </action>
  <verify>
`Test-Path quickbooks/QbConnectService/docs/SMOKE-CHECKLIST.md` and `Test-Path quickbooks/QbConnectService/docs/QBXML-REPIN.md` both true;
`SMOKE-CHECKLIST.md` contains `- [ ]` checkbox lines in the order health → company_info → report → create_customer dry-run → AllowWrites=true → create_customer → confirm in QuickBooks → audit row, plus the environment-facts table headers (QuickBooks Enterprise version, multi-user hosting, svc_qbsdk + log on as a service, firewall port);
`QBXML-REPIN.md` lists `CompanyInfoOp.cs`, `ReportOp.cs`, `WriteOpBase.cs`, `QbWriteDetector.cs`, `ModOp.cs`, `ListInvoicesOp.cs`, `ListItemsOp.cs` and references the `Fixtures/qbxml/` fixtures and the OnScreen Reference;
markdown renders cleanly.
  </verify>
  <done>`SMOKE-CHECKLIST.md` is the ordered on-box smoke (with the environment-facts table) and `QBXML-REPIN.md` is the re-pin procedure + complete constructed-fixture inventory — satisfying DEPLOY-04 and the re-pin obligation carried forward from Phases 3–7. Committed as `docs(09-01): on-box smoke checklist + live-host qbXML re-pin guide`.</done>
</task>

<task type="auto">
  <name>Task 6: --verify-audit CLI switch + unit test</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService/Program.cs
    quickbooks/QbConnectService/src/QbConnectService.Tests/VerifyAuditCliTests.cs
  </files>
  <action>
Add a small operator-facing CLI mode to `Program.cs` that exposes the existing `AuditLog.VerifyChainAsync()` (no QuickBooks needed).

In `Program.cs`, **before** `var builder = WebApplication.CreateBuilder(args);` is built into a full web app (or right after building, before `app.Run()` — keep it minimal), detect `args.Contains("--verify-audit")`. When present: build a minimal host that binds `AuditOptions`/`AuditAuthOptions` from configuration (the same `appsettings.json` load — so it reads `Audit:Path`), construct an `AuditLog`, `await auditLog.VerifyChainAsync()`, then: if `Ok` → `Console.WriteLine("audit chain OK")` and `Environment.Exit(0)`; else → `Console.Error.WriteLine($"audit chain BROKEN at seq {FirstBrokenSeq}")` and `Environment.Exit(1)`. Do NOT start Kestrel / the hosted service / the web app in this mode. Keep the change small and self-contained; factor the verify logic into a small `static async Task<int> RunVerifyAuditAsync(string[] args)` if that reads cleaner, but it must remain testable from the Tests project (since `Program` is `public partial`, the helper can be a `public static` method on `Program` — that's the cleanest seam for the test).

Add `VerifyAuditCliTests.cs` to `QbConnectService.Tests`: write two unit tests against the verify helper (or `AuditLog.VerifyChainAsync` driven via a temp `Audit:Path`): (1) a freshly-written valid 2-row chain → `Ok == true` (and the helper returns 0); (2) a tampered/torn last line → `Ok == false`, `FirstBrokenSeq` points at the bad row (and the helper returns 1). Reuse the existing `AuditLog`/`AuditRecord` test patterns from `QbConnectService.Tests` (there are already Phase-6 audit tests — match their style for creating a temp audit dir and appending rows). Do NOT shell out to the exe; test the method directly.
  </action>
  <verify>
`dotnet build quickbooks/QbConnectService` succeeds;
`dotnet test quickbooks/QbConnectService` is green, count up by ≥2 (≈258+ total), `VerifyAuditCliTests` present and passing;
the `--verify-audit` path does not start the web host (the test asserts it returns/exits without binding Kestrel — or simply tests the helper in isolation).
  </verify>
  <done>`QbConnectService.exe --verify-audit` runs the audit hash-chain verification and exits 0 (OK) / 1 (broken), with a passing unit test; the README (Task 4) already references it. Committed as `feat(09-01): --verify-audit CLI switch + tests`.</done>
</task>

<task type="auto">
  <name>Task 7: CI PSScriptAnalyzer lint job + fix all remaining bare-name forward-references</name>
  <files>
    .github/workflows/quickbooks-ci.yml
    .claude/skills/quickbooks-accounting/references/setup-and-troubleshooting.md
    .claude/skills/quickbooks-accounting/references/qbxml-cheatsheet.md
    quickbooks/dev/MULTI-LLM.md
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/README.md   # conditional — only if it lacks the QBXML-REPIN.md pointer
  </files>
  <action>
1. Add a `powershell-lint` job to `.github/workflows/quickbooks-ci.yml` (runs on `windows-latest`, where PSScriptAnalyzer is preinstalled): a single step `pwsh -Command "Invoke-ScriptAnalyzer -Path quickbooks -Recurse -Severity Warning,Error -EnableExit"` (and also lint `quickbooks/dev/*.ps1`). Keep it parallel to the existing jobs (no `needs:`). Match the existing workflow's style/indentation; don't disturb the other jobs.

2. Fix every remaining bare-name forward-reference to the new files so they resolve to the real folders chosen in this plan (`quickbooks/QbConnectService/deploy/` for the scripts; `quickbooks/QbConnectService/docs/` for `register-integrated-app.md`, `SMOKE-CHECKLIST.md`, `QBXML-REPIN.md`; `quickbooks/QbConnectService/README.md` as-is):
   - `.claude/skills/quickbooks-accounting/references/setup-and-troubleshooting.md` — the "Deploy and host docs" section currently lists `README.md`, `register-integrated-app.md`, `make-cert.ps1`/`install-service.ps1`/`uninstall-service.ps1`/`run-as-task.ps1`, and "the on-box smoke checklist" with "(arrives in Phase 9)" / bare names. Update to: `quickbooks/QbConnectService/README.md`, `quickbooks/QbConnectService/docs/register-integrated-app.md`, `quickbooks/QbConnectService/deploy/{make-cert,install-service,uninstall-service,run-as-task}.ps1`, `quickbooks/QbConnectService/docs/SMOKE-CHECKLIST.md`, and add `quickbooks/QbConnectService/docs/QBXML-REPIN.md`. Drop the "(arrives in Phase 9)" qualifier since they now exist; reword "These arrive in Phase 9 and are referenced here..." to "These live under the service folder:".
   - `.claude/skills/quickbooks-accounting/references/qbxml-cheatsheet.md` — if the "Phase-9 re-pin candidates" section references `QBXML-REPIN.md` or the deploy docs by bare name, point them at `quickbooks/QbConnectService/docs/QBXML-REPIN.md`. (Don't otherwise rewrite the cheatsheet — the re-pin items themselves stay, since the re-pin happens on the host.)
   - `quickbooks/dev/MULTI-LLM.md` — if it references any Phase-9 script/doc by bare name, fix to the real path; otherwise leave it (the "Phase artifacts" section is fine). The final close-out commit (Task 8) updates STATE/ROADMAP/REQUIREMENTS, not MULTI-LLM.md — only touch MULTI-LLM.md here if it has a bare forward-reference.
   - `quickbooks/QbConnectService/src/QbConnectService.Qb.Com/README.md` — if it carries a "Phase 9 re-pin" / `tlbimp`-on-host note, leave the note (it's accurate) but add a pointer to `quickbooks/QbConnectService/docs/QBXML-REPIN.md` if not already present (don't rewrite it).
   (`ServerOptions.cs`'s forward-reference was already fixed in Task 2 — verify it points at `quickbooks/QbConnectService/deploy/make-cert.ps1`; fix here if Task 2 missed it.)
  </action>
  <verify>
`.github/workflows/quickbooks-ci.yml` has a `powershell-lint` job invoking `Invoke-ScriptAnalyzer` over `quickbooks` — YAML parses (`pwsh -Command "(Get-Content .github/workflows/quickbooks-ci.yml -Raw)"` is not a substitute — instead just confirm the job block is well-formed YAML by eye and that the existing jobs are untouched; a `yamllint`-style sanity check is fine if available);
`grep -rn "arrives in Phase 9" .claude/skills/quickbooks-accounting/` returns nothing;
`grep -rn "make-cert.ps1\|SMOKE-CHECKLIST\|register-integrated-app\|QBXML-REPIN" .claude/skills/quickbooks-accounting/ quickbooks/dev/MULTI-LLM.md quickbooks/QbConnectService/src/QbConnectService.Qb.Com/README.md` shows every hit now carries the `quickbooks/QbConnectService/deploy/` or `quickbooks/QbConnectService/docs/` (or `README.md`) path, no bare names;
`dotnet build quickbooks/QbConnectService` still succeeds; `dotnet test quickbooks/QbConnectService` still green.
  </verify>
  <done>CI gains a PSScriptAnalyzer lint job over the QuickBooks `.ps1` files; every bare-name forward-reference to the new deploy scripts / docs now resolves to a real folder; no `.cs`/test changes regress. Committed as `ci(09-01): PowerShell lint job + resolve deploy/doc forward-references`.</done>
</task>

<task type="auto">
  <name>Task 8: Close the phase — SUMMARY + ROADMAP + REQUIREMENTS + STATE</name>
  <files>
    .planning/phases/09-packaging-deploy-smoke/09-01-SUMMARY.md
    .planning/phases/09-packaging-deploy-smoke/09-01-PLAN.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
This is the final `docs(09-01)` commit (per the MULTI-LLM conventions: the last commit updates the phase summary + ROADMAP + REQUIREMENTS + STATE).

1. Create `.planning/phases/09-packaging-deploy-smoke/09-01-SUMMARY.md` following the GSD summary template style used by the prior phases' summaries (frontmatter with phase, plan, subsystem, affects, tech-stack, key files, decisions; then a prose body). Capture: the deploy scripts authored (`deploy/_common.ps1`, `make-cert.ps1`, `install-service.ps1`, `uninstall-service.ps1`, `run-as-task.ps1`) — authored + `-WhatIf`-capable + PSScriptAnalyzer-clean, NOT executed against a live service in CI/dev; the sample-parity test; the `register-integrated-app.md` + `README.md` + `SMOKE-CHECKLIST.md` + `QBXML-REPIN.md` docs; the `--verify-audit` CLI switch + tests (test count 255 → whatever it actually is, e.g. 258+); the CI PSScriptAnalyzer job; the forward-reference fixups; the explicit list of **operator follow-ups still pending on the host** (`tlbimp` interop regen, run the on-box smoke, the QuickBooks-side authorization, the live qbXML re-pin edits). Note: zero new NuGet packages; no `.gitignore` changes beyond append-only (and we didn't need any). List the commit hashes/messages from this run.

2. `.planning/ROADMAP.md`: in the "Phases" checklist flip `- [ ] **Phase 9: ...**` to `- [x]`; under "### Phase 9: Packaging, Deploy & On-Box Smoke" change `**Plans**: TBD` → `**Plans**: 1 plan` and replace the `Plans:\n- [ ] 09-01: TBD` block with `- [x] 09-01-PLAN.md — deploy/cert/install/uninstall/task scripts, sample-parity test, register-integrated-app + deploy README + HRESULT table, on-box smoke checklist, qbXML re-pin guide, --verify-audit CLI`; in the "## Progress" table change the `9. Packaging, Deploy & On-Box Smoke` row to `1/1 | ✓ Complete (reviewed 100/100) | 2026-05-12` (the reviewer will confirm the score — write `✓ Complete` and the date; if your run can't assert 100/100 yet, the reviewer updates it, but leave the row showing Complete).

3. `.planning/REQUIREMENTS.md`: flip `DEPLOY-01`, `DEPLOY-02`, `DEPLOY-03`, `DEPLOY-04` from `- [ ]` to `- [x]` in the DEPLOY section, and change their rows in the Traceability table from `Pending` to `Done`. Update the footer "Last updated" line to note Phase 9 deploy packaging complete. (Note in the DEPLOY-01/04 lines or a short parenthetical that the scripts/checklist are authored-and-verified-in-CI-by-static-analysis; the on-box RUN and the live re-pin remain operator follow-ups documented in the new docs — this keeps the requirements honest about the host-only boundary.)

4. `.planning/STATE.md`: update "Current Position" (Phase: 9 of 9 — complete; Status: Phase 9 complete, project complete; Progress: 100%); add a "Phase 9 — Packaging, Deploy & On-Box Smoke (DEPLOY-01..04)" bullet to "### Completed phases" mirroring the SUMMARY (deploy scripts authored not-run, sample-parity test, the four docs, `--verify-audit` + tests, CI lint job, forward-ref fixups, test count, operator follow-ups still pending on the host); move the Phase-9 items out of "Blockers/Concerns" into a "Post-project operator follow-ups (on `10.120.254.13`)" note (the `tlbimp` regen, the on-box smoke run, the QB-side auth, the live re-pin); update "Session Continuity" / velocity (9 plans, all phases complete). Add a Phase-9 decision bullet or two if warranted (e.g. "scripts authored + statically verified; live execution is an operator step", "added `--verify-audit` CLI to expose the existing `AuditLog.VerifyChainAsync`").

Then: update the Task-1..7 checkboxes in THIS plan file to `[x]`, run the full `dotnet test quickbooks/QbConnectService` once more (must be green), and commit everything in this task as `docs(09-01): phase 9 summary + roadmap/requirements/state`.
  </action>
  <verify>
`Test-Path .planning/phases/09-packaging-deploy-smoke/09-01-SUMMARY.md` true;
`grep -n "Phase 9" .planning/ROADMAP.md` shows `- [x] **Phase 9` and the Progress-table row shows `✓ Complete`;
`grep -n "DEPLOY-0" .planning/REQUIREMENTS.md` shows all four as `- [x]` and `Done` in the table;
`grep -n "9 of 9\|100%" .planning/STATE.md` reflects project-complete;
`dotnet test quickbooks/QbConnectService` green;
`git log --oneline -10` shows one commit per task (8 commits for this run), distinct conventional-commit titles, the last being `docs(09-01): phase 9 summary + roadmap/requirements/state`, no duplicate `feat(09-01)` subjects.
  </verify>
  <done>Phase 9 is closed: SUMMARY written, ROADMAP shows Phase 9 ✓ Complete, REQUIREMENTS DEPLOY-01..04 → done, STATE shows the project complete with the host-only operator follow-ups recorded; this plan's task checkboxes are ticked; tests green; clean atomic commit history. Committed as `docs(09-01): phase 9 summary + roadmap/requirements/state`.</done>
</task>

</tasks>

<verification>
Phase-level checks (DEV environment — nothing requiring QuickBooks or the host):
- `dotnet build quickbooks/QbConnectService` succeeds; `dotnet test quickbooks/QbConnectService` is green and the count rose from 255 (≈258+), including `SampleConfigParityTests` and `VerifyAuditCliTests`.
- `pwsh -NoProfile -Command "if (-not (Get-Module PSScriptAnalyzer -ListAvailable)) { Install-Module PSScriptAnalyzer -Scope CurrentUser -Force -AllowClobber }; Invoke-ScriptAnalyzer -Path quickbooks -Recurse -Severity Warning,Error"` → no findings (CI-verified; this local run is best-effort and auto-installs PSScriptAnalyzer if missing — the Task-7 `powershell-lint` job on `windows-latest` is the real gate); every `deploy/*.ps1` parses; `_common.ps1` dot-sources and exposes `Assert-Elevated`/`Grant-ServiceLogonRight`/`Get-RepoRoot`; each script's `-WhatIf`/params show via `Get-Command -Syntax`.
- `git status --porcelain` shows NO `appsettings.json` / `clients/.env` created; `.gitignore` unchanged.
- All four DEPLOY docs exist: `quickbooks/QbConnectService/README.md`, `quickbooks/QbConnectService/docs/register-integrated-app.md`, `quickbooks/QbConnectService/docs/SMOKE-CHECKLIST.md`, `quickbooks/QbConnectService/docs/QBXML-REPIN.md`; the README's HRESULT table matches `QbErrors.cs`.
- No bare-name forward-references remain (`grep` per Task 7).
- `.github/workflows/quickbooks-ci.yml` has the `powershell-lint` job; other jobs untouched.
- `git log --oneline -10`: 8 atomic commits, distinct conventional-commit titles (`feat(09-01)`, `test(09-01)`, `docs(09-01)`×3, `feat(09-01)` for --verify-audit, `ci(09-01)`, `docs(09-01)` close-out), no duplicate `feat()` subjects; final commit updates SUMMARY+ROADMAP+REQUIREMENTS+STATE and flips Phase 9 to ✓ Complete.
- Stayed on `quickbooks/direct-sdk-integration-2026-05-11`; only files under the allowed paths touched (`quickbooks/`, `.claude/skills/quickbooks-accounting/`, `.github/workflows/quickbooks-ci.yml`, `.planning/phases/09-...`, `.planning/{ROADMAP,REQUIREMENTS,STATE}.md`); no Codex attempt at `tlbimp` / on-host smoke / QB-side auth / live re-pin edits.
</verification>

<success_criteria>
- ROADMAP Phase 9 success criteria 1–4 satisfied by the authored scripts/docs/checklists:
  1. `make-cert.ps1` (cert), `install-service.ps1`/`uninstall-service.ps1` (service as `svc_qbsdk` + restart-on-crash), `run-as-task.ps1` (session-0 fallback) — all present, `-WhatIf`-capable, PSScriptAnalyzer-clean.
  2. Machine-specifics only in gitignored `QbConnectService/appsettings.json` + `clients/.env`, each with a committed `.sample`; enforced by `SampleConfigParityTests`; nothing machine-specific hardcoded in source.
  3. `register-integrated-app.md` (one-time QB-side auth, single-user/Admin, allow-even-if-QB-not-running, bound to `svc_qbsdk`, Reauthorize path, PII gotcha) + `README.md` (full `10.120.254.13` deploy runbook, QBWC-fallback note, HRESULT table from `QbErrors.cs`).
  4. On-box smoke checklist verifies in order health → company_info → report → create_customer dry-run → (AllowWrites=true) one real low-stakes write → confirm in QuickBooks → confirm the audit-log row, and records the environment facts (QB Enterprise year/version, multi-user hosting, `svc_qbsdk` + "log on as a service", firewall path for the HTTPS port).
- DEPLOY-01..04 all → done in REQUIREMENTS.
- `dotnet build` + `dotnet test` green (255 → ≈258+); CI green incl. the new PSScriptAnalyzer job.
- Scope clean (nothing outside allowed paths; no committed machine-specific config; no Codex attempt at host-only tasks — those are documented as operator follow-ups in the new docs + STATE).
- Hygiene clean: one atomic commit per task, distinct conventional-commit titles, no duplicate `feat()` subjects, the final `docs(09-01)` commit updates SUMMARY+ROADMAP+REQUIREMENTS+STATE and flips Phase 9 to ✓ Complete; zero dangling output.
</success_criteria>

<non_goals>
Operator follow-ups that happen LATER on host `10.120.254.13` — this plan DOCUMENTS them; Codex/the executor must NOT attempt them:
- `tlbimp` regen of the real `Interop.QBXMLRP2Lib.dll` (the hand-written `Interop/QBXMLRP2Lib.cs` stub is replaced on the host) — the run-codex HARD RULES already forbid `tlbimp` here.
- Running the on-box smoke checklist (the actual `Invoke-RestMethod` calls against the live service) — operator runs `docs/SMOKE-CHECKLIST.md` on the host.
- The QuickBooks-side integrated-app authorization (single-user, Admin, the allow-even-if-QB-not-running grant, the PII checkbox) — operator runs `docs/register-integrated-app.md` on the host.
- The live qbXML re-pin EDITS (diffing constructed fixtures/builders/parsers against the host's qbXML dialect and fixing them) — operator runs `docs/QBXML-REPIN.md` on the host; only that work edits production qbXML code, and it does so on the host.
- Executing the deploy/cert/install/uninstall/task scripts against a real service — they're authored + statically verified here, run on the host.
- Creating/committing the real `appsettings.json` / `clients/.env` (they're gitignored and operator-supplied on the host).
- Any new wrapped ops, a session-keeper wrapper, or DeepSeek-integrated research (v2 — out of scope).
</non_goals>

<output>
After completion, create `.planning/phases/09-packaging-deploy-smoke/09-01-SUMMARY.md` (Task 8 does this as part of the close-out commit).
</output>
