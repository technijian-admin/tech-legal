---
phase: 03-qbxml-engine
plan: 01
type: execute
wave: 1
depends_on: []          # Phases 1 & 2 are complete (separate phases, reviewed 100/100); no in-phase plan deps
files_modified:
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlOptions.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbOptions.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlModels.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlBuilder.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlParser.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbReportParser.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbResponseSpiller.cs
  - quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbListExecutor.cs
  - quickbooks/QbConnectService/src/QbConnectService/Program.cs
  - quickbooks/QbConnectService/src/QbConnectService/appsettings.sample.json
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fakes/FakeRequestProcessor.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.normal.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.zerorows.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.dataext.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/ItemQueryRs.polymorphic.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/InvoiceAddRs.error.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/GeneralSummaryReportQueryRs.pnl.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.page1.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.page2.qbxml
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbXmlOptionsBindingTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbOptionsBindingTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbXmlBuilderTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbXmlParserTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbReportParserTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbResponseSpillerTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/FakeRequestProcessorTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbListExecutorTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/HostStartupTests.cs
  - quickbooks/QbConnectService/src/QbConnectService.Tests/QbConnectService.Tests.csproj
autonomous: true

checklist:
  - [x] Task 1: QbXmlOptions POCO + relocate OwnerIdZero out of QbOptions + appsettings.sample.json QbXml section + bind in Program.cs + QbXmlOptionsBindingTests + update QbOptionsBindingTests
  - [x] Task 2: QbXmlModels (records + IteratorMode enum) + QbXmlBuilder (envelope + version PI + onError + iterator attrs + OwnerID) + QbXmlBuilderTests + DI
  - [x] Task 3: QbXmlParser (status, normalized rows, zero-row-as-success, DataExtRet customFields, polymorphic Item type discriminator) + entity fixtures + QbXmlParserTests + DI
  - [x] Task 4: QbReportParser (ColDesc-driven, DataRow/SubtotalRow/TotalRow/TextRow) + P&L report fixture + QbReportParserTests + DI
  - [x] Task 5: QbResponseSpiller (size-guard spill-to-file, SpillPath -> Audit:Path -> %TEMP% fallback) + QbResponseSpillerTests + DI
  - [x] Task 6: FakeRequestProcessor multi-response queue (AddResponses) + ProcessRequests capture (BC-preserving) + FakeRequestProcessorTests extension
  - [x] Task 7: QbListExecutor (Start -> Continue -> done accumulation over QbConnectionManager + mid-iteration error abort + size-guard call) + iterator fixtures + QbListExecutorTests + DI
  - [x] Task 8: DI registration smoke + final sweep (Program.cs registers all 5 engine singletons + Configure<QbXmlOptions>; host-resolves test; full build/test green)
  - [x] Plan checkbox update + 03-01-SUMMARY.md (in commits)

must_haves:
  truths:
    - "QbXmlBuilder.BuildRequest emits the full envelope: <?xml version=\"1.0\" encoding=\"utf-8\"?> declaration, then the <?qbxml version=\"N.N\"?> PI with N.N from QbXmlOptions.Version (config), then <QBXML><QBXMLMsgsRq onError=\"stopOnError\">{body}</QBXMLMsgsRq></QBXML> — pinned by a golden-string test against a known QbXmlOptions.Version"   # READ-01
    - "QbXmlParser.Parse returns ParsedQbXmlResponse with message-level QbStatus and per-*Rs ParsedElement carrying its own QbStatus(Code,Severity,Message) verbatim, optional iteratorID/iteratorRemainingCount, and Rows = List<Dictionary<string,object?>> (leaf -> string, complex child -> nested dict, repeated child -> List, DataExtRet -> customFields list, Item subtype -> type discriminator)"  # READ-01, READ-07-parser-half, READ-11-parser-half
    - "A zero-row response (statusCode=\"1\", statusSeverity Info/Warn, no *Ret children) parses as a SUCCESS with an empty Rows list and Status.IsError == false — never an exception; Status.IsError is true only when statusSeverity == \"Error\""  # READ-01
    - "QbReportParser.Parse reads every <ColDesc> to learn the columns (id/title/type), walks <ReportData> rows (DataRow/SubtotalRow/TotalRow/TextRow) mapping each <ColData> to a column BY colID (never by XML position), captures <RowData> as the row label, and returns ParsedReport{Title,Subtitle,Basis?,Columns[],Rows[]} — covered by a golden test against a committed P&L fixture"  # READ-02
    - "QbListExecutor.RunAsync stamps iterator=\"Start\" + MaxReturned (from config) on a clone of the caller's *Rq, sends it through QbConnectionManager.ExecuteAsync, parses, then while iteratorRemainingCount > 0 re-sends iterator=\"Continue\" iteratorID=<from response> with the same requestID, accumulating rows from every page into one ParsedQbXmlResponse; a statusSeverity=\"Error\" page aborts the loop and surfaces that page's status with whatever rows were already accumulated"  # READ-03
    - "When the concatenated raw qbXML across all iterator pages exceeds QbXmlOptions.MaxResponseBytes, QbResponseSpiller writes the raw qbXML to <SpillPath ?? Audit:Path ?? %TEMP%\\QbConnectService\\spill>\\<UTC-timestamp>-<guid>.qbxml and the returned ParsedQbXmlResponse carries RawSpilledTo = that path (the parsed Rows are still populated)"  # READ-03
    - "Whether read requests include <OwnerID>0</OwnerID> to get DataExtRet custom fields is the documented config knob QbXmlOptions.OwnerIdZero (default false), present in appsettings.sample.json under \"QbXml\"; QbXmlBuilder.WithOwnerIdZero(queryElement) is the capability that adds it (idempotent)"  # READ-11
    - "QbXmlBuilder, QbXmlParser, QbReportParser are pure (no file/COM/QbConnectionManager I/O); only QbListExecutor and QbResponseSpiller touch I/O; all five are registered as singletons in Program.cs and Configure<QbXmlOptions>(GetSection(\"QbXml\")) is wired"
    - "dotnet build (solution) + dotnet test (solution) stay green at every commit with no QuickBooks SDK installed; the 44 existing Phase 1/2 tests still pass; zero new NuGet packages added"
  artifacts:
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlOptions.cs"
      provides: "QbXmlOptions POCO: Version=\"16.0\", OwnerIdZero=false (relocated from QbOptions), MaxReturned=100, MaxResponseBytes=5_000_000, SpillPath=\"\""
      contains: "class QbXmlOptions"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlBuilder.cs"
      provides: "Pure builder: BuildRequest(XElement)/(IEnumerable<XElement>) -> full qbXML string with <?xml?> decl + <?qbxml version?> PI + onError=\"stopOnError\"; static Rq(name, content), WithIterator(el, mode, iteratorId?, maxReturned?, requestId), WithOwnerIdZero(el); Version property"
      min_lines: 50
      contains: "XProcessingInstruction"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlParser.cs"
      provides: "Pure parser: Parse(string) -> ParsedQbXmlResponse; per-*Rs QbStatus + iterator attrs; *Ret -> Dictionary<string,object?> (leaf->string, complex->nested dict, repeated->List, DataExtRet->customFields, Item subtype->type); zero-row-as-success; QbXmlParseException on malformed input"
      min_lines: 60
      contains: "QbXmlParseException"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbReportParser.cs"
      provides: "Pure report parser: Parse(string) -> ParsedReport; ColDesc-driven columns; ReportData rows by element name with ColData matched by colID; RowData as label; tolerant of ColTitle element-vs-attr / ColType element-vs-attr casing"
      min_lines: 50
      contains: "ColDesc"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlModels.cs"
      provides: "Records: QbStatus(Code,Severity,Message){IsError}, ParsedElement(Name,Status,IteratorId,IteratorRemaining,Rows), ParsedQbXmlResponse(Message,Elements,RawSpilledTo?){First}, ParsedReport, ReportColumn, ReportRow; enum IteratorMode{None,Start,Continue}"
      contains: "record QbStatus"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbResponseSpiller.cs"
      provides: "Size-guard spill: Threshold, ExceedsThreshold(raw), SpillAsync(raw,ct) -> path; dir = SpillPath ?? config[\"Audit:Path\"] ?? %TEMP%\\QbConnectService\\spill; Directory.CreateDirectory; filename {UTC yyyyMMdd-HHmmssfff}-{guid:N}.qbxml; File.WriteAllTextAsync"
      contains: "SpillAsync"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbListExecutor.cs"
      provides: "Impure iterator runner over QbConnectionManager: RunAsync(XElement queryRq, bool ownerIdZero?, CancellationToken) -> ParsedQbXmlResponse; Start->Continue->done accumulation; mid-iteration Error abort; post-accumulation size-guard via QbResponseSpiller"
      min_lines: 40
      contains: "iterator"
    - path: "quickbooks/QbConnectService/src/QbConnectService/Program.cs"
      provides: "Configure<QbXmlOptions>(GetSection(\"QbXml\")) + AddSingleton for QbXmlBuilder/QbXmlParser/QbReportParser/QbResponseSpiller/QbListExecutor"
      contains: "QbXmlOptions"
    - path: "quickbooks/QbConnectService/src/QbConnectService/appsettings.sample.json"
      provides: "\"QbXml\": { \"Version\": \"16.0\", \"OwnerIdZero\": false, \"MaxReturned\": 100, \"MaxResponseBytes\": 5000000, \"SpillPath\": \"\" }; \"Qb\" no longer has OwnerIdZero"
      contains: "MaxResponseBytes"
    - path: "quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/GeneralSummaryReportQueryRs.pnl.qbxml"
      provides: "Committed P&L report response fixture (2 ColDesc, TextRow/DataRow/SubtotalRow/TotalRow) — note in 03-01-SUMMARY.md whether captured from a real SDKTestPlus3/OSR run or constructed"
      contains: "ColDesc"
  key_links:
    - from: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbListExecutor.cs"
      to: "QbConnectionManager.ExecuteAsync"
      via: "iterator page send"
      pattern: "ExecuteAsync"
    - from: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbListExecutor.cs"
      to: "QbResponseSpiller.SpillAsync"
      via: "post-accumulation size guard"
      pattern: "Spill"
    - from: "quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlBuilder.cs"
      to: "QbXmlOptions.Version"
      via: "<?qbxml version?> PI"
      pattern: "_opts\\.Version|opts\\.Value\\.Version"
    - from: "quickbooks/QbConnectService/src/QbConnectService/Program.cs"
      to: "QbXmlOptions"
      via: "Configure<QbXmlOptions>(GetSection(\"QbXml\"))"
      pattern: "Configure<QbXmlOptions>"
    - from: "quickbooks/QbConnectService/src/QbConnectService.Tests/QbListExecutorTests.cs"
      to: "FakeRequestProcessor.AddResponses"
      via: "two-page iterator scripting"
      pattern: "AddResponses"
---

<objective>
Build the pure, I/O-free **qbXML engine** that Phase 4's read ops (and, via Phase 6, Phase 7's writes) compose on top of: a request builder (`QbXmlBuilder`), an entity-response parser (`QbXmlParser`), a separate header-aware report parser (`QbReportParser`), an iterator runner (`QbListExecutor`) that drives Phase 2's `QbConnectionManager` Start->Continue->done, a size-guard spill-to-file (`QbResponseSpiller`), the `QbXmlOptions` config POCO, the `FakeRequestProcessor` multi-response extension needed to test iterators, plus tests and DI registration.

Purpose: Phase 3 closes READ-01 (builder emits the `<?qbxml version?>` PI; parser surfaces per-element/per-message status; zero-row = success), READ-02 (`ColDesc`-driven report parser, no ordinal guessing), READ-03 (iterators + size-guard spill), and READ-11 (`OwnerID="0"` is a documented config knob with a sensible default). It is the engine layer — Phase 4 builds the actual ops on it.

Output: 8 atomic commits (one per task, suite green at every commit) producing: `QbXmlOptions.cs`, `QbXmlModels.cs`, `QbXmlBuilder.cs`, `QbXmlParser.cs`, `QbReportParser.cs`, `QbResponseSpiller.cs`, `QbListExecutor.cs` in `QbConnectService.Qb.Com` (namespace `QbConnectService.Qb`); the `OwnerIdZero` relocation out of `QbOptions.cs`; `Program.cs` + `appsettings.sample.json` updates; the `FakeRequestProcessor` queue/capture extension; committed `.qbxml` fixtures under `QbConnectService.Tests/Fixtures/qbxml/`; new test classes; the plan-checkbox update; and `03-01-SUMMARY.md`.
</objective>

<execution_context>
@C:\Users\rjain\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rjain\.claude/get-shit-done/templates/summary.md

**Repo hygiene (this branch, this run — non-negotiable):**
- Git repo root is `C:\VSCode\tech-legal\tech-legal` (NOT the parent directory). Use absolute paths for all file I/O. `cd` into the repo root before any git command.
- You are on branch `quickbooks/direct-sdk-integration-2026-05-11`. NEVER `git checkout` another branch. NEVER touch `main`. NEVER `git add -A` / `git add .` / `git add -u` / `git reset --hard` / `git clean` — the working tree has UNRELATED active work (India HR) that must not be staged or destroyed.
- **Per-task scoped staging only.** For each task, `git add` ONLY the exact files that task touches (the ones listed in that task's `<files>`). Then `git commit -m "<message>"`. Never stage anything outside `quickbooks/`.
- Only ever create/modify files under `quickbooks/QbConnectService/...` (plus, at the very end, `.planning/phases/03-qbxml-engine/03-01-PLAN.md` for the checkbox update and `.planning/phases/03-qbxml-engine/03-01-SUMMARY.md` — those two are committed too, in their own commit).
- Do NOT run `git init` anywhere. Do NOT create a nested repo. Do NOT add a `.git` directory under `quickbooks/`.
- The repo's `CLAUDE.md` rules about "GitNexus impact analysis before edits" and "Client Portal API safety" do NOT apply here — this is a new, isolated subsystem (`quickbooks/QbConnectService`), not the GitNexus app or the Client Portal.
- **Commit-message hygiene (new this phase — Phase 2 had two duplicate-titled `feat(...)` commits; do not repeat):** one commit per task with a distinct `feat(03-01): <short summary>` title. If you revise a task you ALREADY committed in this same run, either `git commit --amend` that commit, or make a NEW commit with a distinct `fix(03-01): ...` or `refactor(03-01): ...` message — NEVER re-use the same `feat(...)` title twice.
- **Leave nothing dangling:** after Task 8, (a) tick this plan's `checklist:` boxes in `03-01-PLAN.md`, (b) write `.planning/phases/03-qbxml-engine/03-01-SUMMARY.md` using the summary template — and commit both in a final `docs(03-01): plan checkboxes + summary` commit.
- **Build/test commands** (run from the repo root): `dotnet build quickbooks/QbConnectService/QbConnectService.sln -c Debug` and `dotnet test quickbooks/QbConnectService/QbConnectService.sln`. (A Release/x86 build sanity-check is part of Task 8 only.) Both must be green at every commit. No QuickBooks SDK is required for any of this — everything is exercised against `FakeRequestProcessor` via `QbConnectionManager`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# This phase's research has the full task breakdown, recommended class designs, the XLinq envelope/PI code, the parsed-result record shapes, the fixture sketches, the FakeRequestProcessor extension, the pitfalls, and the DI wiring — follow it.
@.planning/phases/03-qbxml-engine/03-RESEARCH.md

# Phases 1 & 2 code that Phase 3 builds directly on:
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/Qb/IRequestProcessor.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbConnectionManager.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbOptions.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbErrors.cs
@quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbExceptions.cs
@quickbooks/QbConnectService/src/QbConnectService.Tests/Fakes/FakeRequestProcessor.cs
@quickbooks/QbConnectService/src/QbConnectService.Tests/QbConnectionManagerTests.cs
@quickbooks/QbConnectService/src/QbConnectService.Tests/QbOptionsBindingTests.cs
@quickbooks/QbConnectService/src/QbConnectService.Tests/HostStartupTests.cs
@quickbooks/QbConnectService/src/QbConnectService.Tests/FakeRequestProcessorTests.cs
@quickbooks/QbConnectService/src/QbConnectService/Program.cs
@quickbooks/QbConnectService/src/QbConnectService/appsettings.sample.json
@quickbooks/QbConnectService/src/QbConnectService.Tests/QbConnectService.Tests.csproj
@.planning/phases/02-com-session-lifecycle/02-01-SUMMARY.md
</context>

<conventions>
- **All new types** (`QbXmlOptions`, `QbXmlBuilder`, `QbXmlParser`, `QbReportParser`, `QbXmlModels` records, `QbResponseSpiller`, `QbListExecutor`, `QbXmlParseException`) live in the **`QbConnectService.Qb.Com`** project under namespace **`QbConnectService.Qb`** (matching Phase 1/2: `IRequestProcessor.cs`, `QbConnectionManager.cs`, `QbOptions.cs` are all `namespace QbConnectService.Qb` in that project). One type-cluster per file as shown in `<files>` (`QbXmlModels.cs` holds the small records + the `IteratorMode` enum).
- **The test project references the HOST project only** (`..\QbConnectService\QbConnectService.csproj`), NOT `QbConnectService.Qb.Com` directly — and it stays that way (the `<!-- ONLY the host. NEVER QbConnectService.Qb.Com. -->` comment in `QbConnectService.Tests.csproj` is load-bearing). All new engine types are `public`, so the test project sees them transitively through the host. Do NOT add a `ProjectReference` to `Qb.Com` from the test project.
- **Zero new NuGet packages.** Use only in-box `System.Xml.Linq` (`XDocument`/`XElement`/`XDeclaration`/`XProcessingInstruction`/`XAttribute`/`SaveOptions`), `System.Text.Json` (only if a test wants to serialize a parsed result for a golden comparison — not required), `Microsoft.Extensions.Options` (`IOptions<T>`), `Microsoft.Extensions.Logging` (`ILogger<T>`), `Microsoft.Extensions.DependencyInjection`, `Microsoft.Extensions.Configuration` (`IConfiguration` — available in `Qb.Com` via the `Microsoft.AspNetCore.App` framework reference it already has). Do NOT add `Verify.Xunit` — committed `.qbxml` fixtures + plain `Assert.Equal` (whitespace-normalized where needed) are enough and keep the zero-new-packages streak Phases 1 & 2 set.
- **Pure vs impure boundary is load-bearing** (ARCHITECTURE.md: "Builder and Parser are pure"): `QbXmlBuilder`, `QbXmlParser`, `QbReportParser` do NO I/O — no file, no COM, no `QbConnectionManager`, no `ILogger` even (they can take `IOptions<QbXmlOptions>` for config). Only `QbListExecutor` (calls `QbConnectionManager` + `QbResponseSpiller` + logs) and `QbResponseSpiller` (writes a file) touch I/O.
- **Fixtures** go under `quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/` and must be marked `CopyToOutputDirectory="PreserveNewest"` in `QbConnectService.Tests.csproj` (e.g. `<None Update="Fixtures\qbxml\*.qbxml"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></None>`) so tests can `File.ReadAllText(Path.Combine(AppContext.BaseDirectory, "Fixtures", "qbxml", "<name>.qbxml"))` at runtime. (That csproj edit is part of Task 3, the first task that adds a fixture.)
- Follow the test-construction pattern Phase 2's `QbConnectionManagerTests` use for anything needing a `QbConnectionManager`: `new QbConnectionManager(() => fake, Options.Create(new QbOptions{...}), Options.Create(new RequestOptions{...}), NullLogger<QbConnectionManager>.Instance)` with a `List<FakeRequestProcessor>` the factory appends to. Follow `HostStartupTests`' pattern (`Host.CreateDefaultBuilder().ConfigureServices(...)`) for the host-resolves test.
</conventions>

<scope_discipline>
**In scope (Phase 3 only) — the qbXML *engine*:** `QbXmlBuilder` (pure), `QbXmlParser` (pure), `QbReportParser` (pure, separate class), `QbListExecutor` (iterator Start->Continue loop over Phase 2's `QbConnectionManager`), `QbResponseSpiller` (size-guard spill-to-file), the `QbXmlOptions` config POCO (with `OwnerIdZero` relocated into it), the `FakeRequestProcessor` multi-response/capture extension, their tests + DI registration, and the `appsettings.sample.json`/`Program.cs` wiring.

**NOT in scope — do NOT build any of this here:**
- **Any read op** — `company_info`, `get_company_preferences`, `report`, `list_customers`/`list_vendors`/`list_items`/`list_accounts`, `list_invoices`/`list_bills`/`list_payments`, `get_transaction`, `run_query`. Those are **Phase 4**. Phase 3 gives Phase 4 the *seam* (`QbXmlBuilder.Rq(name, content)` to build a `*Rq` body; `QbXmlParser.Parse` / `QbReportParser.Parse` to read the response; `QbListExecutor.RunAsync` to page a list query) — it does NOT build any op-specific request shapes or op handlers.
- **Any REST surface** — no `/api/health`, no `/api/qbxml`, no `/api/ops/*`, no Kestrel HTTPS-only binding, no bearer middleware, no `OpRegistry`. **Phase 5.** Phase 3 has **no end-user-visible / HTTP-visible behavior** — it's a library layer. The bar for the reviewer is "exercised via `FakeRequestProcessor` through `QbConnectionManager` and via direct unit tests", NOT "callable over HTTP". (Reviewer note: an absent "callable over HTTP" test is by design — that's Phase 5.)
- **Any write op / write safety** — no `create_*`, `mod_*`, `receive_payment`, `create_journal_entry`; no `AllowWrites` 403 gate; no `/dryrun`; no hash-chained audit log. **Phases 6 & 7.** EXCEPTION (and it is only this): the stale-`EditSequence` qbXML *status code* (`0x800404C5` / the `3200`-class code) is a per-element `statusCode` that `QbXmlParser` simply *surfaces* verbatim on `ParsedElement.Status.Code` (alongside every other `statusCode`) so Phase 7 can recognise it later. **Exposing `statusCode` is NOT handling stale-`EditSequence`** — there is zero retry/auto-fix/mapping logic for it in Phase 3. (`QbErrors` in `Qb.Com` is HRESULTs only and stays untouched; qbXML `statusCode` mapping is Phase 7's job.)
- The Python client (`qb_client.py`), the `quickbooks-accounting` Claude skill, `MULTI-LLM.md` updates, `run-codex-phase.ps1` edits — **Phase 8**.
- Deploy scripts (`make-cert.ps1`, `install-service.ps1`, `uninstall-service.ps1`, `run-as-task.ps1`), `register-integrated-app.md`, the `README.md` deploy runbook, regenerating the real interop DLL, `appsettings.json` (the gitignored real config) — **Phase 9**.

**Deliberate decisions / known limits (so the reviewer doesn't flag them):**
- **`OwnerIdZero` is relocated** from `QbOptions` (`Qb.Com`) to the new `QbXmlOptions` (Task 1) so all qbXML knobs live together (research recommendation 4). This is a small, deliberate cross-cutting refactor: drop the `bool OwnerIdZero` property from `QbOptions.cs`, add it to `QbXmlOptions`, move the `"OwnerIdZero"` key from the `"Qb"` section to the `"QbXml"` section in `appsettings.sample.json`, and update `QbOptionsBindingTests.cs` (remove the `Qb:OwnerIdZero` key/assertion) + add `QbXmlOptionsBindingTests.cs`. The suite must stay green at that commit. (`QbConnectionManager.cs` does NOT reference `OwnerIdZero` today — verified — so nothing in the manager changes.)
- **Parsed-body shape is `List<Dictionary<string,object?>>`**, not `JsonNode` (research recommendation 2): scalar leaf -> `string`; complex child element -> nested `Dictionary<string,object?>`; repeated sibling elements -> `List<...>`; `DataExtRet` blocks -> a `"customFields"` key holding `List<Dictionary<string,object?>>` of `{OwnerID, DataExtName, DataExtType, DataExtValue}`; polymorphic `Item*Ret` rows get a `"type"` key (`"Service"`, `"Inventory"`, ...). No JSON dependency needed for parsing. (A test MAY serialize a parsed row to JSON for a compact golden assertion — that's fine, `System.Text.Json` is in-box.)
- **`QbReportParser` is a separate class**, not a method on `QbXmlParser` (FEATURES.md / ARCHITECTURE.md are explicit). It shares no structural code with the entity parser.
- **The report fixture (`GeneralSummaryReportQueryRs.pnl.qbxml`)**: source it from a real `SDKTestPlus3`/OSR `ProfitAndLossStandard` run if you can obtain one; if you cannot, construct a plausible one **and say so explicitly in `03-01-SUMMARY.md`** ("the P&L report fixture is constructed, not captured — exact `ColDesc`/`ColTitle`/`ColType`/`RowData` attribute casing should be re-pinned in Phase 9 against the live host"). Either way, write `QbReportParser` *tolerantly*: for the column title try `<ColTitle value="...">` element first, then a `colTitle` attribute on `<ColDesc>`; for the type try `<ColType>` element then a `colType` attribute; use `LocalName` comparisons; null-safe everywhere. The *design* (ColDesc -> columns; ReportData rows; ColData by `colID`) is correct regardless of the exact attribute names.
- **`QbListExecutor` doesn't *decide* whether `OwnerID="0"` is sent.** Give it a signature like `RunAsync(XElement queryRq, bool ownerIdZero = false, CancellationToken ct = default)` (or read `_opts.OwnerIdZero` as the default for the `ownerIdZero` parameter) — Phase 4's ops pass it through. Phase 3 just wires the *capability* (`QbXmlBuilder.WithOwnerIdZero`) and the config knob (`QbXmlOptions.OwnerIdZero`, documented default `false`).
- **`QbResponseSpiller` reads `Audit:Path` via `IConfiguration`** (inject `IConfiguration` and read `config["Audit:Path"]`) — do NOT pre-create the `AuditOptions` POCO (that's Phase 6). Resolution order for the spill directory: `QbXmlOptions.SpillPath` if non-empty, else `config["Audit:Path"]` if non-empty, else `Path.Combine(Path.GetTempPath(), "QbConnectService", "spill")`. `Directory.CreateDirectory(dir)` before writing.
- **`onError="stopOnError"`** is a constant on `<QBXMLMsgsRq>` for v1 (single op per message anyway). Don't parameterize it.
- **`MaxReturned` is a CHILD ELEMENT** on the query types (`CustomerQuery`/`VendorQuery`/`ItemQuery`/...), NOT an attribute. `iterator`, `iteratorID`, `requestID` ARE attributes. `WithIterator` adds `<MaxReturned>` as a child only if the element doesn't already have one (so a Phase-4 op that supplied its own `MaxReturned` isn't overridden).
- **The `<?xml ...?>` declaration must be emitted.** `XDocument.ToString()` / `XElement.Save(StringWriter)` drop it by default — save to a `TextWriter` (the `Utf8StringWriter : StringWriter { override Encoding => Encoding.UTF8 }` trick) so the declaration is written and says `encoding="utf-8"`. Pick `encoding="utf-8"` (QuickBooks accepts utf-8/UTF-8/US-ASCII) and PIN it in a builder golden test so it can't drift.
- **No qbXML schema validation at runtime** (the `qbxmlops*.xsd` aren't in the repo). Golden-string tests on builder output + the round-trip through `FakeRequestProcessor` + Phase 9's on-box `SDKTestPlus3` smoke are the safety net.
</scope_discipline>

<tasks>

<task type="auto">
  <name>Task 1: QbXmlOptions POCO + relocate OwnerIdZero out of QbOptions + appsettings.sample.json QbXml section + bind in Program.cs + binding tests</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlOptions.cs (new),
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbOptions.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService/appsettings.sample.json (modify),
    quickbooks/QbConnectService/src/QbConnectService/Program.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbXmlOptionsBindingTests.cs (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbOptionsBindingTests.cs (modify)
  </files>
  <action>
    Create `QbXmlOptions.cs` in `QbConnectService.Qb.Com`, `namespace QbConnectService.Qb`, one `public sealed class QbXmlOptions` (per 03-RESEARCH.md "Pattern 6: Config"):
    - `public string Version { get; set; } = "16.0";` — emitted in the `<?qbxml version?>` PI.
    - `public bool OwnerIdZero { get; set; }` — RELOCATED from `QbOptions`; default `false`; documented; READ-11.
    - `public int MaxReturned { get; set; } = 100;` — iterator page size.
    - `public int MaxResponseBytes { get; set; } = 5_000_000;` — spill threshold (~5 MB).
    - `public string SpillPath { get; set; } = "";` — `""` means "fall back to `Audit:Path`, then `%TEMP%\QbConnectService\spill`".
    In `QbOptions.cs`: REMOVE the `public bool OwnerIdZero { get; set; }` property (it now lives in `QbXmlOptions`). Leave everything else in `QbOptions`/`RequestOptions` unchanged.
    In `appsettings.sample.json`: REMOVE `"OwnerIdZero": false` from the `"Qb"` object; REPLACE the existing `"QbXml": { "Version": "16.0" }` with `"QbXml": { "Version": "16.0", "OwnerIdZero": false, "MaxReturned": 100, "MaxResponseBytes": 5000000, "SpillPath": "" }`. Keep `"Audit": { "Path": "C:\\ProgramData\\QbConnectService\\audit" }` unchanged. Use double-backslash Windows paths as the file already does; placeholders only — no machine-specifics.
    In `Program.cs`: add `builder.Services.Configure<QbXmlOptions>(builder.Configuration.GetSection("QbXml"));` near the existing `Configure<QbOptions>` / `Configure<RequestOptions>` lines. Leave the rest of `Program.cs` untouched — the `AddSingleton<QbXmlBuilder>()` etc. registrations come in Tasks 2-8 (you may add them incrementally as each type is created, or all in Task 8 — but DO add `Configure<QbXmlOptions>` now). Add `using QbConnectService.Qb;` if not already present (it is).
    Update `QbOptionsBindingTests.cs`: remove the `["Qb:OwnerIdZero"] = "false"` entry from the in-memory config dictionary (it's no longer a `Qb` key). The rest of that test stays as-is. (It already only asserts `OpenMode`/`ConnectionType`/`AppName`/`TimeoutSeconds`/`BusyWaitSeconds`, so removing the `OwnerIdZero` key won't break an assertion.)
    Create `QbXmlOptionsBindingTests.cs` in the test project, `namespace QbConnectService.Tests`: build an `IConfiguration` from an in-memory dictionary mirroring the `"QbXml"` section (`["QbXml:Version"]="16.0"`, `["QbXml:OwnerIdZero"]="false"`, `["QbXml:MaxReturned"]="100"`, `["QbXml:MaxResponseBytes"]="5000000"`, `["QbXml:SpillPath"]=""`), `var o = configuration.GetSection("QbXml").Get<QbXmlOptions>();` and assert `o.Version == "16.0"`, `o.OwnerIdZero == false`, `o.MaxReturned == 100`, `o.MaxResponseBytes == 5_000_000`, `o.SpillPath == ""`. Also a `[Fact]` that a `new QbXmlOptions()` (no config) has those same defaults (so the "sensible default" claim of READ-11 is pinned by a test).
  </action>
  <verify>From the repo root: `dotnet build quickbooks/QbConnectService/QbConnectService.sln -c Debug` succeeds; `dotnet test quickbooks/QbConnectService/QbConnectService.sln` is green (all 44 prior tests + the new `QbXmlOptionsBindingTests`); `QbOptions` no longer compiles a reference to `OwnerIdZero` and nothing in the solution references `QbOptions.OwnerIdZero`; the host still boots.</verify>
  <done>`QbXmlOptions` exists in `Qb.Com` (namespace `QbConnectService.Qb`) with the five properties + documented defaults; `OwnerIdZero` is gone from `QbOptions`; `appsettings.sample.json` has the full `"QbXml"` section and no `Qb:OwnerIdZero`; `Program.cs` binds `"QbXml"`; the binding tests pass. Commit: `feat(03-01): add QbXmlOptions, relocate OwnerIdZero, bind QbXml config`.</done>
</task>

<task type="auto">
  <name>Task 2: QbXmlModels (records + IteratorMode) + QbXmlBuilder (envelope + version PI + onError + iterator attrs + OwnerID) + QbXmlBuilderTests + DI</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlModels.cs (new),
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlBuilder.cs (new),
    quickbooks/QbConnectService/src/QbConnectService/Program.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbXmlBuilderTests.cs (new)
  </files>
  <action>
    Create `QbXmlModels.cs` in `Qb.Com`, `namespace QbConnectService.Qb` (per 03-RESEARCH.md "Pattern 2" code skeleton + "Pattern 3"):
    - `public enum IteratorMode { None, Start, Continue }`.
    - `public sealed record QbStatus(string Code, string Severity, string Message)` with `public bool IsError => string.Equals(Severity, "Error", StringComparison.OrdinalIgnoreCase);` and `public static QbStatus FromElement(System.Xml.Linq.XElement e) => new(e.Attribute("statusCode")?.Value ?? "0", e.Attribute("statusSeverity")?.Value ?? "Info", e.Attribute("statusMessage")?.Value ?? "Status OK");`.
    - `public sealed record ParsedElement(string Name, QbStatus Status, string? IteratorId, int? IteratorRemaining, List<Dictionary<string, object?>> Rows);`.
    - `public sealed record ParsedQbXmlResponse(QbStatus Message, List<ParsedElement> Elements, string? RawSpilledTo = null)` with `public ParsedElement First => Elements[0];`.
    - `public sealed record ReportColumn(string Id, string Title, string? Type);`.
    - `public sealed record ReportRow(string RowType, string? Label, string? RowDataType, Dictionary<string, string> Cells);` — `RowType` = the row element's local name (`DataRow`/`SubtotalRow`/`TotalRow`/`TextRow`); `Label`/`RowDataType` from `<RowData value="...">` / its `rowType` attribute; `Cells` keyed by column title.
    - `public sealed record ParsedReport(string? Title, string? Subtitle, string? Basis, List<ReportColumn> Columns, List<ReportRow> Rows);`.
    Create `QbXmlBuilder.cs` in `Qb.Com`, `namespace QbConnectService.Qb` — pure, NO I/O (per 03-RESEARCH.md "Pattern 1" code):
    - `public sealed class QbXmlBuilder` with ctor `QbXmlBuilder(IOptions<QbXmlOptions> opts) => _opts = opts.Value;` and `public string Version => _opts.Version;`.
    - `public static XElement Rq(string requestName, params object[] content) => new(requestName, content);` — Phase 4's ops use this to build a `*Rq` body.
    - `public static XElement WithIterator(XElement queryElement, IteratorMode mode, string? iteratorId = null, int? maxReturned = null, string requestId = "1")`: if `mode == None` return as-is; else `SetAttributeValue("requestID", requestId)`, `SetAttributeValue("iterator", mode == Start ? "Start" : "Continue")`, and if `Continue && iteratorId is not null` `SetAttributeValue("iteratorID", iteratorId)`; if `maxReturned is int m && queryElement.Element("MaxReturned") is null` then `queryElement.AddFirst(new XElement("MaxReturned", m))` (child element, not attribute; don't override an op-supplied one); return `queryElement`.
    - `public static XElement WithOwnerIdZero(XElement queryElement)`: if `queryElement.Element("OwnerID") is null` then `queryElement.Add(new XElement("OwnerID", "0"))`; return it (idempotent).
    - `public string BuildRequest(XElement requestBody) => BuildRequest(new[] { requestBody });`
    - `public string BuildRequest(IEnumerable<XElement> requestBodies)`: build `new XDocument(new XDeclaration("1.0", "utf-8", null), new XProcessingInstruction("qbxml", $"version=\"{_opts.Version}\""), new XElement("QBXML", new XElement("QBXMLMsgsRq", new XAttribute("onError", "stopOnError"), requestBodies)))`; then `using var sw = new Utf8StringWriter(); doc.Save(sw, SaveOptions.None); return sw.ToString();`. Add the private `sealed class Utf8StringWriter : System.IO.StringWriter { public override System.Text.Encoding Encoding => System.Text.Encoding.UTF8; }`.
    In `Program.cs`: add `builder.Services.AddSingleton<QbXmlBuilder>();`.
    Create `QbXmlBuilderTests.cs` in the test project, `namespace QbConnectService.Tests`. Helper: `static QbXmlBuilder Builder(string version = "16.0") => new(Options.Create(new QbXmlOptions { Version = version }));`. Tests:
    - **Full-envelope golden string**: `Builder().BuildRequest(QbXmlBuilder.Rq("CompanyQueryRq"))` produces a string that, when whitespace-normalized (or compared line-by-line), equals the expected `<?xml version="1.0" encoding="utf-8"?>` + `<?qbxml version="16.0"?>` + `<QBXML>` + `<QBXMLMsgsRq onError="stopOnError">` + `<CompanyQueryRq />` + `</QBXMLMsgsRq>` + `</QBXML>`. (Decide your whitespace policy — `SaveOptions.None` keeps indentation; pin whichever you pick. Assert the FULL string, not just `Contains`.)
    - **Version PI tracks config**: `Builder("13.0").BuildRequest(QbXmlBuilder.Rq("CompanyQueryRq"))` contains `<?qbxml version="13.0"?>` and NOT `version="16.0"`.
    - **`WithIterator` Start**: `var b = QbXmlBuilder.Rq("CustomerQueryRq"); QbXmlBuilder.WithIterator(b, IteratorMode.Start, maxReturned: 100, requestId: "1");` then `XElement` has attributes `requestID="1"` and `iterator="Start"` and a first child `<MaxReturned>100</MaxReturned>`, and NO `iteratorID` attribute.
    - **`WithIterator` Continue**: `QbXmlBuilder.WithIterator(QbXmlBuilder.Rq("CustomerQueryRq"), IteratorMode.Continue, iteratorId: "{abc}", requestId: "1")` has `iterator="Continue"` and `iteratorID="{abc}"`.
    - **`WithIterator` doesn't override an op-supplied `MaxReturned`**: `var b = QbXmlBuilder.Rq("CustomerQueryRq", new XElement("MaxReturned", 50)); QbXmlBuilder.WithIterator(b, IteratorMode.Start, maxReturned: 100);` then there is exactly ONE `<MaxReturned>` and its value is `50`.
    - **`WithOwnerIdZero` adds `<OwnerID>0</OwnerID>` and is idempotent**: call it twice on the same element; exactly one `<OwnerID>` child with value `0`.
  </action>
  <verify>From the repo root: `dotnet build` succeeds; `dotnet test` green; `QbXmlBuilderTests` passes including the full-envelope golden string and the version-PI-tracks-config test.</verify>
  <done>`QbXmlModels.cs` (the records + `IteratorMode`) and `QbXmlBuilder.cs` (pure; envelope + `<?xml?>` decl + `<?qbxml version?>` PI from config + `onError="stopOnError"` + `Rq`/`WithIterator`/`WithOwnerIdZero` + `BuildRequest` overloads + `Version`) exist in `Qb.Com`; `Program.cs` registers `QbXmlBuilder` as a singleton; the golden tests pass. Satisfies READ-01 (builder half). Commit: `feat(03-01): add QbXmlModels and QbXmlBuilder`.</done>
</task>

<task type="auto">
  <name>Task 3: QbXmlParser (status, normalized rows, zero-row-as-success, DataExtRet customFields, polymorphic Item type) + entity fixtures + QbXmlParserTests + DI</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbXmlParser.cs (new),
    quickbooks/QbConnectService/src/QbConnectService/Program.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbConnectService.Tests.csproj (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.normal.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.zerorows.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.dataext.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/ItemQueryRs.polymorphic.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/InvoiceAddRs.error.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbXmlParserTests.cs (new)
  </files>
  <action>
    Create `QbXmlParser.cs` in `Qb.Com`, `namespace QbConnectService.Qb` — pure, NO I/O (per 03-RESEARCH.md "Pattern 2" code skeleton; tidy the recursion as you see fit — the *contract* is what matters):
    - `public sealed class QbXmlParser` with `public ParsedQbXmlResponse Parse(string rawQbXmlResponse)`:
      - `var doc = XDocument.Parse(rawQbXmlResponse);` (do NOT pre-process the string — `XDocument.Parse` handles a BOM, leading whitespace, the `<?xml?>` decl). On any `System.Xml.XmlException` rethrow as `QbXmlParseException` with a helpful message.
      - `var msgs = doc.Root?.Element("QBXMLMsgsRs") ?? throw new QbXmlParseException("Response has no <QBXMLMsgsRs>.");`
      - Message status: if `<QBXMLMsgsRs>` has a `statusSeverity` attribute use `QbStatus.FromElement(msgs)`, else `new QbStatus("0", "Info", "Status OK")` (defensive — some builds put status on the wrapper, some don't).
      - For each child element of `msgs` whose `LocalName` ends with `"Rs"`: `var status = QbStatus.FromElement(rs);` `var iterId = rs.Attribute("iteratorID")?.Value;` `int? iterRemain = int.TryParse(rs.Attribute("iteratorRemainingCount")?.Value, out var n) ? n : null;` `var rows = rs.Elements().Where(IsRetElement).Select(MapRet).ToList();` `elements.Add(new ParsedElement(rs.Name.LocalName, status, iterId, iterRemain, rows));`.
      - `return new ParsedQbXmlResponse(message, elements);`.
    - `IsRetElement(XElement e) => e.Name.LocalName.EndsWith("Ret", StringComparison.Ordinal) && e.Name.LocalName != "DataExtRet";` (`DataExtRet` is handled INSIDE `MapRet` as a nested `customFields` list, never a top-level row).
    - `MapRet(XElement ret) -> Dictionary<string,object?>` (use `StringComparer.Ordinal` for the dict): if `ItemRetNames.Contains(ret.Name.LocalName)` set `row["type"] = ret.Name.LocalName["Item".Length..^"Ret".Length]` (`ItemServiceRet` -> `"Service"`). Then group `ret.Elements()` by `LocalName`: if the group key is `"DataExtRet"`, set `row["customFields"] = group.Select(MapDataExt).ToList()` and continue; otherwise map each element via `MapNode` and store: a single scalar -> the `string`; a single complex element -> the nested `Dictionary`; repeated elements -> a `List<...>`.
    - `MapNode(XElement e) => e.HasElements ? (object)MapComplex(e) : e.Value;` (leaf -> string; complex -> recurse). `MapComplex` reuses the same grouping logic recursively (it's generic — you can literally call `MapRet`'s body, just without the Item-type stamp; refactor as you prefer).
    - `MapDataExt(XElement d) -> Dictionary<string,object?>` = `{ ["OwnerID"]=d.Element("OwnerID")?.Value, ["DataExtName"]=d.Element("DataExtName")?.Value, ["DataExtType"]=d.Element("DataExtType")?.Value, ["DataExtValue"]=d.Element("DataExtValue")?.Value }`.
    - `private static readonly HashSet<string> ItemRetNames = new(StringComparer.Ordinal) { "ItemServiceRet","ItemInventoryRet","ItemNonInventoryRet","ItemInventoryAssemblyRet","ItemFixedAssetRet","ItemOtherChargeRet","ItemSubtotalRet","ItemDiscountRet","ItemPaymentRet","ItemSalesTaxRet","ItemSalesTaxGroupRet","ItemGroupRet" };`.
    - `public sealed class QbXmlParseException(string message) : Exception(message);` (in this file).
    In `Program.cs`: add `builder.Services.AddSingleton<QbXmlParser>();`.
    In `QbConnectService.Tests.csproj`: add a `<None Update="Fixtures\qbxml\*.qbxml"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></None>` item (so all fixture files copy to the test output dir for `File.ReadAllText` at runtime).
    Commit these fixture files under `quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/` (use the sketches in 03-RESEARCH.md "Code Examples" verbatim or close to it):
    - `CustomerQueryRs.normal.qbxml` — `<CustomerQueryRs statusCode="0" statusSeverity="Info" statusMessage="Status OK">` with TWO `<CustomerRet>` (the first with a nested `<BillAddress>`).
    - `CustomerQueryRs.zerorows.qbxml` — `<CustomerQueryRs requestID="1" statusCode="1" statusSeverity="Info" statusMessage="A query request did not find a matching object in QuickBooks" />` (self-closing, no `*Ret`).
    - `CustomerQueryRs.dataext.qbxml` — one `<CustomerRet>` containing two `<DataExtRet>` blocks (`Region/West`, `Tier/Gold`).
    - `ItemQueryRs.polymorphic.qbxml` — `<ItemQueryRs ...>` with one `<ItemServiceRet>` and one `<ItemInventoryRet>` (different field sets).
    - `InvoiceAddRs.error.qbxml` — `<InvoiceAddRs requestID="1" statusCode="3140" statusSeverity="Error" statusMessage="There is an invalid reference ..." />`.
    Create `QbXmlParserTests.cs` in the test project, `namespace QbConnectService.Tests`. Helper: `static string Fixture(string name) => File.ReadAllText(Path.Combine(AppContext.BaseDirectory, "Fixtures", "qbxml", name));`. Tests:
    - **Normal**: parse `CustomerQueryRs.normal.qbxml` -> `Elements.Count == 1`, `Elements[0].Name == "CustomerQueryRs"`, `Status.Code == "0"`, `Status.IsError == false`, `Rows.Count == 2`, `Rows[0]["FullName"]` (string) equals the expected name, `Rows[0]["BillAddress"]` is a `Dictionary<string,object?>` with `["City"]` equal to the expected city, `Rows[1]` does NOT contain `"BillAddress"`.
    - **Zero-row = success (READ-01)**: parse `CustomerQueryRs.zerorows.qbxml` -> NO exception; `Elements[0].Status.IsError == false`; `Elements[0].Status.Code == "1"`; `Elements[0].Status.Message` preserved verbatim; `Elements[0].Rows.Count == 0`.
    - **DataExtRet -> customFields (READ-11 parser half)**: parse `CustomerQueryRs.dataext.qbxml` -> `Rows[0]["customFields"]` is a `List<Dictionary<string,object?>>` with 2 entries; the first has `["DataExtName"] == "Region"` and `["DataExtValue"] == "West"`; `Rows[0]` does NOT contain a top-level `"DataExtRet"` key.
    - **Polymorphic Item -> type discriminator (READ-07 parser half)**: parse `ItemQueryRs.polymorphic.qbxml` -> `Rows.Count == 2`; `Rows[0]["type"] == "Service"`; `Rows[1]["type"] == "Inventory"`; each row has the union of its own fields (e.g. the Inventory row has `"QuantityOnHand"`, the Service row doesn't).
    - **Per-element Error surfaced, not thrown**: parse `InvoiceAddRs.error.qbxml` -> NO exception; `Elements[0].Status.IsError == true`; `Status.Code == "3140"`; `Rows.Count == 0`.
    - **Malformed input throws `QbXmlParseException`**: `Assert.Throws<QbXmlParseException>(() => new QbXmlParser().Parse("<not-xml"));` and `Assert.Throws<QbXmlParseException>(() => new QbXmlParser().Parse("<QBXML><Wrong/></QBXML>"));` (no `<QBXMLMsgsRs>`).
    - (Optional, nice) **Stale-EditSequence statusCode is surfaced verbatim**: a tiny inline string `<QBXML><QBXMLMsgsRs><CustomerModRs statusCode="3200" statusSeverity="Error" statusMessage="The EditSequence ... is out of date." /></QBXMLMsgsRs></QBXML>` -> `Status.Code == "3200"`, `Status.IsError == true` — proving Phase 7 will be able to read it. (Exposing, not handling.)
  </action>
  <verify>From the repo root: `dotnet build` succeeds; `dotnet test` green; all `QbXmlParserTests` pass; the fixture files land in the test output directory (the `File.ReadAllText` calls succeed).</verify>
  <done>`QbXmlParser.cs` exists in `Qb.Com` (pure; `Parse` -> `ParsedQbXmlResponse`; per-`*Rs` `QbStatus` + iterator attrs; `*Ret` -> normalized `Dictionary<string,object?>` with leaf->string / complex->nested dict / repeated->List / `DataExtRet`->`customFields` / Item subtype->`type`; zero-row-as-success; `QbXmlParseException` on malformed input); `Program.cs` registers `QbXmlParser` as a singleton; the entity fixtures are committed and copied to output; the parser tests pass. Satisfies READ-01 (parser half) + the parser halves of READ-07/READ-11. Commit: `feat(03-01): add QbXmlParser + entity fixtures`.</done>
</task>

<task type="auto">
  <name>Task 4: QbReportParser (ColDesc-driven, DataRow/SubtotalRow/TotalRow/TextRow, ColData by colID) + P&L report fixture + QbReportParserTests + DI</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbReportParser.cs (new),
    quickbooks/QbConnectService/src/QbConnectService/Program.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/GeneralSummaryReportQueryRs.pnl.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbReportParserTests.cs (new)
  </files>
  <action>
    Create `QbReportParser.cs` in `Qb.Com`, `namespace QbConnectService.Qb` — pure, NO I/O (per 03-RESEARCH.md "Pattern 3"):
    - `public sealed class QbReportParser` with `public ParsedReport Parse(string rawQbXmlResponse)`:
      - `var doc = XDocument.Parse(rawQbXmlResponse);` (rethrow `XmlException` as `QbXmlParseException`).
      - Find the `<...ReportQueryRs>` element under `<QBXMLMsgsRs>` (the child whose `LocalName` ends with `"ReportQueryRs"`), then its `<ReportRet>` child. If `<ReportRet>` is absent (e.g. an error-status report response), return `new ParsedReport(null, null, null, new(), new())` with the columns/rows empty (don't throw — the caller checks the `*Rs` status separately; but it's fine to also expose nothing).
      - `Title = ReportRet.Element("ReportTitle")?.Value; Subtitle = ReportRet.Element("ReportSubtitle")?.Value; Basis = ReportRet.Element("ReportBasis")?.Value;`.
      - Columns: for each `<ColDesc>` child of `ReportRet` (in document order): `id = colDesc.Attribute("colID")?.Value ?? "";` `title` = TOLERANT — prefer `colDesc.Elements().FirstOrDefault(e => e.Name.LocalName == "ColTitle")?.Attribute("value")?.Value`, then fall back to `colDesc.Attribute("colTitle")?.Value`, then `""` (if there are multiple `<ColTitle>` with `titleRow` attributes, prefer `titleRow="1"`, else the first); `type` = `colDesc.Elements().FirstOrDefault(e => e.Name.LocalName == "ColType")?.Value ?? colDesc.Attribute("colType")?.Value`. Build an ordered `List<ReportColumn>` and a `Dictionary<string,string>` `colId -> title`.
      - Rows: for each child of `<ReportData>` in document order: `rowType = child.Name.LocalName` (`DataRow`/`SubtotalRow`/`TotalRow`/`TextRow` — or whatever it is, store the local name verbatim); the `<RowData>` child (if any): `label = rowData?.Attribute("value")?.Value`, `rowDataType = rowData?.Attribute("rowType")?.Value`; `cells = new Dictionary<string,string>();` for each `<ColData>` child: `var cid = colData.Attribute("colID")?.Value;` if `cid != null && colIdToTitle.TryGetValue(cid, out var t)` then `cells[t] = colData.Attribute("value")?.Value ?? "";` (**match by `colID` — NEVER by XML position; `ColData` can be sparse**). Add `new ReportRow(rowType, label, rowDataType, cells)`.
      - `return new ParsedReport(Title, Subtitle, Basis, columns, rows);`.
      - Use `LocalName` comparisons throughout; null-safe everywhere.
    In `Program.cs`: add `builder.Services.AddSingleton<QbReportParser>();`.
    Commit `GeneralSummaryReportQueryRs.pnl.qbxml` under `Fixtures/qbxml/` — a `ProfitAndLossStandard`-style `<GeneralSummaryReportQueryRs requestID="1" statusCode="0" statusSeverity="Info" statusMessage="Status OK">` containing a `<ReportRet>` with `<ReportTitle>Profit &amp; Loss</ReportTitle>`, `<ReportSubtitle>January 2026</ReportSubtitle>`, `<ReportBasis>AccrualBasis</ReportBasis>`, TWO `<ColDesc>` (`colID="1"` with `<ColTitle titleRow="1" value="" />` and `<ColType>Amount</ColType>`; `colID="2"` with `<ColTitle titleRow="1" value="TOTAL" />` and `<ColType>Amount</ColType>`), and a `<ReportData>` containing at least: a `<TextRow rowNumber="1"><RowData rowType="text" value="Ordinary Income/Expense" /></TextRow>`, a `<DataRow rowNumber="3"><RowData rowType="account" value="Consulting" /><ColData colID="1" value="12000.00" /><ColData colID="2" value="12000.00" /></DataRow>`, a `<SubtotalRow rowNumber="4"><RowData rowType="subtotal" value="Total Income" /><ColData colID="1" value="12000.00" /><ColData colID="2" value="12000.00" /></SubtotalRow>`, and a `<TotalRow rowNumber="12"><RowData rowType="total" value="Net Income" /><ColData colID="1" value="3500.00" /><ColData colID="2" value="3500.00" /></TotalRow>`. **Source this from a real `SDKTestPlus3`/OSR run if you can obtain one; if you construct it, you MUST note in `03-01-SUMMARY.md` that the P&L report fixture is constructed (not captured) and the exact `ColDesc`/`ColTitle`/`ColType`/`RowData` attribute casing should be re-pinned in Phase 9 against the live host.** (The parser is written tolerantly so it works either way; the golden test pins whatever the fixture says.)
    Create `QbReportParserTests.cs` in the test project, `namespace QbConnectService.Tests` (reuse the `Fixture(...)` helper pattern). Tests against `GeneralSummaryReportQueryRs.pnl.qbxml`:
    - `report.Title == "Profit & Loss"` (note the entity-decoded `&`), `report.Subtitle == "January 2026"`, `report.Basis == "AccrualBasis"`.
    - `report.Columns.Count == 2`; `report.Columns[0].Title == ""`; `report.Columns[1].Title == "TOTAL"`; `report.Columns[1].Type == "Amount"`.
    - `report.Rows.Select(r => r.RowType)` is `["TextRow","DataRow","SubtotalRow","TotalRow"]` in that order.
    - The `DataRow`'s `Cells["TOTAL"] == "12000.00"` and its `Label == "Consulting"`.
    - `report.Rows.Last().RowType == "TotalRow"` and `report.Rows.Last().Label == "Net Income"` and `report.Rows.Last().Cells["TOTAL"] == "3500.00"`.
    - (Sparse-`ColData` robustness) Add a tiny inline-string test: a `DataRow` that has only `<ColData colID="2" value="9.99" />` (omits `colID="1"`) -> the parsed row's `Cells` has the `"TOTAL"` key (= `"9.99"`) and does NOT have the column-1 title key; nothing shifts.
  </action>
  <verify>From the repo root: `dotnet build` succeeds; `dotnet test` green; all `QbReportParserTests` pass; the report fixture lands in the test output directory.</verify>
  <done>`QbReportParser.cs` exists in `Qb.Com` (pure; `ColDesc`-driven columns; `ReportData` rows by element name with `ColData` matched by `colID`, never by position; `RowData` as label; tolerant of `ColTitle`/`ColType` element-vs-attribute casing); `Program.cs` registers `QbReportParser` as a singleton; the P&L fixture is committed (and noted in the SUMMARY if constructed); the report tests pass including the sparse-`ColData` case. Satisfies READ-02. Commit: `feat(03-01): add QbReportParser + P&L report fixture`.</done>
</task>

<task type="auto">
  <name>Task 5: QbResponseSpiller (size-guard spill-to-file, SpillPath -> Audit:Path -> %TEMP% fallback) + QbResponseSpillerTests + DI</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbResponseSpiller.cs (new),
    quickbooks/QbConnectService/src/QbConnectService/Program.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbResponseSpillerTests.cs (new)
  </files>
  <action>
    Create `QbResponseSpiller.cs` in `Qb.Com`, `namespace QbConnectService.Qb` (per 03-RESEARCH.md "Pattern 5"):
    - `public sealed class QbResponseSpiller` with ctor `QbResponseSpiller(IOptions<QbXmlOptions> opts, IConfiguration config)` — store `_opts = opts.Value` and the `_auditPath = config["Audit:Path"]` (may be null/empty).
    - `public int Threshold => _opts.MaxResponseBytes;`
    - `public bool ExceedsThreshold(string raw) => System.Text.Encoding.UTF8.GetByteCount(raw) > _opts.MaxResponseBytes;`
    - `public async Task<string> SpillAsync(string rawQbXml, CancellationToken ct = default)`: `var dir = !string.IsNullOrWhiteSpace(_opts.SpillPath) ? _opts.SpillPath : (!string.IsNullOrWhiteSpace(_auditPath) ? _auditPath : Path.Combine(Path.GetTempPath(), "QbConnectService", "spill"));` `Directory.CreateDirectory(dir);` `var file = Path.Combine(dir, $"{DateTime.UtcNow:yyyyMMdd-HHmmssfff}-{Guid.NewGuid():N}.qbxml");` `await File.WriteAllTextAsync(file, rawQbXml, ct);` `return file;`.
    `IConfiguration` is available in `Qb.Com` via its `Microsoft.AspNetCore.App` framework reference (`Microsoft.Extensions.Configuration.Abstractions` is part of the shared framework) — add `using Microsoft.Extensions.Configuration;`.
    In `Program.cs`: add `builder.Services.AddSingleton<QbResponseSpiller>();`.
    Create `QbResponseSpillerTests.cs` in the test project, `namespace QbConnectService.Tests`. Helper to build a spiller with a given temp `SpillPath`: `static QbResponseSpiller Spiller(int maxBytes, string spillPath) => new(Options.Create(new QbXmlOptions { MaxResponseBytes = maxBytes, SpillPath = spillPath }), new ConfigurationBuilder().AddInMemoryCollection(new Dictionary<string,string?>()).Build());`. Each test creates a fresh `var dir = Path.Combine(Path.GetTempPath(), "qbspill-" + Guid.NewGuid().ToString("N"));` and `try { ... } finally { if (Directory.Exists(dir)) Directory.Delete(dir, recursive: true); }`. Tests:
    - **Under threshold**: `Spiller(maxBytes: 1_000_000, spillPath: dir).ExceedsThreshold("<small/>")` is `false`.
    - **Over threshold -> file written**: `var spiller = Spiller(maxBytes: 10, spillPath: dir);` `var big = new string('x', 5000);` `Assert.True(spiller.ExceedsThreshold(big));` `var path = await spiller.SpillAsync(big);` `Assert.True(File.Exists(path));` `Assert.Equal(big, await File.ReadAllTextAsync(path));` `Assert.StartsWith(dir, path);` `Assert.EndsWith(".qbxml", path);`.
    - **Empty `SpillPath` falls back to `Audit:Path`**: build a spiller with `SpillPath = ""` but `IConfiguration` containing `["Audit:Path"] = dir`; `await spiller.SpillAsync("<x/>")` writes a file under `dir`.
    - **Empty `SpillPath` and empty `Audit:Path` falls back to `%TEMP%\QbConnectService\spill`**: build with `SpillPath = ""` and an empty config; `await spiller.SpillAsync("<x/>")` returns a path under `Path.Combine(Path.GetTempPath(), "QbConnectService", "spill")` and the file exists. (Clean up that file/dir afterwards.)
    - **Threshold getter**: `Spiller(maxBytes: 42, spillPath: dir).Threshold == 42`.
  </action>
  <verify>From the repo root: `dotnet build` succeeds; `dotnet test` green; all `QbResponseSpillerTests` pass and clean up their temp dirs.</verify>
  <done>`QbResponseSpiller.cs` exists in `Qb.Com` (`Threshold`, `ExceedsThreshold`, `SpillAsync` -> path; dir resolution `SpillPath` -> `Audit:Path` -> `%TEMP%\QbConnectService\spill`; `Directory.CreateDirectory`; collision-proof `{UTC ts}-{guid:N}.qbxml` filename); `Program.cs` registers it as a singleton; the spiller tests pass. Contributes to READ-03 (the spill half). Commit: `feat(03-01): add QbResponseSpiller`.</done>
</task>

<task type="auto">
  <name>Task 6: FakeRequestProcessor multi-response queue (AddResponses) + ProcessRequests capture (BC-preserving) + FakeRequestProcessorTests extension</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fakes/FakeRequestProcessor.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/FakeRequestProcessorTests.cs (modify)
  </files>
  <action>
    Extend `FakeRequestProcessor.cs` (it's in the test project — extending it is in scope; this is PURELY ADDITIVE and must keep all existing behavior and the 44 existing tests green) per 03-RESEARCH.md "Pitfall 4" + Open Question 5:
    - Add `private readonly Dictionary<string, Queue<string>> _responseQueues = new(StringComparer.OrdinalIgnoreCase);`.
    - Add `public FakeRequestProcessor AddResponses(string requestElementName, params string[] responsesInOrder) { _responseQueues[requestElementName] = new Queue<string>(responsesInOrder); return this; }` — registers an ordered queue of responses for a request name.
    - Add `public List<string> ProcessRequests { get; } = new();` — each call to `ProcessRequest` appends the raw `qbXmlRequest` string (do this AFTER `ThrowIfScripted()`, BEFORE the hook/lookup, so it's captured even when the hook is used).
    - Modify `ProcessRequest`'s resolution order to: (1) `ThrowIfScripted();` (2) `ProcessRequests.Add(qbXmlRequest);` (3) if `ProcessRequestHook is not null` return `ProcessRequestHook(qbXmlRequest);` (4) determine `rqName` exactly as today (parse `QBXMLMsgsRq`'s first child's `LocalName`, fall back to the first `*Rq` descendant); (5) if `rqName != null && _responseQueues.TryGetValue(rqName, out var q) && q.Count > 0` then `return q.Dequeue();` (6) else if `rqName != null && _responses.TryGetValue(rqName, out var response)` `return response;` (7) else if `_responses.TryGetValue("*", out var fallback)` `return fallback;` (8) else `throw new InvalidOperationException($"FakeRequestProcessor: no canned response for request '{rqName ?? "<unparsed>"}'. Call AddResponse(...) or AddResponses(...).");`. Keep `AddResponse`, `EnqueueError`, `EnqueueComError`, `ThrowIfScripted`, `CallLog`, the `Last*` recorded args, `SupportedQbXmlVersions`, `UnattendedModePreference`, `ProcessRequestHook`, and `Dispose` exactly as they are.
    Extend `FakeRequestProcessorTests.cs` (`namespace QbConnectService.Tests`) — ADD these `[Fact]`s without changing the existing four:
    - **Queue dequeues in order then falls back to single response**: `var fake = new FakeRequestProcessor().AddResponse("CustomerQueryRq", "<single/>").AddResponses("CustomerQueryRq", "<page1/>", "<page2/>"); fake.BeginSession("", QbFileMode.DoNotCare);` then three `ProcessRequest(ticket, "<?xml version=\"1.0\"?><QBXML><QBXMLMsgsRq onError=\"stopOnError\"><CustomerQueryRq/></QBXMLMsgsRq></QBXML>")` calls return `"<page1/>"`, `"<page2/>"`, `"<single/>"` (queue first, then the registered single response once the queue is empty).
    - **`ProcessRequests` capture**: after the three calls above, `fake.ProcessRequests.Count == 3` and each entry contains `"CustomerQueryRq"`.
    - **Hook still wins and is still captured**: a fake with `ProcessRequestHook = req => "<hooked/>"` returns `"<hooked/>"` and still appends to `ProcessRequests`.
    - **Existing behavior unchanged**: a fake with only `AddResponse("CompanyQueryRq", "<co/>")` (no queue, no hook) still returns `"<co/>"` for a `CompanyQueryRq` request and still throws `InvalidOperationException` for an unscripted request name.
  </action>
  <verify>From the repo root: `dotnet build` succeeds; `dotnet test` green — the 44 existing tests still pass (this is purely additive in the test project) plus the new `FakeRequestProcessorTests` cases.</verify>
  <done>`FakeRequestProcessor` has `AddResponses(name, params string[])` (per-name `Queue<string>`, dequeued before the single-response lookup, BC-preserving) and a `ProcessRequests` capture list; `AddResponse`/`ProcessRequestHook`/`EnqueueComError`/`CallLog`/`Last*`/`Dispose` are unchanged; the new tests pass and the 44 existing ones stay green. This is the test-infra prerequisite for the two-page iterator test in Task 7. Commit: `feat(03-01): FakeRequestProcessor multi-response queue + request capture`.</done>
</task>

<task type="auto">
  <name>Task 7: QbListExecutor (Start -> Continue -> done accumulation over QbConnectionManager + mid-iteration error abort + size-guard call) + iterator fixtures + QbListExecutorTests + DI</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService.Qb.Com/QbListExecutor.cs (new),
    quickbooks/QbConnectService/src/QbConnectService/Program.cs (modify),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.page1.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/Fixtures/qbxml/CustomerQueryRs.page2.qbxml (new),
    quickbooks/QbConnectService/src/QbConnectService.Tests/QbListExecutorTests.cs (new)
  </files>
  <action>
    Create `QbListExecutor.cs` in `Qb.Com`, `namespace QbConnectService.Qb` — impure (uses `QbConnectionManager` + `QbResponseSpiller` + `ILogger`); per 03-RESEARCH.md "Pattern 4" code:
    - `public sealed class QbListExecutor` with ctor `QbListExecutor(QbConnectionManager mgr, QbXmlBuilder builder, QbXmlParser parser, QbResponseSpiller spiller, IOptions<QbXmlOptions> opts, ILogger<QbListExecutor> log)` — store all.
    - `public async Task<ParsedQbXmlResponse> RunAsync(XElement queryRq, bool? ownerIdZero = null, CancellationToken ct = default)`:
      - `var emitOwnerId = ownerIdZero ?? _opts.OwnerIdZero;` `const string requestId = "1";` `var rawPages = new System.Text.StringBuilder();`
      - **Page 1 (Start)**: `var startBody = new XElement(queryRq);` (clone — never mutate the caller's element) `QbXmlBuilder.WithIterator(startBody, IteratorMode.Start, maxReturned: _opts.MaxReturned, requestId: requestId);` `if (emitOwnerId) QbXmlBuilder.WithOwnerIdZero(startBody);` `var raw = await _mgr.ExecuteAsync(_builder.BuildRequest(startBody), ct);` `rawPages.Append(raw);` `var parsed = _parser.Parse(raw);` `var first = parsed.First;`
      - If `first.Status.IsError`: `return await FinishAsync(parsed, rawPages, ct);` (surface the mid-iteration error with whatever came back; nothing accumulated).
      - `var accumulated = new List<Dictionary<string,object?>>(first.Rows);` `var iteratorId = first.IteratorId;` `var remaining = first.IteratorRemaining ?? 0;`
      - **Pages 2..n (Continue)**: `while (remaining > 0 && iteratorId is not null)`: `var contBody = new XElement(queryRq);` `QbXmlBuilder.WithIterator(contBody, IteratorMode.Continue, iteratorId: iteratorId, maxReturned: _opts.MaxReturned, requestId: requestId);` `if (emitOwnerId) QbXmlBuilder.WithOwnerIdZero(contBody);` `raw = await _mgr.ExecuteAsync(_builder.BuildRequest(contBody), ct);` `rawPages.Append(raw);` `var p = _parser.Parse(raw).First;` if `p.Status.IsError` `return await FinishAsync(new ParsedQbXmlResponse(parsed.Message, new List<ParsedElement> { p with { Rows = accumulated } }), rawPages, ct);` (abort, surface this page's status, keep the rows we already have); else `accumulated.AddRange(p.Rows);` `iteratorId = p.IteratorId ?? iteratorId;` `remaining = p.IteratorRemaining ?? 0;` `_log.LogDebug("Iterator page fetched; {Remaining} remaining, {Total} rows so far.", remaining, accumulated.Count);`.
      - **Done**: `var merged = new ParsedQbXmlResponse(parsed.Message, new List<ParsedElement> { first with { Rows = accumulated, IteratorRemaining = 0 } });` `return await FinishAsync(merged, rawPages, ct);`.
    - `private async Task<ParsedQbXmlResponse> FinishAsync(ParsedQbXmlResponse result, System.Text.StringBuilder rawPages, CancellationToken ct)`: `var s = rawPages.ToString();` `if (_spiller.ExceedsThreshold(s)) { var path = await _spiller.SpillAsync(s, ct); _log.LogInformation("Iterator response {Bytes} bytes exceeded {Limit}; spilled raw qbXML to {Path}.", System.Text.Encoding.UTF8.GetByteCount(s), _spiller.Threshold, path); return result with { RawSpilledTo = path }; }` `return result;`.
      (Keep `requestID` constant across the iteration; take `iteratorID` from each response; `iteratorRemainingCount` of 0 or missing ends the loop; clone the body per page.)
    In `Program.cs`: add `builder.Services.AddSingleton<QbListExecutor>();`.
    Commit `CustomerQueryRs.page1.qbxml` and `CustomerQueryRs.page2.qbxml` under `Fixtures/qbxml/` (per 03-RESEARCH.md "Two-page iterator"): page 1 = `<CustomerQueryRs requestID="1" statusCode="0" statusSeverity="Info" statusMessage="Status OK" iteratorRemainingCount="1" iteratorID="{eb05f701-e727-472f-8ade-6753c4f67a46}"><CustomerRet><ListID>80000001-1</ListID><Name>Acme Roofing</Name></CustomerRet></CustomerQueryRs>` wrapped in `<QBXML><QBXMLMsgsRs>...</QBXMLMsgsRs></QBXML>` with an `<?xml?>` decl; page 2 = same but `iteratorRemainingCount="0"` and `<CustomerRet><ListID>80000002-1</ListID><Name>Globex</Name></CustomerRet>`.
    Create `QbListExecutorTests.cs` in the test project, `namespace QbConnectService.Tests`. Build the dependency chain the way Phase 2's `QbConnectionManagerTests` does: `var fakes = new List<FakeRequestProcessor>(); var fake = new FakeRequestProcessor(); fakes.Add(fake); var mgr = new QbConnectionManager(() => fake, Options.Create(new QbOptions { AppId="app", AppName="QbConnectService", CompanyFilePath=@"C:\co.QBW" }), Options.Create(new RequestOptions { TimeoutSeconds=30, BusyWaitSeconds=5 }), NullLogger<QbConnectionManager>.Instance);` (a single shared fake is fine here — the iterator path never rebuilds the connection). `var opts = Options.Create(new QbXmlOptions { MaxReturned = 1, MaxResponseBytes = 5_000_000 });` `var builder = new QbXmlBuilder(opts);` `var parser = new QbXmlParser();` `var spiller = new QbResponseSpiller(opts, new ConfigurationBuilder().AddInMemoryCollection(new Dictionary<string,string?>()).Build());` `var exec = new QbListExecutor(mgr, builder, parser, spiller, opts, NullLogger<QbListExecutor>.Instance);`. Use the `Fixture(...)` helper for the page files. Always `await mgr.DisposeAsync();` at the end of each test. Tests:
    - **Single page**: `fake.AddResponses("CustomerQueryRq", Fixture("CustomerQueryRs.page2.qbxml"))` (page2 has `iteratorRemainingCount="0"`); `var result = await exec.RunAsync(QbXmlBuilder.Rq("CustomerQueryRq"));` -> `result.First.Rows.Count == 1`, `result.First.Rows[0]["Name"] == "Globex"`, `result.First.IteratorRemaining == 0`, `result.RawSpilledTo == null`; the fake received exactly 1 `ProcessRequest` and its captured request string contains `iterator="Start"`.
    - **Two-page accumulation (READ-03)**: `fake.AddResponses("CustomerQueryRq", Fixture("CustomerQueryRs.page1.qbxml"), Fixture("CustomerQueryRs.page2.qbxml"));` `var result = await exec.RunAsync(QbXmlBuilder.Rq("CustomerQueryRq"));` -> `result.First.Rows.Count == 2`; `result.First.Rows.Select(r => (string)r["Name"]!)` is `["Acme Roofing","Globex"]`; `result.First.IteratorRemaining == 0`; `result.RawSpilledTo == null`. Assert `fake.ProcessRequests.Count == 2`; the FIRST captured request contains `iterator="Start"` and a `<MaxReturned>1</MaxReturned>`; the SECOND captured request contains `iterator="Continue"` and `iteratorID="{eb05f701-e727-472f-8ade-6753c4f67a46}"` and `requestID="1"` (parse it with `XDocument.Parse` and read the `CustomerQueryRq` element's attributes — don't substring-match brittlely).
    - **Size-guard spill (READ-03)**: rebuild `exec` with `opts = Options.Create(new QbXmlOptions { MaxReturned = 1, MaxResponseBytes = 100, SpillPath = <a fresh temp dir> })` (and a matching builder/spiller); `fake.AddResponses("CustomerQueryRq", <a big single-page response: a `<CustomerQueryRs ... iteratorRemainingCount="0">` with one `<CustomerRet>` whose `<Notes>` field is `new string('x', 5000)` — build the string in the test>);` `var result = await exec.RunAsync(QbXmlBuilder.Rq("CustomerQueryRq"));` -> `result.RawSpilledTo` is non-null and `File.Exists(result.RawSpilledTo)` and `(await File.ReadAllTextAsync(result.RawSpilledTo))` equals the big raw response, AND `result.First.Rows.Count == 1` (we parsed before spilling). Clean up the temp dir in a `finally`.
    - **Mid-iteration error on page 2**: page1 = the normal `CustomerQueryRs.page1.qbxml` (`iteratorRemainingCount="1"`); page2err = an inline `<QBXML><QBXMLMsgsRs><CustomerQueryRs requestID="1" statusCode="3200" statusSeverity="Error" statusMessage="Boom on continue" iteratorID="{eb05f701-e727-472f-8ade-6753c4f67a46}" /></QBXMLMsgsRs></QBXML>`; `fake.AddResponses("CustomerQueryRq", Fixture("CustomerQueryRs.page1.qbxml"), page2err);` `var result = await exec.RunAsync(QbXmlBuilder.Rq("CustomerQueryRq"));` -> `result.First.Status.IsError == true`, `result.First.Status.Code == "3200"`, `result.First.Rows.Count == 1` (the page-1 row is retained), `fake.ProcessRequests.Count == 2`.
    - **Caller's element is not mutated**: `var rq = QbXmlBuilder.Rq("CustomerQueryRq"); fake.AddResponses("CustomerQueryRq", Fixture("CustomerQueryRs.page2.qbxml")); await exec.RunAsync(rq);` then `rq.Attribute("iterator") is null` and `rq.Element("MaxReturned") is null` (the builder cloned it per page).
  </action>
  <verify>From the repo root: `dotnet build` succeeds; `dotnet test` green; all `QbListExecutorTests` pass including the two-page accumulation, the size-guard spill (file written, rows still parsed), the mid-iteration error abort, and the no-mutation check; temp dirs are cleaned up.</verify>
  <done>`QbListExecutor.cs` exists in `Qb.Com` (impure; `RunAsync(XElement queryRq, bool? ownerIdZero = null, CancellationToken)`; clones the body per page; `iterator="Start"` + `MaxReturned` then `iterator="Continue"` + `iteratorID` while `iteratorRemainingCount > 0`, same `requestID`; accumulates rows from every page; aborts on `statusSeverity="Error"` surfacing that page's status with prior rows kept; post-accumulation calls `QbResponseSpiller` and sets `RawSpilledTo` when over threshold); `Program.cs` registers it as a singleton; the iterator + spill + error-abort + no-mutation tests pass against `FakeRequestProcessor` through `QbConnectionManager`. Satisfies READ-03. Commit: `feat(03-01): add QbListExecutor + iterator fixtures`.</done>
</task>

<task type="auto">
  <name>Task 8: DI registration smoke + final sweep</name>
  <files>
    quickbooks/QbConnectService/src/QbConnectService/Program.cs (modify — confirm/finalize),
    quickbooks/QbConnectService/src/QbConnectService.Tests/HostStartupTests.cs (modify)
  </files>
  <action>
    Confirm `Program.cs` ends up with ALL of these (add any that were deferred): `builder.Services.Configure<QbXmlOptions>(builder.Configuration.GetSection("QbXml"));`, `builder.Services.AddSingleton<QbXmlBuilder>();`, `builder.Services.AddSingleton<QbXmlParser>();`, `builder.Services.AddSingleton<QbReportParser>();`, `builder.Services.AddSingleton<QbResponseSpiller>();`, `builder.Services.AddSingleton<QbListExecutor>();`. Leave everything else in `Program.cs` (the Worker, the `Func<IRequestProcessor>` registration, `QbConnectionManager`, `MapGet`, `public partial class Program`) untouched.
    Add a host-resolves test to `HostStartupTests.cs` (`namespace QbConnectService.Tests`), following the existing `Host.CreateDefaultBuilder().ConfigureServices(...)` pattern in that file (NOT `WebApplicationFactory` — `HostStartupTests` uses the generic host; match it): a new `[Fact]` `Host_resolves_the_qbXML_engine_singletons_and_QbXmlOptions_defaults` that builds a host registering `Configure<QbXmlOptions>(_ => {})` (or via an in-memory config section), `AddSingleton<QbXmlBuilder>()`, `AddSingleton<QbXmlParser>()`, `AddSingleton<QbReportParser>()`, `AddSingleton<QbResponseSpiller>()`, `AddSingleton<QbListExecutor>()`, plus the minimal deps `QbListExecutor`/`QbResponseSpiller` need (`Configure<QbOptions>(_ => {})`, `Configure<RequestOptions>(_ => {})`, `AddSingleton<Func<IRequestProcessor>>(_ => () => new FakeRequestProcessor())`, `AddSingleton<QbConnectionManager>()`, and an `IConfiguration` is provided by `Host.CreateDefaultBuilder` already), `await host.StartAsync();` then `host.Services.GetRequiredService<QbXmlBuilder>()` / `<QbXmlParser>` / `<QbReportParser>` / `<QbResponseSpiller>` / `<QbListExecutor>` all resolve without throwing, and `host.Services.GetRequiredService<IOptions<QbXmlOptions>>().Value` has `Version == "16.0"`, `OwnerIdZero == false`, `MaxReturned == 100`, `MaxResponseBytes == 5_000_000`, `SpillPath == ""`; `await host.StopAsync(TimeSpan.FromSeconds(5)); host.Dispose();`. (If `QbResponseSpiller` needs `IConfiguration` and the generic host doesn't provide one in your setup, register `services.AddSingleton<IConfiguration>(new ConfigurationBuilder().Build())` — but `Host.CreateDefaultBuilder` does register `IConfiguration`, so it should just work.)
    Then run the final sweep: `dotnet build quickbooks/QbConnectService/QbConnectService.sln -c Debug` AND `dotnet build quickbooks/QbConnectService/QbConnectService.sln -c Release` (Release exercises the `PublishSingleFile`/`win-x86` settings — must still build) AND `dotnet test quickbooks/QbConnectService/QbConnectService.sln` — all green. Confirm no new NuGet `PackageReference` was added to any csproj (only the `<None Update>` fixture-copy item in the test csproj).
  </action>
  <verify>From the repo root: `dotnet build -c Debug` and `dotnet build -c Release` both succeed; `dotnet test` green (the 44 prior tests + every new test from Tasks 1-7 + the new `HostStartupTests` resolve test — total well above 44); `git diff --stat` shows only files under `quickbooks/QbConnectService/`; no csproj gained a `PackageReference`.</verify>
  <done>`Program.cs` registers `Configure<QbXmlOptions>` + all five engine singletons; the host-resolves test proves the whole engine wires up under DI and `QbXmlOptions` binds to its documented defaults; Debug + Release builds and the full test suite are green; zero new NuGet packages. Commit: `feat(03-01): wire qbXML engine DI + host-resolves smoke test`.</done>
</task>

</tasks>

<verification>
After all 8 tasks (and the final SUMMARY commit), the following must hold — re-run from the repo root `C:\VSCode\tech-legal\tech-legal`:
- `dotnet build quickbooks/QbConnectService/QbConnectService.sln -c Debug` — succeeds.
- `dotnet build quickbooks/QbConnectService/QbConnectService.sln -c Release` — succeeds (single-file/x86 settings still build).
- `dotnet test quickbooks/QbConnectService/QbConnectService.sln` — all green; the 44 Phase 1/2 tests still pass; the new `QbXmlOptionsBindingTests`, `QbXmlBuilderTests`, `QbXmlParserTests`, `QbReportParserTests`, `QbResponseSpillerTests`, the extended `FakeRequestProcessorTests`, `QbListExecutorTests`, and the new `HostStartupTests` resolve test all pass.
- `QbXmlBuilder` / `QbXmlParser` / `QbReportParser` reference no I/O (no `System.IO.File`, no `QbConnectionManager`, no `ILogger`); `QbListExecutor` and `QbResponseSpiller` are the only new types touching I/O.
- `git log --oneline` since the plan commit shows 8 `feat(03-01): ...` commits with DISTINCT titles + 1 `docs(03-01): ...` commit; no two `feat(...)` titles repeat; `git diff --stat <plan-commit>..HEAD` lists only files under `quickbooks/QbConnectService/` plus `.planning/phases/03-qbxml-engine/03-01-PLAN.md` and `.planning/phases/03-qbxml-engine/03-01-SUMMARY.md`.
- No `PackageReference` added to any `.csproj`; no nested `.git`; the branch is still `quickbooks/direct-sdk-integration-2026-05-11`; nothing outside `quickbooks/` (other than the two planning docs) was staged.
</verification>

<success_criteria>
- READ-01: `QbXmlBuilder.BuildRequest` always emits the `<?xml?>` declaration + `<?qbxml version="N.N"?>` PI (N.N from `QbXmlOptions.Version`) + `<QBXMLMsgsRq onError="stopOnError">` wrapper — pinned by a golden-string test; `QbXmlParser.Parse` returns `ParsedQbXmlResponse` surfacing per-message and per-`*Rs` `statusCode`/`statusSeverity`/`statusMessage` verbatim; a zero-row response (`statusCode="1"`, `Info`/`Warn`, no `*Ret`) parses as a success with empty `Rows` and `IsError == false` — covered by a test.
- READ-02: `QbReportParser` (a SEPARATE class) reads `ColDesc` to learn columns, walks `ReportData` rows (`DataRow`/`SubtotalRow`/`TotalRow`/`TextRow`), maps `ColData` to columns BY `colID` (never by XML position — covered by a sparse-`ColData` test), captures `RowData` as the label, returns `ParsedReport` — covered by a golden test against a committed P&L fixture (constructed-vs-captured status noted in the SUMMARY).
- READ-03: `QbListExecutor` runs the iterator protocol (`iterator="Start"` + `MaxReturned` -> `iterator="Continue"` + `iteratorID` while `iteratorRemainingCount > 0`, constant `requestID`), accumulates rows from every page into one `ParsedQbXmlResponse`, aborts on a `statusSeverity="Error"` page surfacing that page's status with prior rows kept; a concatenated response over `QbXmlOptions.MaxResponseBytes` is spilled by `QbResponseSpiller` to `<SpillPath ?? Audit:Path ?? %TEMP%\QbConnectService\spill>\<UTC ts>-<guid>.qbxml` and the result carries `RawSpilledTo` (rows still populated) — all covered by tests against `FakeRequestProcessor` via `QbConnectionManager`.
- READ-11: `QbXmlOptions.OwnerIdZero` (relocated from `QbOptions`) is the documented config knob, default `false`, present in `appsettings.sample.json` under `"QbXml"`; `QbXmlBuilder.WithOwnerIdZero` is the (idempotent) capability that adds `<OwnerID>0</OwnerID>`; `QbXmlParser` surfaces `DataExtRet` as `customFields` when present — all covered by tests.
- Build/test green at every commit with no QuickBooks SDK; the 44 existing tests still pass; zero new NuGet packages; all five engine pieces registered as singletons in `Program.cs` with `Configure<QbXmlOptions>`; the pure/impure boundary is intact.
- Repo hygiene: 8 distinct-titled `feat(03-01)` commits + 1 `docs(03-01)` commit; per-task scoped staging; nothing outside `quickbooks/` (bar the two planning docs) touched; still on the branch.
</success_criteria>

<output>
After completing all 8 tasks:
1. Tick every `[ ]` in this plan's `checklist:` frontmatter (in `.planning/phases/03-qbxml-engine/03-01-PLAN.md`).
2. Create `.planning/phases/03-qbxml-engine/03-01-SUMMARY.md` using the summary template (`@C:\Users\rjain\.claude/get-shit-done/templates/summary.md`) — record: the new types and where they live; the relocation of `OwnerIdZero`; whether the P&L report fixture is captured-from-a-real-source or constructed (and if constructed, that the `ColDesc`/`ColTitle`/`ColType`/`RowData` attribute casing should be re-pinned in Phase 9); the `FakeRequestProcessor` extension; that zero new NuGet packages were added; the test count delta; any deviations; and that READ-01/02/03/11 are satisfied at the engine layer (the actual read OPS are Phase 4).
3. Commit both with `docs(03-01): plan checkboxes + Phase 3 summary` (stage only those two files plus, if you only just ticked the checklist, this PLAN file).
4. STOP — Phase 3 is done. Do NOT start Phase 4 work.
</output>
